# ============================================================
# AUTO-GENERATED by scripts/validate_all_specs.py
# Fagun Autonomous QA Platform
# Generated: 2026-06-17T03:17:26.188539Z
# Specs:     70 MD files
# Target:    https://dev.mehadedu.com/en
# DO NOT EDIT — re-run generator to update
# ============================================================
"""
Fagun Autonomous QA Platform — Spec-Driven Test Suite
======================================================
Generated from all specs/*.md files.
Each test class maps 1:1 to a spec MD file.
Test types per spec:
  smoke / navigation / requirement / functional / boundary /
  negative / security (XSS, SQLi, SSTI) / edge_case / performance
"""
from __future__ import annotations
import os as _os
import time
import pytest
from playwright.sync_api import Page

BASE_URL       = _os.getenv("BASE_URL",         "https://dev.mehadedu.com/en")
TEST_EMAIL     = _os.getenv("TEST_EMAIL",       "")
TEST_PASS      = _os.getenv("TEST_PASSWORD",    "")
# Teacher account — real phone registered as tutor in the system
TEACHER_PHONE  = _os.getenv("TEACHER_PHONE",   _os.getenv("TEST_PHONE", "98976564"))
TEACHER_OTP    = _os.getenv("TEACHER_OTP",     _os.getenv("TEST_OTP",   "123456"))
TEACHER_CTRY   = _os.getenv("TEACHER_COUNTRY", _os.getenv("TEST_COUNTRY", "+880"))
# Student account — different real phone registered as student
STUDENT_PHONE  = _os.getenv("STUDENT_PHONE",   "98765432")
STUDENT_OTP    = _os.getenv("STUDENT_OTP",     _os.getenv("TEST_OTP",   "123456"))
STUDENT_CTRY   = _os.getenv("STUDENT_COUNTRY", _os.getenv("TEST_COUNTRY", "+880"))
# Legacy fallbacks
TEST_PHONE     = TEACHER_PHONE
TEST_OTP       = TEACHER_OTP
TEST_COUNTRY   = TEACHER_CTRY

def _otp_login(pg, phone: str, otp: str, country: str, tutor: bool = False):
    """Shared OTP login helper. tutor=True navigates to /en/tutor-login."""
    login_url = _os.getenv("LOGIN_URL", BASE_URL)
    if tutor:
        login_url = BASE_URL.rstrip("/").rsplit("/en", 1)[0] + "/en/tutor-login" if "/en" in BASE_URL else BASE_URL + "/tutor-login"
    pg.goto(login_url, wait_until="commit", timeout=25000)
    pg.wait_for_timeout(2000)
    # For student: click header Login button to open dialog
    # For tutor: the tutor-login page has the form directly (no dialog needed)
    if not tutor:
        login_btn = pg.locator('button:not([aria-label]):has-text("Log In"), button:not([aria-label="Login"]):has-text("Login")').first
        login_btn.wait_for(state='visible', timeout=10000)
        login_btn.click()
        pg.wait_for_selector('[role="dialog"]', state='visible', timeout=10000)
        pg.wait_for_timeout(1000)
        container = pg.locator('[role="dialog"]')
    else:
        container = pg
    # Country code
    cc_btn = container.locator('button[aria-label="Country code"], button:has-text("Country code")').first
    cc_btn.wait_for(state='visible', timeout=8000)
    cc_btn.click()
    pg.wait_for_timeout(700)
    search_input = pg.locator('[role="listbox"] input[placeholder*="Search"], input[placeholder="Search..."]').first
    search_input.wait_for(state='visible', timeout=5000)
    # Search by country name based on dial code
    country_name = "Bangladesh" if country.startswith("+880") else country
    search_input.fill(country_name)
    pg.wait_for_timeout(600)
    pg.locator('[role="option"]:has-text("' + country_name + '")').first.click()
    pg.wait_for_timeout(500)
    # Phone number
    phone_input = container.locator('input[type="tel"], input[placeholder*="123"]').first
    phone_input.wait_for(state='visible', timeout=8000)
    phone_input.fill(phone)
    pg.wait_for_timeout(400)
    # Send Code
    container.locator('button:has-text("Send Code")').first.click()
    pg.wait_for_timeout(2000)
    # OTP
    otp_input = container.locator('input[placeholder="000000"]').first
    otp_input.wait_for(state='visible', timeout=15000)
    for _ in range(30):
        pg.wait_for_timeout(1000)
        if not otp_input.is_disabled():
            break
    otp_input.fill(otp)
    pg.wait_for_timeout(800)
    # Continue
    container.locator('button:has-text("Continue")').first.click()
    pg.wait_for_timeout(4000)

# ── Session auth ──────────────────────────────────────────────────────
# Supports two auth modes (set via env):
#   1. Email/password  → TEST_EMAIL + TEST_PASSWORD
#   2. WhatsApp OTP   → TEACHER_PHONE + TEACHER_OTP (default for Mehad)
# Auth uses REAL registered phone numbers — data written to real database.
@pytest.fixture(scope="session")
def _storage(browser, tmp_path_factory):
    """Teacher/default session: login once, share auth across all spec tests."""
    sf = tmp_path_factory.mktemp("auth") / "teacher_ss.json"
    ctx = browser.new_context(viewport={"width": 1280, "height": 720},
                               ignore_https_errors=True, locale="en-US")
    pg = ctx.new_page()
    try:
        if TEST_EMAIL:
            login_url = _os.getenv("LOGIN_URL", BASE_URL + "/login")
            pg.goto(login_url, wait_until="commit", timeout=25000)
            pg.wait_for_timeout(2000)
            try:
                pg.locator('input[type="email"], input[type="text"]').first.wait_for(state='visible', timeout=20000)
            except Exception:
                pg.wait_for_timeout(3000)
            pg.locator('input[type="email"]').first.fill(TEST_EMAIL)
            pg.locator('input[type="password"]').first.fill(TEST_PASS)
            try:
                pg.locator('button[type="submit"]').first.click()
            except Exception:
                try:
                    pg.locator('button:has-text("Sign in"), button:has-text("Log in"), button:has-text("Login"), button:has-text("Sign In")').first.click()
                except Exception:
                    pg.keyboard.press("Enter")
            pg.wait_for_url(lambda u: "/login" not in u, timeout=30000)
        else:
            _otp_login(pg, TEACHER_PHONE, TEACHER_OTP, TEACHER_CTRY, tutor=True)
        ctx.storage_state(path=str(sf))
    except Exception as exc:
        ctx.close()
        raise RuntimeError(f"Teacher auth setup failed — set TEACHER_PHONE + TEACHER_OTP: {exc}")
    ctx.close()
    yield str(sf)

@pytest.fixture(scope="session")
def _student_storage(browser, tmp_path_factory):
    """Student session: separate login for cross-account data verification."""
    sf = tmp_path_factory.mktemp("auth") / "student_ss.json"
    ctx = browser.new_context(viewport={"width": 1280, "height": 720},
                               ignore_https_errors=True, locale="en-US")
    pg = ctx.new_page()
    try:
        _otp_login(pg, STUDENT_PHONE, STUDENT_OTP, STUDENT_CTRY, tutor=False)
        ctx.storage_state(path=str(sf))
    except Exception as exc:
        ctx.close()
        raise RuntimeError(f"Student auth setup failed — set STUDENT_PHONE + STUDENT_OTP: {exc}")
    ctx.close()
    yield str(sf)

@pytest.fixture(scope="class")
def page(browser, _storage, request):
    """Class-scoped authenticated page fixture (teacher/default account)."""
    ctx = browser.new_context(
        viewport={"width": 1280, "height": 720},
        ignore_https_errors=True, locale="en-US",
        storage_state=_storage,
    )
    pg = ctx.new_page()
    path = getattr(request.cls, "PAGE_PATH", "/")
    pg.goto(BASE_URL + path, wait_until="commit", timeout=45000)
    pg.wait_for_timeout(2500)
    yield pg
    ctx.close()

@pytest.fixture(scope="class")
def student_page(browser, _student_storage, request):
    """Class-scoped student-account page fixture for cross-account tests."""
    ctx = browser.new_context(
        viewport={"width": 1280, "height": 720},
        ignore_https_errors=True, locale="en-US",
        storage_state=_student_storage,
    )
    pg = ctx.new_page()
    path = getattr(request.cls, "PAGE_PATH", "/")
    pg.goto(BASE_URL + path, wait_until="commit", timeout=45000)
    pg.wait_for_timeout(2500)
    yield pg
    ctx.close()




# ==============================================================
# SPEC: Add Admin — Super Admin Dashboard — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_AddAdmin:
    """Auto-generated tests from specs/add_admin.md
    Page: Add Admin — Super Admin Dashboard  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 14  Flows: 3  ECs: 5  Reqs: 7
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Add Admin — Super Admin Dashboard loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__add_new_admin_0002(self, page: Page):
        """Flow: Flow 1: Add New Admin"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__view_admin_profile_0003(self, page: Page):
        """Flow: Flow 2: View Admin Profile"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__delete_admin_0004(self, page: Page):
        """Flow: Flow 3: Delete Admin"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Admin list shows all admins with name, email, phone, role, status, dates"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: First name and last name max 32 characters each"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Email must be valid format and unique"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Phone number validates by country code"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Create button creates admin and shows success"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Delete requires confirmation modal"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Deleted admin cannot login"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0013(self, page: Page):
        """EC-01: First name over 32 chars"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0015(self, page: Page):
        """EC-02: Duplicate email"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0017(self, page: Page):
        """EC-03: Invalid email format"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0019(self, page: Page):
        """EC-04: Empty required fields"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0021(self, page: Page):
        """EC-05: Delete own super admin account"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0022(self, page: Page):
        """Add Admin — Super Admin Dashboard loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Add Admin — Super Admin Dashboard took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0023(self, page: Page):
        """Add Admin — Super Admin Dashboard page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Add Admin — Super Admin Dashboard: page title is empty'
        assert '404' not in title, f'Add Admin — Super Admin Dashboard: title says 404: {title}'
        assert '500' not in title, f'Add Admin — Super Admin Dashboard: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0024(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Add Admin — Super Admin Dashboard mobile viewport 500: {title}'
        assert '502' not in title, f'Add Admin — Super Admin Dashboard mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0025(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Add Admin — Super Admin Dashboard tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0026(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Add Admin — Super Admin Dashboard uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0027(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Add Admin — Super Admin Dashboard: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Add Admin — Super Admin Dashboard: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0028(self, page: Page):
        """Add Admin — Super Admin Dashboard body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Add Admin — Super Admin Dashboard: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0029(self, page: Page):
        """Add Admin — Super Admin Dashboard must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Add Admin — Super Admin Dashboard: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0030(self, page: Page):
        """Add Admin — Super Admin Dashboard must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Add Admin — Super Admin Dashboard: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0031(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Add Admin — Super Admin Dashboard 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0032(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Add Admin — Super Admin Dashboard AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Add Admin — Super Admin Dashboard AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0033(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Add Admin — Super Admin Dashboard: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Add Super Admin — Super Admin Dashboard — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_AddSuperAdmin:
    """Auto-generated tests from specs/add_super_admin.md
    Page: Add Super Admin — Super Admin Dashboard  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 12  Flows: 2  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Add Super Admin — Super Admin Dashboard loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__add_new_super_admin_0002(self, page: Page):
        """Flow: Flow 1: Add New Super Admin"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__delete_super_admin_0003(self, page: Page):
        """Flow: Flow 2: Delete Super Admin"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Super Admin list shows all super admins"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: First/last name max 32 characters"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Email must be unique and valid format"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Password validates strength requirements"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Delete requires confirmation"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Deleted super admin cannot login"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0011(self, page: Page):
        """EC-01: Duplicate email"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0013(self, page: Page):
        """EC-02: Name over 32 chars"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0015(self, page: Page):
        """EC-03: Weak password"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0017(self, page: Page):
        """EC-04: Delete self"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0018(self, page: Page):
        """Add Super Admin — Super Admin Dashboard loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Add Super Admin — Super Admin Dashboard took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0019(self, page: Page):
        """Add Super Admin — Super Admin Dashboard page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Add Super Admin — Super Admin Dashboard: page title is empty'
        assert '404' not in title, f'Add Super Admin — Super Admin Dashboard: title says 404: {title}'
        assert '500' not in title, f'Add Super Admin — Super Admin Dashboard: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0020(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Add Super Admin — Super Admin Dashboard mobile viewport 500: {title}'
        assert '502' not in title, f'Add Super Admin — Super Admin Dashboard mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0021(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Add Super Admin — Super Admin Dashboard tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0022(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Add Super Admin — Super Admin Dashboard uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0023(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Add Super Admin — Super Admin Dashboard: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Add Super Admin — Super Admin Dashboard: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0024(self, page: Page):
        """Add Super Admin — Super Admin Dashboard body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Add Super Admin — Super Admin Dashboard: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0025(self, page: Page):
        """Add Super Admin — Super Admin Dashboard must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Add Super Admin — Super Admin Dashboard: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0026(self, page: Page):
        """Add Super Admin — Super Admin Dashboard must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Add Super Admin — Super Admin Dashboard: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0027(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Add Super Admin — Super Admin Dashboard 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0028(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Add Super Admin — Super Admin Dashboard AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Add Super Admin — Super Admin Dashboard AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0029(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Add Super Admin — Super Admin Dashboard: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Become a Tutor Page Spec — https://dev.mehadedu.com/en/become-tutor
# ==============================================================
class TestSpec_BecomeTutor:
    """Auto-generated tests from specs/become_tutor.md
    Page: Become a Tutor Page Spec  URL: https://dev.mehadedu.com/en/become-tutor
    Selectors: 17  Flows: 6  ECs: 0  Reqs: 6
    """
    PAGE_PATH = '/become-tutor'
    PAGE_URL  = 'https://dev.mehadedu.com/en/become-tutor'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Become a Tutor Page Spec loads without server error."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_with_correct_heading_0002(self, page: Page):
        """Flow: Page loads with correct heading"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_why_choose_mehad_section_has_four_benefi_0003(self, page: Page):
        """Flow: Why Choose Mehad section has four benefit cards"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_how_to_become_a_tutor_section_has_four_n_0004(self, page: Page):
        """Flow: How to become a tutor section has four numbered steps"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_requirements_section_is_visible_0005(self, page: Page):
        """Flow: Requirements section is visible"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_apply_now_button_navigates_to_tutor_logi_0006(self, page: Page):
        """Flow: Apply Now button navigates to tutor-login"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__no_broken_links_or_500_errors_0007(self, page: Page):
        """Flow: Negative — No broken links or 500 errors"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0008(self, page: Page):
        """REQ: REQ-01: the heading Become a tutor on Mehad is visible"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.title.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0009(self, page: Page):
        """REQ: REQ-02: four benefit cards are shown: Earn Good Income, Flexible Schedule, Globa"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.title.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0010(self, page: Page):
        """REQ: REQ-03: four numbered steps are shown: Create Your Account, Set Your Schedule, R"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.title.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0011(self, page: Page):
        """REQ: REQ-04: requirements including Teaching experience or relevant degree are list"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0012(self, page: Page):
        """REQ: REQ-05: the browser navigates to `/en/tutor-login`"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0013(self, page: Page):
        """REQ: REQ-06: no 404 or 500 error appears in the page title or body"""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert page.title() != '', f'Empty title on {page.url}'
        assert '500' not in page.title(), f'Server error: {page.title()!r}'

    @pytest.mark.performance
    def test_load_time_0014(self, page: Page):
        """Become a Tutor Page Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Become a Tutor Page Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0015(self, page: Page):
        """Become a Tutor Page Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Become a Tutor Page Spec: page title is empty'
        assert '404' not in title, f'Become a Tutor Page Spec: title says 404: {title}'
        assert '500' not in title, f'Become a Tutor Page Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0016(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Become a Tutor Page Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Become a Tutor Page Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0017(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Become a Tutor Page Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0018(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Become a Tutor Page Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0019(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Become a Tutor Page Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Become a Tutor Page Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0020(self, page: Page):
        """Become a Tutor Page Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Become a Tutor Page Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0021(self, page: Page):
        """Become a Tutor Page Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Become a Tutor Page Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0022(self, page: Page):
        """Become a Tutor Page Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Become a Tutor Page Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0023(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Become a Tutor Page Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0024(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/become-tutor'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Become a Tutor Page Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Become a Tutor Page Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0025(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Become a Tutor Page Spec: served over HTTP, not HTTPS: {page.url}')

    @pytest.mark.parametrize('boundary', ['', ' ', 'A', 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA', '', 'null', 'undefined', 'None', '0'])
    @pytest.mark.boundary
    def test_boundary_page_title_0026(self, page: Page, boundary: str):
        """Boundary values for - page title."""
        page.goto(BASE_URL + '/become-tutor', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.title.filter(visible=True).first.fill(boundary, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on boundary {boundary!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field - page title not visible on {page.url}')



# ==============================================================
# SPEC: Super Admin 2FA Number Update — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_Change2faNumber:
    """Auto-generated tests from specs/change_2fa_number.md
    Page: Super Admin 2FA Number Update  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 13  Flows: 2  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Super Admin 2FA Number Update loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__super_admin_login_0002(self, page: Page):
        """Flow: Flow 1: Super Admin Login"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__update_2fa_number_0003(self, page: Page):
        """Flow: Flow 2: Update 2FA Number"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Super admin login page available at /en/super-admin-login"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: OTP login flow same as student/tutor"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Account Settings accessible from sidebar"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: 2FA Number card shows current number"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: New number requires OTP verification before saving"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Updated number used for login from next session"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0011(self, page: Page):
        """EC-01: Invalid new phone number"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0013(self, page: Page):
        """EC-02: Wrong verification OTP"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0015(self, page: Page):
        """EC-03: Same number entered"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0017(self, page: Page):
        """EC-04: Network error during update"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0018(self, page: Page):
        """Super Admin 2FA Number Update loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Super Admin 2FA Number Update took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0019(self, page: Page):
        """Super Admin 2FA Number Update page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Super Admin 2FA Number Update: page title is empty'
        assert '404' not in title, f'Super Admin 2FA Number Update: title says 404: {title}'
        assert '500' not in title, f'Super Admin 2FA Number Update: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0020(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin 2FA Number Update mobile viewport 500: {title}'
        assert '502' not in title, f'Super Admin 2FA Number Update mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0021(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin 2FA Number Update tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0022(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Super Admin 2FA Number Update uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0023(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Super Admin 2FA Number Update: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Super Admin 2FA Number Update: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0024(self, page: Page):
        """Super Admin 2FA Number Update body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Super Admin 2FA Number Update: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0025(self, page: Page):
        """Super Admin 2FA Number Update must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Super Admin 2FA Number Update: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0026(self, page: Page):
        """Super Admin 2FA Number Update must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Super Admin 2FA Number Update: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0027(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Super Admin 2FA Number Update 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0028(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin 2FA Number Update AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Super Admin 2FA Number Update AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0029(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Super Admin 2FA Number Update: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Contact Us Page Spec — https://dev.mehadedu.com/en/contact-us
# ==============================================================
class TestSpec_ContactUs:
    """Auto-generated tests from specs/contact_us.md
    Page: Contact Us Page Spec  URL: https://dev.mehadedu.com/en/contact-us
    Selectors: 10  Flows: 5  ECs: 7  Reqs: 5
    """
    PAGE_PATH = '/contact-us'
    PAGE_URL  = 'https://dev.mehadedu.com/en/contact-us'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Contact Us Page Spec loads without server error."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_email_0002(self, page: Page):
        """Element must be present: Email"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.email.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Email' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_with_correct_heading_0003(self, page: Page):
        """Flow: Page loads with correct heading"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_contact_information_section_is_shown_0004(self, page: Page):
        """Flow: Contact Information section is shown"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_message_form_is_present_0005(self, page: Page):
        """Flow: Message form is present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__form_submission_with_empty_fie_0006(self, page: Page):
        """Flow: Negative — Form submission with empty fields shows validatio"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__invalid_email_format_shows_err_0007(self, page: Page):
        """Flow: Negative — Invalid email format shows error"""
        page.wait_for_timeout(200)
        try:
            page.email.fill('qa_test@markopolo.ai', timeout=5000)
        except Exception: pass
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0008(self, page: Page):
        """REQ: REQ-01: the heading Contact Us and subtitle are visible"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        _exp = 'Contact Us'
        t = page.title()
        assert _exp in t or '500' not in t, f'Expected title with {_exp!r}, got {t!r}'

    @pytest.mark.requirement
    def test_req_01_0009(self, page: Page):
        """REQ: REQ-02: the Contact Information heading and business hours note are visible"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.email.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0010(self, page: Page):
        """REQ: REQ-03: a Send us a message heading is visible along with form fields"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.email.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0011(self, page: Page):
        """REQ: REQ-04: validation errors are shown for required fields"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.email.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_04_0012(self, page: Page):
        """REQ: REQ-05: a validation error is shown for the email field"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.email.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.edge_case
    def test_ec_ec_01_0014(self, page: Page):
        """EC-01: the contact us page is loaded"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0016(self, page: Page):
        """EC-02: the user attempts to submit the message form without filling"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0018(self, page: Page):
        """EC-03: validation errors are shown for required fields"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0020(self, page: Page):
        """EC-04: - submit button: `button[type=submit], button:has-text(Send)"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0022(self, page: Page):
        """EC-05: error messages: `[aria-invalid=true], text=/required|please """
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0024(self, page: Page):
        """EC-06: - email input: `input[type=email]`"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0026(self, page: Page):
        """EC-07: error: `[aria-invalid=true], text=/valid email/i`"""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.parametrize('xss_p', ['<script>alert(1)</script>', '"><script>alert(1)</script>', '<img src=x onerror=alert(1)>', '<svg onload=alert(1)>', 'javascript:alert(1)', '<body onload=alert(1)>'])
    @pytest.mark.security
    def test_xss_email_0027(self, page: Page, xss_p: str):
        """XSS payload must not execute in email."""
        alerts: list = []
        page.on('dialog', lambda d: (alerts.append(d.message), d.dismiss()))
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.email.filter(visible=True).first.fill(xss_p, timeout=5000)
            page.wait_for_timeout(500)
            assert len(alerts) == 0, f'XSS executed! payload={xss_p!r} alerts={alerts}'
            assert '500' not in page.title(), f'Server error on XSS: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('sqli_p', ["' OR '1'='1", "' OR '1'='1'--", "' OR 1=1--", "admin'--", "' OR 'x'='x", "'; DROP TABLE users;--"])
    @pytest.mark.security
    def test_sqli_email_0028(self, page: Page, sqli_p: str):
        """SQLi must not expose DB errors in email."""
        db_errs = ['sql syntax', 'mysql_fetch', 'pg_query', 'sqlite_', 'ORA-', 'SQLSTATE', 'syntax error']
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.email.filter(visible=True).first.fill(sqli_p, timeout=5000)
            page.wait_for_timeout(500)
            content = page.content().lower()
            for kw in db_errs:
                assert kw not in content, f'DB error exposed! kw={kw!r} payload={sqli_p!r}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('bad_val', 'notanemail')
    @pytest.mark.negative
    def test_invalid_input_email_0029(self, page: Page, bad_val: str):
        """Invalid input from spec — must not cause server error."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.email.filter(visible=True).first.fill(bad_val, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on invalid {bad_val!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Email not visible on {page.url}')

    @pytest.mark.performance
    def test_load_time_0030(self, page: Page):
        """Contact Us Page Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Contact Us Page Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0031(self, page: Page):
        """Contact Us Page Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Contact Us Page Spec: page title is empty'
        assert '404' not in title, f'Contact Us Page Spec: title says 404: {title}'
        assert '500' not in title, f'Contact Us Page Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0032(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Contact Us Page Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Contact Us Page Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0033(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Contact Us Page Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0034(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Contact Us Page Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0035(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Contact Us Page Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Contact Us Page Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0036(self, page: Page):
        """Contact Us Page Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Contact Us Page Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0037(self, page: Page):
        """Contact Us Page Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Contact Us Page Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0038(self, page: Page):
        """Contact Us Page Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Contact Us Page Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0039(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Contact Us Page Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0040(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/contact-us'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Contact Us Page Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Contact Us Page Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0041(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Contact Us Page Spec: served over HTTP, not HTTPS: {page.url}')

    @pytest.mark.parametrize('bad_email', ['notanemail', '@nodomain', 'no@', 'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa@x.com', ''])
    @pytest.mark.boundary
    def test_email_boundary_0042(self, page: Page, bad_email: str):
        """Email boundary: invalid formats must not crash."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.email.filter(visible=True).first.fill(bad_email, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on email boundary {bad_email!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Email field not visible on {page.url}')

    @pytest.mark.parametrize('value', ['user@example.com', 'test+qa@domain.co.uk', 'qa@markopolo.ai'])
    @pytest.mark.functional
    def test_fill_email_0043(self, page: Page, value: str):
        """Fill Email with valid data — must not crash."""
        page.goto(BASE_URL + '/contact-us', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.email.filter(visible=True).first.fill(value, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error filling Email={value!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Email not found on {page.url}')



# ==============================================================
# SPEC: Dashboard Help Center Spec — https://dev.mehadedu.com/en/dashboard/help-center
# ==============================================================
class TestSpec_DashboardHelpCenter:
    """Auto-generated tests from specs/dashboard_help_center.md
    Page: Dashboard Help Center Spec  URL: https://dev.mehadedu.com/en/dashboard/help-center
    Selectors: 7  Flows: 4  ECs: 0  Reqs: 3
    """
    PAGE_PATH = '/dashboard/help-center'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/help-center'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Dashboard Help Center Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_spec_0002(self, page: Page):
        """Element must be present: Spec"""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.spec.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Spec' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_link_0003(self, page: Page):
        """Element must be present: Link"""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.link.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Link' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_student_can_navigate_to_help_center_from_0004(self, page: Page):
        """Flow: Student can navigate to Help Center from sidebar"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_teacher_can_navigate_to_help_center_from_0005(self, page: Page):
        """Flow: Teacher can navigate to Help Center from sidebar"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_without_errors_0006(self, page: Page):
        """Flow: Page loads without errors"""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__unauthenticated_access_redirec_0007(self, page: Page):
        """Flow: Negative — Unauthenticated access redirects to login"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/home'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0008(self, page: Page):
        """REQ: REQ-01: the browser navigates to `/en/dashboard/help-center` and help content is"""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.spec.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0009(self, page: Page):
        """REQ: REQ-02: no 404 or 500 error is displayed"""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.spec.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0010(self, page: Page):
        """REQ: REQ-03: the user is redirected to the homepage or login prompt"""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0011(self, page: Page):
        """Dashboard Help Center Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Dashboard Help Center Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0012(self, page: Page):
        """Dashboard Help Center Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Dashboard Help Center Spec: page title is empty'
        assert '404' not in title, f'Dashboard Help Center Spec: title says 404: {title}'
        assert '500' not in title, f'Dashboard Help Center Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0013(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Dashboard Help Center Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Dashboard Help Center Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0014(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Dashboard Help Center Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0015(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Dashboard Help Center Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0016(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Dashboard Help Center Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Dashboard Help Center Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0017(self, page: Page):
        """Dashboard Help Center Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Dashboard Help Center Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0018(self, page: Page):
        """Dashboard Help Center Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Dashboard Help Center Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0019(self, page: Page):
        """Dashboard Help Center Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Dashboard Help Center Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0020(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Dashboard Help Center Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0021(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/help-center'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Dashboard Help Center Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Dashboard Help Center Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0022(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/help-center', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Dashboard Help Center Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: End-to-End Data Flow Consistency — https://dev.mehadedu.com/en/dashboard/wallet
# ==============================================================
class TestSpec_DataFlowE2e:
    """Auto-generated tests from specs/data_flow_e2e.md
    Page: Spec: End-to-End Data Flow Consistency  URL: https://dev.mehadedu.com/en/dashboard/wallet
    Selectors: 1  Flows: 0  ECs: 5  Reqs: 37
    """
    PAGE_PATH = '/dashboard/wallet'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/wallet'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: End-to-End Data Flow Consistency loads without server error."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0002(self, page: Page):
        """REQ: REQ-DF01-01: Wallet shows real payment records only — no placeholder rows"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0003(self, page: Page):
        """REQ: REQ-DF01-02: Each transaction row has: amount, source (course/session name), tut"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0004(self, page: Page):
        """REQ: REQ-DF01-03: Transaction status values: `Paid`, `Pending`, `Failed`, `Refunded`"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0005(self, page: Page):
        """REQ: REQ-DF01-04: Booking ID in transaction matches booking ID in My Bookings"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0006(self, page: Page):
        """REQ: REQ-DF01-05: Empty state shown when no transactions exist (not a mock row)"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0007(self, page: Page):
        """REQ: REQ-DF02-01: Booking appears immediately after student books course or 1-to-1 se"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0008(self, page: Page):
        """REQ: REQ-DF02-02: Booking card has: schedule (date + time), tutor info, type (course/"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0009(self, page: Page):
        """REQ: REQ-DF02-03: Booking ID is a real system-generated ID (format: DBK-YYYYMMDD-XXXX"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0010(self, page: Page):
        """REQ: REQ-DF02-04: Status transitions: `Upcoming` → `Active` → `Completed` / `Cancelle"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0011(self, page: Page):
        """REQ: REQ-DF02-05: Cancelled bookings do not appear in Upcoming tab"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0012(self, page: Page):
        """REQ: REQ-DF03-01: Completed session appears in Session History tab"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0013(self, page: Page):
        """REQ: REQ-DF03-02: Recording link visible only for completed sessions (not upcoming/ca"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0014(self, page: Page):
        """REQ: REQ-DF03-03: Recording link resolves to a valid resource (not 404)"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_13_0015(self, page: Page):
        """REQ: REQ-DF03-04: Pending/upcoming sessions have no recording link visible"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_14_0016(self, page: Page):
        """REQ: REQ-DF04-01: All sessions (live, completed, cancelled) visible to Super Admin"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_15_0017(self, page: Page):
        """REQ: REQ-DF04-02: Each row has: student name, tutor name, session type, status, times"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_16_0018(self, page: Page):
        """REQ: REQ-DF04-03: Recording link appears only for completed sessions"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_17_0019(self, page: Page):
        """REQ: REQ-DF04-04: Status filter works: Live / Completed / Cancelled"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_18_0020(self, page: Page):
        """REQ: REQ-DF04-05: Session count matches sum across all status categories"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_19_0021(self, page: Page):
        """REQ: REQ-DF05-01: All tutor-created slots visible in calendar"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_df_01_0023(self, page: Page):
        """EC-DF-01: No bookings exist"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_df_02_0025(self, page: Page):
        """EC-DF-02: Session cancelled after payment"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_df_03_0027(self, page: Page):
        """EC-DF-03: Two students book same tutor slot"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_df_04_0029(self, page: Page):
        """EC-DF-04: Recording not yet processed"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_df_05_0031(self, page: Page):
        """EC-DF-05: Admin filters by student name"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0032(self, page: Page):
        """Spec: End-to-End Data Flow Consistency loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: End-to-End Data Flow Consistency took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0033(self, page: Page):
        """Spec: End-to-End Data Flow Consistency page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: End-to-End Data Flow Consistency: page title is empty'
        assert '404' not in title, f'Spec: End-to-End Data Flow Consistency: title says 404: {title}'
        assert '500' not in title, f'Spec: End-to-End Data Flow Consistency: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0034(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: End-to-End Data Flow Consistency mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: End-to-End Data Flow Consistency mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0035(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: End-to-End Data Flow Consistency tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0036(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: End-to-End Data Flow Consistency uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0037(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: End-to-End Data Flow Consistency: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: End-to-End Data Flow Consistency: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0038(self, page: Page):
        """Spec: End-to-End Data Flow Consistency body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: End-to-End Data Flow Consistency: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0039(self, page: Page):
        """Spec: End-to-End Data Flow Consistency must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: End-to-End Data Flow Consistency: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0040(self, page: Page):
        """Spec: End-to-End Data Flow Consistency must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: End-to-End Data Flow Consistency: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0041(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: End-to-End Data Flow Consistency 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0042(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/wallet'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: End-to-End Data Flow Consistency AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: End-to-End Data Flow Consistency AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0043(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: End-to-End Data Flow Consistency: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Find Tutors Page Spec — https://dev.mehadedu.com/en/find-tutors
# ==============================================================
class TestSpec_FindTutors:
    """Auto-generated tests from specs/find_tutors.md
    Page: Find Tutors Page Spec  URL: https://dev.mehadedu.com/en/find-tutors
    Selectors: 23  Flows: 13  ECs: 0  Reqs: 3
    """
    PAGE_PATH = '/find-tutors'
    PAGE_URL  = 'https://dev.mehadedu.com/en/find-tutors'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Find Tutors Page Spec loads without server error."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_search_0002(self, page: Page):
        """Element must be present: Search"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.search.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Search' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_and_shows_tutor_count_0003(self, page: Page):
        """Flow: Page loads and shows tutor count"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_filter_bar_has_all_four_filters_0004(self, page: Page):
        """Flow: Filter bar has all four filters"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_sort_dropdown_is_present_and_defaults_to_0005(self, page: Page):
        """Flow: Sort dropdown is present and defaults to Our top picks"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_each_tutor_card_has_required_elements_0006(self, page: Page):
        """Flow: Each tutor card has required elements"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_load_more_button_fetches_additional_tuto_0007(self, page: Page):
        """Flow: Load more button fetches additional tutors"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_filtering_by_subject_narrows_results_0008(self, page: Page):
        """Flow: Filtering by subject narrows results"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_keyword_search_filters_tutors_by_name_0009(self, page: Page):
        """Flow: Keyword search filters tutors by name"""
        page.wait_for_timeout(200)
        try:
            page.search.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_tutor_card_click_navigates_to_tutor_prof_0010(self, page: Page):
        """Flow: Tutor card click navigates to tutor profile"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0011(self, page: Page):
        """REQ: REQ-01: only tutors teaching that subject are shown and the URL reflects the fil"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.search.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0012(self, page: Page):
        """REQ: REQ-02: only matching tutors are shown"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.search.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0013(self, page: Page):
        """REQ: REQ-03: only tutors for Math (subjectId=2) are shown and the subject dropdown re"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.search.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.parametrize('xss_p', ['<script>alert(1)</script>', '"><script>alert(1)</script>', '<img src=x onerror=alert(1)>', '<svg onload=alert(1)>', 'javascript:alert(1)', '<body onload=alert(1)>'])
    @pytest.mark.security
    def test_xss_search_0014(self, page: Page, xss_p: str):
        """XSS payload must not execute in search."""
        alerts: list = []
        page.on('dialog', lambda d: (alerts.append(d.message), d.dismiss()))
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(xss_p, timeout=5000)
            page.wait_for_timeout(500)
            assert len(alerts) == 0, f'XSS executed! payload={xss_p!r} alerts={alerts}'
            assert '500' not in page.title(), f'Server error on XSS: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('sqli_p', ["' OR '1'='1", "' OR '1'='1'--", "' OR 1=1--", "admin'--", "' OR 'x'='x", "'; DROP TABLE users;--"])
    @pytest.mark.security
    def test_sqli_search_0015(self, page: Page, sqli_p: str):
        """SQLi must not expose DB errors in search."""
        db_errs = ['sql syntax', 'mysql_fetch', 'pg_query', 'sqlite_', 'ORA-', 'SQLSTATE', 'syntax error']
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(sqli_p, timeout=5000)
            page.wait_for_timeout(500)
            content = page.content().lower()
            for kw in db_errs:
                assert kw not in content, f'DB error exposed! kw={kw!r} payload={sqli_p!r}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('bad_val', ['abc', '000000'])
    @pytest.mark.negative
    def test_invalid_input_search_0016(self, page: Page, bad_val: str):
        """Invalid input from spec — must not cause server error."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(bad_val, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on invalid {bad_val!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.performance
    def test_load_time_0017(self, page: Page):
        """Find Tutors Page Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Find Tutors Page Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0018(self, page: Page):
        """Find Tutors Page Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Find Tutors Page Spec: page title is empty'
        assert '404' not in title, f'Find Tutors Page Spec: title says 404: {title}'
        assert '500' not in title, f'Find Tutors Page Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0019(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Find Tutors Page Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Find Tutors Page Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0020(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Find Tutors Page Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0021(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Find Tutors Page Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0022(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Find Tutors Page Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Find Tutors Page Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0023(self, page: Page):
        """Find Tutors Page Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Find Tutors Page Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0024(self, page: Page):
        """Find Tutors Page Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Find Tutors Page Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0025(self, page: Page):
        """Find Tutors Page Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Find Tutors Page Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0026(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Find Tutors Page Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0027(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/find-tutors'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Find Tutors Page Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Find Tutors Page Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0028(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Find Tutors Page Spec: served over HTTP, not HTTPS: {page.url}')

    @pytest.mark.parametrize('boundary', ['', ' ', 'A', 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA', '', 'null', 'undefined', 'None', '0'])
    @pytest.mark.boundary
    def test_boundary_search_0029(self, page: Page, boundary: str):
        """Boundary values for Search."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(boundary, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on boundary {boundary!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.parametrize('value', ['url: /en/find-tutors', '98976564', '123456'])
    @pytest.mark.functional
    def test_fill_search_0030(self, page: Page, value: str):
        """Fill Search with valid data — must not crash."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(value, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error filling Search={value!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not found on {page.url}')



# ==============================================================
# SPEC: Homepage Spec — https://dev.mehadedu.com/en
# ==============================================================
class TestSpec_Homepage:
    """Auto-generated tests from specs/homepage.md
    Page: Homepage Spec  URL: https://dev.mehadedu.com/en
    Selectors: 40  Flows: 15  ECs: 0  Reqs: 2
    """
    PAGE_PATH = '/'
    PAGE_URL  = 'https://dev.mehadedu.com/en'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Homepage Spec loads without server error."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_nav_0002(self, page: Page):
        """Element must be present: - nav"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.nav.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element '- nav' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_successfully_0003(self, page: Page):
        """Flow: Page loads successfully"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_header_nav_links_are_present_0004(self, page: Page):
        """Flow: Header nav links are present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_log_in_button_opens_login_modal_0005(self, page: Page):
        """Flow: Log In button opens login modal"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_hero_search_widget_has_all_four_filters_0006(self, page: Page):
        """Flow: Hero search widget has all four filters"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_find_a_teacher_button_navigates_to_find__0007(self, page: Page):
        """Flow: Find a Teacher button navigates to find-tutors"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_most_requested_subjects_section_shows_su_0008(self, page: Page):
        """Flow: Most Requested Subjects section shows subject cards"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_top_teachers_section_shows_tutor_cards_w_0009(self, page: Page):
        """Flow: Top Teachers section shows tutor cards with View All link"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_tutor_card_links_navigate_to_profile_0010(self, page: Page):
        """Flow: Tutor card links navigate to profile"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0011(self, page: Page):
        """REQ: REQ-01: at least 5 subject cards are visible with links to `/en/find-tutors?subj"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.nav.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0012(self, page: Page):
        """REQ: REQ-02: at least one tutor card is shown with a View All link to `/en/find-tut"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.nav.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.performance
    def test_load_time_0013(self, page: Page):
        """Homepage Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Homepage Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0014(self, page: Page):
        """Homepage Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Homepage Spec: page title is empty'
        assert '404' not in title, f'Homepage Spec: title says 404: {title}'
        assert '500' not in title, f'Homepage Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0015(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Homepage Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Homepage Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0016(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Homepage Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0017(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Homepage Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0018(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Homepage Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Homepage Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0019(self, page: Page):
        """Homepage Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Homepage Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0020(self, page: Page):
        """Homepage Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Homepage Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0021(self, page: Page):
        """Homepage Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Homepage Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0022(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Homepage Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0023(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Homepage Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Homepage Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0024(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Homepage Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Hours Packages Management — https://dev.mehadedu.com/en/dashboard/packages
# ==============================================================
class TestSpec_HoursPackages:
    """Auto-generated tests from specs/hours_packages.md
    Page: Hours Packages Management  URL: https://dev.mehadedu.com/en/dashboard/packages
    Selectors: 12  Flows: 4  ECs: 5  Reqs: 7
    """
    PAGE_PATH = '/dashboard/packages'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/packages'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Hours Packages Management loads without server error."""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__create_a_package_0002(self, page: Page):
        """Flow: Flow 1: Create a Package"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__edit_a_package_0003(self, page: Page):
        """Flow: Flow 2: Edit a Package"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__toggle_package_visibility_0004(self, page: Page):
        """Flow: Flow 3: Toggle Package Visibility"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_4__search_package_0005(self, page: Page):
        """Flow: Flow 4: Search Package"""
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0006(self, page: Page):
        """REQ: REQ-01: Packages page accessible from Admin Settings"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0007(self, page: Page):
        """REQ: REQ-02: Package name max 100 characters"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0008(self, page: Page):
        """REQ: REQ-03: Active packages visible to students"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0009(self, page: Page):
        """REQ: REQ-04: Disabled packages hidden from students"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0010(self, page: Page):
        """REQ: REQ-05: Edit saves duration and discount correctly"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0011(self, page: Page):
        """REQ: REQ-06: Search returns correct matching packages"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0012(self, page: Page):
        """REQ: REQ-07: Students can purchase active packages"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0014(self, page: Page):
        """EC-01: Package name over 100 chars"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0016(self, page: Page):
        """EC-02: Percentage over 100"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0018(self, page: Page):
        """EC-03: Negative percentage"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0020(self, page: Page):
        """EC-04: Disable active package"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0022(self, page: Page):
        """EC-05: Search with no match"""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0023(self, page: Page):
        """Hours Packages Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Hours Packages Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0024(self, page: Page):
        """Hours Packages Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Hours Packages Management: page title is empty'
        assert '404' not in title, f'Hours Packages Management: title says 404: {title}'
        assert '500' not in title, f'Hours Packages Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0025(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Hours Packages Management mobile viewport 500: {title}'
        assert '502' not in title, f'Hours Packages Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0026(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Hours Packages Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0027(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Hours Packages Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0028(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Hours Packages Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Hours Packages Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0029(self, page: Page):
        """Hours Packages Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Hours Packages Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0030(self, page: Page):
        """Hours Packages Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Hours Packages Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0031(self, page: Page):
        """Hours Packages Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Hours Packages Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0032(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Hours Packages Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0033(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/packages'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Hours Packages Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Hours Packages Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0034(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/packages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Hours Packages Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Instructors — Super Admin Session Management — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_Instructors:
    """Auto-generated tests from specs/instructors.md
    Page: Instructors — Super Admin Session Management  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 11  Flows: 4  ECs: 3  Reqs: 7
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Instructors — Super Admin Session Management loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_all_sessions_0002(self, page: Page):
        """Flow: Flow 1: View All Sessions"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__filter_sessions_by_status_0003(self, page: Page):
        """Flow: Flow 2: Filter Sessions by Status"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__cancel_a_session_0004(self, page: Page):
        """Flow: Flow 3: Cancel a Session"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_4__view_case_file_0005(self, page: Page):
        """Flow: Flow 4: View Case File"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0006(self, page: Page):
        """REQ: REQ-01: Sessions page shows all session records"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0007(self, page: Page):
        """REQ: REQ-02: Each row shows tutor, student, subject, type, status, price, date"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0008(self, page: Page):
        """REQ: REQ-03: Search filters by tutor/student/subject name"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0009(self, page: Page):
        """REQ: REQ-04: Status filter: Pending, Completed, Cancelled"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0010(self, page: Page):
        """REQ: REQ-05: Type filter: One-to-One, Group Session"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0011(self, page: Page):
        """REQ: REQ-06: Cancel session requires confirmation"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0012(self, page: Page):
        """REQ: REQ-07: Cancellation reflected on both tutor and student side"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0014(self, page: Page):
        """EC-01: Search with invalid name"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0016(self, page: Page):
        """EC-02: Cancel already-cancelled session"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0018(self, page: Page):
        """EC-03: Filter shows no results"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0019(self, page: Page):
        """Instructors — Super Admin Session Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Instructors — Super Admin Session Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0020(self, page: Page):
        """Instructors — Super Admin Session Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Instructors — Super Admin Session Management: page title is empty'
        assert '404' not in title, f'Instructors — Super Admin Session Management: title says 404: {title}'
        assert '500' not in title, f'Instructors — Super Admin Session Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0021(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Instructors — Super Admin Session Management mobile viewport 500: {title}'
        assert '502' not in title, f'Instructors — Super Admin Session Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0022(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Instructors — Super Admin Session Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0023(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Instructors — Super Admin Session Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0024(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Instructors — Super Admin Session Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Instructors — Super Admin Session Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0025(self, page: Page):
        """Instructors — Super Admin Session Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Instructors — Super Admin Session Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0026(self, page: Page):
        """Instructors — Super Admin Session Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Instructors — Super Admin Session Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0027(self, page: Page):
        """Instructors — Super Admin Session Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Instructors — Super Admin Session Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0028(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Instructors — Super Admin Session Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0029(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Instructors — Super Admin Session Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Instructors — Super Admin Session Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0030(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Instructors — Super Admin Session Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Login — Homepage & Student Login Modal — https://dev.mehadedu.com/en
# ==============================================================
class TestSpec_Login:
    """Auto-generated tests from specs/login.md
    Page: Login — Homepage & Student Login Modal  URL: https://dev.mehadedu.com/en
    Selectors: 12  Flows: 3  ECs: 8  Reqs: 10
    """
    PAGE_PATH = '/'
    PAGE_URL  = 'https://dev.mehadedu.com/en'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Login — Homepage & Student Login Modal loads without server error."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__successful_student_login_0002(self, page: Page):
        """Flow: Flow 1: Successful Student Login"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__login_modal_opens_and_closes_0003(self, page: Page):
        """Flow: Flow 2: Login Modal Opens and Closes"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__country_code_selection_0004(self, page: Page):
        """Flow: Flow 3: Country Code Selection"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Log In button must be visible in header on unauthenticated state"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Clicking Log In opens a dialog with role=dialog and heading Welcome b"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Country code defaults to +966 (Saudi Arabia) and is changeable"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Phone number field is type=tel with inputmode=numeric"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Send Code button is disabled until phone field has valid input"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: OTP field must be disabled until Send Code is clicked successfully"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: OTP field has maxlength=6 and accepts only numeric input"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Continue button triggers authentication and closes modal on success"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: After successful login, header shows authenticated user name"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: Page title must not contain 500 or 404"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert page.title() != '', f'Empty title on {page.url}'
        assert '500' not in page.title(), f'Server error: {page.title()!r}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: Phone field empty  click Send Code"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: Invalid phone format (letters)"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: Wrong OTP entered (000000)"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0022(self, page: Page):
        """EC-04: Close modal mid-flow"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0024(self, page: Page):
        """EC-05: Already logged-in visits homepage"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0026(self, page: Page):
        """EC-06: Send Code clicked twice"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0028(self, page: Page):
        """EC-07: Phone too short (< 7 digits)"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_08_0030(self, page: Page):
        """EC-08: Network failure during OTP send"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0031(self, page: Page):
        """Login — Homepage & Student Login Modal loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Login — Homepage & Student Login Modal took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0032(self, page: Page):
        """Login — Homepage & Student Login Modal page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Login — Homepage & Student Login Modal: page title is empty'
        assert '404' not in title, f'Login — Homepage & Student Login Modal: title says 404: {title}'
        assert '500' not in title, f'Login — Homepage & Student Login Modal: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0033(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Login — Homepage & Student Login Modal mobile viewport 500: {title}'
        assert '502' not in title, f'Login — Homepage & Student Login Modal mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0034(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Login — Homepage & Student Login Modal tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0035(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Login — Homepage & Student Login Modal uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0036(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Login — Homepage & Student Login Modal: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Login — Homepage & Student Login Modal: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0037(self, page: Page):
        """Login — Homepage & Student Login Modal body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Login — Homepage & Student Login Modal: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0038(self, page: Page):
        """Login — Homepage & Student Login Modal must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Login — Homepage & Student Login Modal: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0039(self, page: Page):
        """Login — Homepage & Student Login Modal must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Login — Homepage & Student Login Modal: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0040(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Login — Homepage & Student Login Modal 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0041(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Login — Homepage & Student Login Modal AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Login — Homepage & Student Login Modal AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0042(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Login — Homepage & Student Login Modal: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Super Admin Login and Reset Password — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_LoginReset:
    """Auto-generated tests from specs/login_reset.md
    Page: Super Admin Login and Reset Password  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 12  Flows: 2  ECs: 5  Reqs: 8
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Super Admin Login and Reset Password loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__super_admin_login_0002(self, page: Page):
        """Flow: Flow 1: Super Admin Login"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__forgot_password_0003(self, page: Page):
        """Flow: Flow 2: Forgot Password"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Super admin login page at /en/super-admin-login"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Phone number validates by country code (max 12 digits)"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Password field is masked"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Invalid credentials show error message"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Error message check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Forgot password sends OTP to registered number"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Forgot password check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: New password must differ from old password"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0010(self, page: Page):
        """REQ: REQ-07: Confirm password must match new password"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0011(self, page: Page):
        """REQ: REQ-08: Old password does not work after reset"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0013(self, page: Page):
        """EC-01: Wrong password"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0015(self, page: Page):
        """EC-02: Invalid phone number"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0017(self, page: Page):
        """EC-03: Wrong OTP in reset"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0019(self, page: Page):
        """EC-04: Passwords do not match"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0021(self, page: Page):
        """EC-05: New password same as old"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0022(self, page: Page):
        """Super Admin Login and Reset Password loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Super Admin Login and Reset Password took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0023(self, page: Page):
        """Super Admin Login and Reset Password page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Super Admin Login and Reset Password: page title is empty'
        assert '404' not in title, f'Super Admin Login and Reset Password: title says 404: {title}'
        assert '500' not in title, f'Super Admin Login and Reset Password: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0024(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin Login and Reset Password mobile viewport 500: {title}'
        assert '502' not in title, f'Super Admin Login and Reset Password mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0025(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin Login and Reset Password tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0026(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Super Admin Login and Reset Password uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0027(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Super Admin Login and Reset Password: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Super Admin Login and Reset Password: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0028(self, page: Page):
        """Super Admin Login and Reset Password body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Super Admin Login and Reset Password: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0029(self, page: Page):
        """Super Admin Login and Reset Password must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Super Admin Login and Reset Password: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0030(self, page: Page):
        """Super Admin Login and Reset Password must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Super Admin Login and Reset Password: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0031(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Super Admin Login and Reset Password 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0032(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin Login and Reset Password AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Super Admin Login and Reset Password AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0033(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Super Admin Login and Reset Password: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Messaging — Student ↔ Tutor — /en/dashboard/messages
# ==============================================================
class TestSpec_Messaging:
    """Auto-generated tests from specs/messaging.md
    Page: Spec: Messaging — Student ↔ Tutor  URL: /en/dashboard/messages
    Selectors: 24  Flows: 5  ECs: 4  Reqs: 20
    """
    PAGE_PATH = '/dashboard/messages'
    PAGE_URL  = '/en/dashboard/messages'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Messaging — Student ↔ Tutor loads without server error."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_row_0002(self, page: Page):
        """Element must be present: Row"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.row.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Row' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_message_0003(self, page: Page):
        """Element must be present: Message"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.message.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Message' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_student_initiates_new_conversation_from__0004(self, page: Page):
        """Flow: Student initiates new conversation from tutor profile"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_student_sends_a_message_0005(self, page: Page):
        """Flow: Student sends a message"""
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_tutor_sees_incoming_message_with_unread__0006(self, page: Page):
        """Flow: Tutor sees incoming message with unread badge"""
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_empty_conversations_state_0007(self, page: Page):
        """Flow: Empty conversations state"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_tutor_has_system_group_conversation_0008(self, page: Page):
        """Flow: Tutor has system/group conversation"""
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0009(self, page: Page):
        """REQ: REQ-01: the browser navigates to /en/dashboard/messages?user=326"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0010(self, page: Page):
        """REQ: REQ-02: the left panel shows a conversation entry for Automations Tutor"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0011(self, page: Page):
        """REQ: REQ-03: the preview shows No messages yet"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0012(self, page: Page):
        """REQ: REQ-04: the center panel shows Direct chat"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0013(self, page: Page):
        """REQ: REQ-05: the message textbox is empty and enabled"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0014(self, page: Page):
        """REQ: REQ-06: the send button is disabled (no text yet)"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0015(self, page: Page):
        """REQ: REQ-07: types Hello teacher, I just booked a session - QA test"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0016(self, page: Page):
        """REQ: REQ-08: the send button becomes enabled"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0017(self, page: Page):
        """REQ: REQ-09: the message appears in the center panel as a sent bubble at the current """
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.row.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_09_0018(self, page: Page):
        """REQ: REQ-10: the conversation list preview updates to show the message text"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0019(self, page: Page):
        """REQ: REQ-11: the timestamp shows e.g. 1:48 PM"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0020(self, page: Page):
        """REQ: REQ-12: the sidebar Messages link shows a badge count of 1"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0021(self, page: Page):
        """REQ: REQ-13: the conversation list shows Automations Student with unread badge 1"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_13_0022(self, page: Page):
        """REQ: REQ-14: the preview shows Hello teacher, I just booked a session - QA test"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_14_0023(self, page: Page):
        """REQ: REQ-15: the timestamp shows 1:48 PM"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_15_0024(self, page: Page):
        """REQ: REQ-16: the center panel shows:"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_16_0025(self, page: Page):
        """REQ: REQ-17: the left panel shows no conversation rows"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_17_0026(self, page: Page):
        """REQ: REQ-18: the conversation list also shows group session threads"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_18_0027(self, page: Page):
        """REQ: REQ-19: one thread is titled QA Math Group Session (system message)"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        _exp = 'QA Math Group Session'
        t = page.title()
        assert _exp in t or '500' not in t, f'Expected title with {_exp!r}, got {t!r}'

    @pytest.mark.requirement
    def test_req_19_0028(self, page: Page):
        """REQ: REQ-20: its preview shows Hello from teacher - QA test message"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0030(self, page: Page):
        """EC-01: No conversations: center panel shows No conversation selecte"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0032(self, page: Page):
        """EC-02: No messages in conversation: conversation preview shows No m"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0034(self, page: Page):
        """EC-03: Send button disabled when message textbox is empty"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0036(self, page: Page):
        """EC-04: Unread count badge shows 0 when all messages read"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0037(self, page: Page):
        """Spec: Messaging — Student ↔ Tutor loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Messaging — Student ↔ Tutor took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0038(self, page: Page):
        """Spec: Messaging — Student ↔ Tutor page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Messaging — Student ↔ Tutor: page title is empty'
        assert '404' not in title, f'Spec: Messaging — Student ↔ Tutor: title says 404: {title}'
        assert '500' not in title, f'Spec: Messaging — Student ↔ Tutor: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0039(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Messaging — Student ↔ Tutor mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Messaging — Student ↔ Tutor mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0040(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Messaging — Student ↔ Tutor tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0041(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Messaging — Student ↔ Tutor uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0042(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Messaging — Student ↔ Tutor: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Messaging — Student ↔ Tutor: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0043(self, page: Page):
        """Spec: Messaging — Student ↔ Tutor body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Messaging — Student ↔ Tutor: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0044(self, page: Page):
        """Spec: Messaging — Student ↔ Tutor must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Messaging — Student ↔ Tutor: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0045(self, page: Page):
        """Spec: Messaging — Student ↔ Tutor must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Messaging — Student ↔ Tutor: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0046(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Messaging — Student ↔ Tutor 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0047(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/messages'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Messaging — Student ↔ Tutor AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Messaging — Student ↔ Tutor AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0048(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Messaging — Student ↔ Tutor: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Notifications Management — Super Admin — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_Notifications:
    """Auto-generated tests from specs/notifications.md
    Page: Notifications Management — Super Admin  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 10  Flows: 2  ECs: 4  Reqs: 7
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Notifications Management — Super Admin loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_and_approve_tutor_profile_c_0002(self, page: Page):
        """Flow: Flow 1: View and Approve Tutor Profile Change"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__reject_profile_change_0003(self, page: Page):
        """Flow: Flow 2: Reject Profile Change"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Notifications page shows All and Unread tabs"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Profile change notifications appear when tutor submits update"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Clicking notification opens tutor profile"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: View button shows approval modal with Approve/Reject"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Approved changes become publicly visible"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Rejected changes include reason for tutor"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0010(self, page: Page):
        """REQ: REQ-07: Rejected changes not visible to students"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: No notifications"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Approve without reviewing"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Reject without reason"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: Unread badge updates on read"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0019(self, page: Page):
        """Notifications Management — Super Admin loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Notifications Management — Super Admin took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0020(self, page: Page):
        """Notifications Management — Super Admin page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Notifications Management — Super Admin: page title is empty'
        assert '404' not in title, f'Notifications Management — Super Admin: title says 404: {title}'
        assert '500' not in title, f'Notifications Management — Super Admin: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0021(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Notifications Management — Super Admin mobile viewport 500: {title}'
        assert '502' not in title, f'Notifications Management — Super Admin mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0022(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Notifications Management — Super Admin tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0023(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Notifications Management — Super Admin uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0024(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Notifications Management — Super Admin: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Notifications Management — Super Admin: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0025(self, page: Page):
        """Notifications Management — Super Admin body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Notifications Management — Super Admin: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0026(self, page: Page):
        """Notifications Management — Super Admin must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Notifications Management — Super Admin: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0027(self, page: Page):
        """Notifications Management — Super Admin must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Notifications Management — Super Admin: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0028(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Notifications Management — Super Admin 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0029(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Notifications Management — Super Admin AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Notifications Management — Super Admin AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0030(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Notifications Management — Super Admin: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Super Admin Payout Management — https://dev.mehadedu.com/en/dashboard/payout
# ==============================================================
class TestSpec_Payout:
    """Auto-generated tests from specs/payout.md
    Page: Super Admin Payout Management  URL: https://dev.mehadedu.com/en/dashboard/payout
    Selectors: 12  Flows: 3  ECs: 3  Reqs: 6
    """
    PAGE_PATH = '/dashboard/payout'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/payout'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Super Admin Payout Management loads without server error."""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__approve_payout_request_0002(self, page: Page):
        """Flow: Flow 1: Approve Payout Request"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__view_session_details_0003(self, page: Page):
        """Flow: Flow 2: View Session Details"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__view_payout_dashboard_stats_0004(self, page: Page):
        """Flow: Flow 3: View Payout Dashboard Stats"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Payout page shows 3 summary cards"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Pending payouts list shows tutor name, sessions, amount, date range, fee"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Approve changes status from Pending to Paid"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Approval updates tutors available balance"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Manual Trigger button creates test payout requests"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Session details modal shows full breakdown"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Approve already-approved payout"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: No pending payouts"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: View details for old payout"""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0017(self, page: Page):
        """Super Admin Payout Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Super Admin Payout Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0018(self, page: Page):
        """Super Admin Payout Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Super Admin Payout Management: page title is empty'
        assert '404' not in title, f'Super Admin Payout Management: title says 404: {title}'
        assert '500' not in title, f'Super Admin Payout Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0019(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin Payout Management mobile viewport 500: {title}'
        assert '502' not in title, f'Super Admin Payout Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0020(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin Payout Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0021(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Super Admin Payout Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0022(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Super Admin Payout Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Super Admin Payout Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0023(self, page: Page):
        """Super Admin Payout Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Super Admin Payout Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0024(self, page: Page):
        """Super Admin Payout Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Super Admin Payout Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0025(self, page: Page):
        """Super Admin Payout Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Super Admin Payout Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0026(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Super Admin Payout Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0027(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/payout'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Super Admin Payout Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Super Admin Payout Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0028(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/payout', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Super Admin Payout Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Platform Fee Management — https://dev.mehadedu.com/en/dashboard/account-settings
# ==============================================================
class TestSpec_Platfromfee:
    """Auto-generated tests from specs/platfromfee.md
    Page: Platform Fee Management  URL: https://dev.mehadedu.com/en/dashboard/account-settings
    Selectors: 5  Flows: 2  ECs: 4  Reqs: 5
    """
    PAGE_PATH = '/dashboard/account-settings'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/account-settings'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Platform Fee Management loads without server error."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__update_platform_fee_0002(self, page: Page):
        """Flow: Flow 1: Update Platform Fee"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__verify_fee_deduction_0003(self, page: Page):
        """Flow: Flow 2: Verify Fee Deduction"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Platform fee configurable from Account Settings"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Fee applies globally to all tutors"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Fee deducted from tutor earnings per session"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Save shows Platform Fee Updated Successfully"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Updated fee visible in tutor earnings breakdown"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0010(self, page: Page):
        """EC-01: Fee value negative"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0012(self, page: Page):
        """EC-02: Fee value over 100%"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0014(self, page: Page):
        """EC-03: Empty fee field"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0016(self, page: Page):
        """EC-04: Fee = 0"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0017(self, page: Page):
        """Platform Fee Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Platform Fee Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0018(self, page: Page):
        """Platform Fee Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Platform Fee Management: page title is empty'
        assert '404' not in title, f'Platform Fee Management: title says 404: {title}'
        assert '500' not in title, f'Platform Fee Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0019(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Platform Fee Management mobile viewport 500: {title}'
        assert '502' not in title, f'Platform Fee Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0020(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Platform Fee Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0021(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Platform Fee Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0022(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Platform Fee Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Platform Fee Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0023(self, page: Page):
        """Platform Fee Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Platform Fee Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0024(self, page: Page):
        """Platform Fee Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Platform Fee Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0025(self, page: Page):
        """Platform Fee Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Platform Fee Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0026(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Platform Fee Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0027(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/account-settings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Platform Fee Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Platform Fee Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0028(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Platform Fee Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Production End-to-End Test Walkthrough — /en/dashboard/availability
# ==============================================================
class TestSpec_ProductionE2e:
    """Auto-generated tests from specs/production_e2e.md
    Page: Production End-to-End Test Walkthrough  URL: /en/dashboard/availability
    Selectors: 5  Flows: 0  ECs: 0  Reqs: 0
    """
    PAGE_PATH = '/dashboard/availability'
    PAGE_URL  = '/en/dashboard/availability'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Production End-to-End Test Walkthrough loads without server error."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_phone_0002(self, page: Page):
        """Element must be present: Phone"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.phone.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Phone' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_otp_0003(self, page: Page):
        """Element must be present: Otp"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.otp.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Otp' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.performance
    def test_load_time_0004(self, page: Page):
        """Production End-to-End Test Walkthrough loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Production End-to-End Test Walkthrough took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0005(self, page: Page):
        """Production End-to-End Test Walkthrough page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Production End-to-End Test Walkthrough: page title is empty'
        assert '404' not in title, f'Production End-to-End Test Walkthrough: title says 404: {title}'
        assert '500' not in title, f'Production End-to-End Test Walkthrough: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0006(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Production End-to-End Test Walkthrough mobile viewport 500: {title}'
        assert '502' not in title, f'Production End-to-End Test Walkthrough mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0007(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Production End-to-End Test Walkthrough tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0008(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Production End-to-End Test Walkthrough uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0009(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Production End-to-End Test Walkthrough: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Production End-to-End Test Walkthrough: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0010(self, page: Page):
        """Production End-to-End Test Walkthrough body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Production End-to-End Test Walkthrough: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0011(self, page: Page):
        """Production End-to-End Test Walkthrough must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Production End-to-End Test Walkthrough: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0012(self, page: Page):
        """Production End-to-End Test Walkthrough must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Production End-to-End Test Walkthrough: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0013(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Production End-to-End Test Walkthrough 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0014(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/availability'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Production End-to-End Test Walkthrough AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Production End-to-End Test Walkthrough AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0015(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Production End-to-End Test Walkthrough: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Promo Code Management — https://dev.mehadedu.com/en/dashboard/promo-codes
# ==============================================================
class TestSpec_PromoCodes:
    """Auto-generated tests from specs/promo_codes.md
    Page: Promo Code Management  URL: https://dev.mehadedu.com/en/dashboard/promo-codes
    Selectors: 15  Flows: 2  ECs: 6  Reqs: 7
    """
    PAGE_PATH = '/dashboard/promo-codes'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/promo-codes'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Promo Code Management loads without server error."""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__create_promo_code_0002(self, page: Page):
        """Flow: Flow 1: Create Promo Code"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__apply_promo_code_at_checkout_0003(self, page: Page):
        """Flow: Flow 2: Apply Promo Code at Checkout"""
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Create Promo Code button opens creation modal"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Code name must be unique"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Discount type: Percentage or Fixed Amount"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Active codes apply discount at checkout"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Disabled codes cannot be used"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Expired codes are blocked"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0010(self, page: Page):
        """REQ: REQ-07: Code stops working after usage limit reached"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Invalid promo code at checkout"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Expired promo code"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Disabled code at checkout"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: Code used past usage limit"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0020(self, page: Page):
        """EC-05: Percentage over 100%"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0022(self, page: Page):
        """EC-06: Negative discount value"""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0023(self, page: Page):
        """Promo Code Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Promo Code Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0024(self, page: Page):
        """Promo Code Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Promo Code Management: page title is empty'
        assert '404' not in title, f'Promo Code Management: title says 404: {title}'
        assert '500' not in title, f'Promo Code Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0025(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Promo Code Management mobile viewport 500: {title}'
        assert '502' not in title, f'Promo Code Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0026(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Promo Code Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0027(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Promo Code Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0028(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Promo Code Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Promo Code Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0029(self, page: Page):
        """Promo Code Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Promo Code Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0030(self, page: Page):
        """Promo Code Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Promo Code Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0031(self, page: Page):
        """Promo Code Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Promo Code Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0032(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Promo Code Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0033(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/promo-codes'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Promo Code Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Promo Code Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0034(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/promo-codes', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Promo Code Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Reports Management — Super Admin — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_Reports:
    """Auto-generated tests from specs/reports.md
    Page: Reports Management — Super Admin  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 14  Flows: 3  ECs: 5  Reqs: 7
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Reports Management — Super Admin loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_reports_dashboard_0002(self, page: Page):
        """Flow: Flow 1: View Reports Dashboard"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__start_review_and_resolve_0003(self, page: Page):
        """Flow: Flow 2: Start Review and Resolve"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__dismiss_report_0004(self, page: Page):
        """Flow: Flow 3: Dismiss Report"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Reports page shows all report statistics cards"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Reports table shows student name, date, topic, status"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Start Review changes status to Under Review"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Mark as Resolved changes status to Resolved"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Dismiss Report dismisses the report"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Filter by All, Pending, Under Review, Resolved, Dismissed"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Search by student name or subject"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0013(self, page: Page):
        """EC-01: No reports exist"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0015(self, page: Page):
        """EC-02: Search with no match"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0017(self, page: Page):
        """EC-03: Dismiss already-dismissed report"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0019(self, page: Page):
        """EC-04: Filter shows no results"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0021(self, page: Page):
        """EC-05: XSS in search input"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'XSS EC check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0022(self, page: Page):
        """Reports Management — Super Admin loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Reports Management — Super Admin took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0023(self, page: Page):
        """Reports Management — Super Admin page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Reports Management — Super Admin: page title is empty'
        assert '404' not in title, f'Reports Management — Super Admin: title says 404: {title}'
        assert '500' not in title, f'Reports Management — Super Admin: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0024(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reports Management — Super Admin mobile viewport 500: {title}'
        assert '502' not in title, f'Reports Management — Super Admin mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0025(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reports Management — Super Admin tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0026(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Reports Management — Super Admin uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0027(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Reports Management — Super Admin: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Reports Management — Super Admin: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0028(self, page: Page):
        """Reports Management — Super Admin body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Reports Management — Super Admin: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0029(self, page: Page):
        """Reports Management — Super Admin must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Reports Management — Super Admin: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0030(self, page: Page):
        """Reports Management — Super Admin must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Reports Management — Super Admin: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0031(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Reports Management — Super Admin 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0032(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reports Management — Super Admin AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Reports Management — Super Admin AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0033(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Reports Management — Super Admin: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Reset Password — Super Admin Account Settings — https://dev.mehadedu.com/en/dashboard/account-settings
# ==============================================================
class TestSpec_ResetPassword:
    """Auto-generated tests from specs/reset_password.md
    Page: Reset Password — Super Admin Account Settings  URL: https://dev.mehadedu.com/en/dashboard/account-settings
    Selectors: 7  Flows: 2  ECs: 5  Reqs: 6
    """
    PAGE_PATH = '/dashboard/account-settings'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/account-settings'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Reset Password — Super Admin Account Settings loads without server error."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__reset_password_successfully_0002(self, page: Page):
        """Flow: Flow 1: Reset Password Successfully"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__password_mismatch_0003(self, page: Page):
        """Flow: Flow 2: Password Mismatch"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Reset Password accessible from Account Settings"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Forgot password check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: New password must be strong"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Confirm password must match new password"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Old password becomes invalid after reset"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Eye icon toggles password visibility"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Toggle check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Success message shown after reset"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0011(self, page: Page):
        """EC-01: Passwords do not match"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0013(self, page: Page):
        """EC-02: Weak password"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0015(self, page: Page):
        """EC-03: Same as old password"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0017(self, page: Page):
        """EC-04: Empty new password field"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0019(self, page: Page):
        """EC-05: Empty confirm password"""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0020(self, page: Page):
        """Reset Password — Super Admin Account Settings loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Reset Password — Super Admin Account Settings took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0021(self, page: Page):
        """Reset Password — Super Admin Account Settings page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Reset Password — Super Admin Account Settings: page title is empty'
        assert '404' not in title, f'Reset Password — Super Admin Account Settings: title says 404: {title}'
        assert '500' not in title, f'Reset Password — Super Admin Account Settings: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0022(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reset Password — Super Admin Account Settings mobile viewport 500: {title}'
        assert '502' not in title, f'Reset Password — Super Admin Account Settings mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0023(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reset Password — Super Admin Account Settings tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0024(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Reset Password — Super Admin Account Settings uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0025(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Reset Password — Super Admin Account Settings: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Reset Password — Super Admin Account Settings: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0026(self, page: Page):
        """Reset Password — Super Admin Account Settings body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Reset Password — Super Admin Account Settings: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0027(self, page: Page):
        """Reset Password — Super Admin Account Settings must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Reset Password — Super Admin Account Settings: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0028(self, page: Page):
        """Reset Password — Super Admin Account Settings must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Reset Password — Super Admin Account Settings: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0029(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Reset Password — Super Admin Account Settings 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0030(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/account-settings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reset Password — Super Admin Account Settings AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Reset Password — Super Admin Account Settings AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0031(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/account-settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Reset Password — Super Admin Account Settings: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Reviews Management — Super Admin — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_Reviews:
    """Auto-generated tests from specs/reviews.md
    Page: Reviews Management — Super Admin  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 12  Flows: 3  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Reviews Management — Super Admin loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__moderate_reviews_0002(self, page: Page):
        """Flow: Flow 1: Moderate Reviews"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__reject_review_0003(self, page: Page):
        """Flow: Flow 2: Reject Review"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__search_and_filter_reviews_0004(self, page: Page):
        """Flow: Flow 3: Search and Filter Reviews"""
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Reviews table shows student name, subject, review, status"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Approve makes review visible on tutor profile"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Reject hides review from public"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Delete permanently removes review"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Search by student name filters correctly"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Filter by status, rating, subject, date works"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Search with no match"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Filter shows no results"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Delete already-deleted review"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: XSS in search input"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'XSS EC check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0019(self, page: Page):
        """Reviews Management — Super Admin loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Reviews Management — Super Admin took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0020(self, page: Page):
        """Reviews Management — Super Admin page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Reviews Management — Super Admin: page title is empty'
        assert '404' not in title, f'Reviews Management — Super Admin: title says 404: {title}'
        assert '500' not in title, f'Reviews Management — Super Admin: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0021(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reviews Management — Super Admin mobile viewport 500: {title}'
        assert '502' not in title, f'Reviews Management — Super Admin mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0022(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reviews Management — Super Admin tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0023(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Reviews Management — Super Admin uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0024(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Reviews Management — Super Admin: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Reviews Management — Super Admin: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0025(self, page: Page):
        """Reviews Management — Super Admin body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Reviews Management — Super Admin: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0026(self, page: Page):
        """Reviews Management — Super Admin must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Reviews Management — Super Admin: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0027(self, page: Page):
        """Reviews Management — Super Admin must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Reviews Management — Super Admin: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0028(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Reviews Management — Super Admin 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0029(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Reviews Management — Super Admin AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Reviews Management — Super Admin AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0030(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Reviews Management — Super Admin: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Session — Join Classroom — https://dev.mehadedu.com/en/dashboard/sessions
# ==============================================================
class TestSpec_Session:
    """Auto-generated tests from specs/session.md
    Page: Session — Join Classroom  URL: https://dev.mehadedu.com/en/dashboard/sessions
    Selectors: 14  Flows: 2  ECs: 6  Reqs: 9
    """
    PAGE_PATH = '/dashboard/sessions'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/sessions'

    pytestmark = pytest.mark.skip(
        reason='Route no longer exists in current app; live session coverage moved to booked sessions and data-flow tests'
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Session — Join Classroom loads without server error."""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__join_classroom_at_session_time_0002(self, page: Page):
        """Flow: Flow 1: Join Classroom at Session Time"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__view_completed_sessions__history_0003(self, page: Page):
        """Flow: Flow 3: View Completed Sessions (History)"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Sessions page shows upcoming and past sessions"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Each session shows tutor name, student name, subject, type, status, pric"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Join Classroom button appears only at scheduled session time"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Clicking Join Classroom opens confirmation modal"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Session automatically closes when end time is reached"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Completed sessions appear in session history"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0010(self, page: Page):
        """REQ: REQ-07: Search functionality filters by tutor/student/subject name"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0011(self, page: Page):
        """REQ: REQ-08: Status filter shows Pending, Completed, Cancelled"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0012(self, page: Page):
        """REQ: REQ-09: Type filter shows 1-to-1 and Group Session options"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0014(self, page: Page):
        """EC-01: Join before session time"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0016(self, page: Page):
        """EC-02: Session end time reached"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0018(self, page: Page):
        """EC-03: Search with invalid name"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0020(self, page: Page):
        """EC-04: Filter Cancelled sessions"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0022(self, page: Page):
        """EC-05: Filter Group sessions"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0024(self, page: Page):
        """EC-06: Session cancelled mid-join"""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.performance
    def test_load_time_0025(self, page: Page):
        """Session — Join Classroom loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Session — Join Classroom took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0026(self, page: Page):
        """Session — Join Classroom page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Session — Join Classroom: page title is empty'
        assert '404' not in title, f'Session — Join Classroom: title says 404: {title}'
        assert '500' not in title, f'Session — Join Classroom: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0027(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Session — Join Classroom mobile viewport 500: {title}'
        assert '502' not in title, f'Session — Join Classroom mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0028(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Session — Join Classroom tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0029(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Session — Join Classroom uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0030(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Session — Join Classroom: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Session — Join Classroom: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0031(self, page: Page):
        """Session — Join Classroom body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Session — Join Classroom: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0032(self, page: Page):
        """Session — Join Classroom must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Session — Join Classroom: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0033(self, page: Page):
        """Session — Join Classroom must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Session — Join Classroom: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0034(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Session — Join Classroom 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0035(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/sessions'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Session — Join Classroom AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Session — Join Classroom AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0036(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Session — Join Classroom: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Static Public Pages Spec — https://dev.mehadedu.com/en/how-mehad-works
# ==============================================================
class TestSpec_StaticPages:
    """Auto-generated tests from specs/static_pages.md
    Page: Static Public Pages Spec  URL: https://dev.mehadedu.com/en/how-mehad-works
    Selectors: 12  Flows: 12  ECs: 0  Reqs: 12
    """
    PAGE_PATH = '/how-mehad-works'
    PAGE_URL  = 'https://dev.mehadedu.com/en/how-mehad-works'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Static Public Pages Spec loads without server error."""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_how_it_works_page_loads_0002(self, page: Page):
        """Flow: How It Works page loads"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_about_us_page_loads_0003(self, page: Page):
        """Flow: About Us page loads"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_faqs_page_loads_and_shows_accordion_0004(self, page: Page):
        """Flow: FAQs page loads and shows accordion"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_pricing_page_loads_0005(self, page: Page):
        """Flow: Pricing page loads"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_subjects_page_loads_0006(self, page: Page):
        """Flow: Subjects page loads"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_privacy_policy_page_loads_0007(self, page: Page):
        """Flow: Privacy Policy page loads"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_terms___conditions_page_loads_0008(self, page: Page):
        """Flow: Terms & Conditions page loads"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_refund_policy_page_loads_0009(self, page: Page):
        """Flow: Refund Policy page loads"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0010(self, page: Page):
        """REQ: REQ-01: the page loads without errors and shows explanatory content"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0011(self, page: Page):
        """REQ: REQ-02: the page loads without a 404 or 500 error"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0012(self, page: Page):
        """REQ: REQ-03: an FAQ section with expandable questions is shown"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0013(self, page: Page):
        """REQ: REQ-04: the page loads without errors and shows pricing information"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0014(self, page: Page):
        """REQ: REQ-05: the page loads without errors and shows a list of subjects"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0015(self, page: Page):
        """REQ: REQ-06: the page loads and shows privacy policy content"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0016(self, page: Page):
        """REQ: REQ-07: the page loads and shows terms content"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0017(self, page: Page):
        """REQ: REQ-08: the page loads and shows refund policy content"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0018(self, page: Page):
        """REQ: REQ-09: the page loads and shows cookie policy content"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0019(self, page: Page):
        """REQ: REQ-10: each link navigates to the correct page without 404"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0020(self, page: Page):
        """REQ: REQ-11: Blog and Careers links are visible"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0021(self, page: Page):
        """REQ: REQ-12: a Page Not Found or 404 message is displayed"""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0022(self, page: Page):
        """Static Public Pages Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Static Public Pages Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0023(self, page: Page):
        """Static Public Pages Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Static Public Pages Spec: page title is empty'
        assert '404' not in title, f'Static Public Pages Spec: title says 404: {title}'
        assert '500' not in title, f'Static Public Pages Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0024(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Static Public Pages Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Static Public Pages Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0025(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Static Public Pages Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0026(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Static Public Pages Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0027(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Static Public Pages Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Static Public Pages Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0028(self, page: Page):
        """Static Public Pages Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Static Public Pages Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0029(self, page: Page):
        """Static Public Pages Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Static Public Pages Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0030(self, page: Page):
        """Static Public Pages Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Static Public Pages Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0031(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Static Public Pages Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0032(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/how-mehad-works'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Static Public Pages Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Static Public Pages Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0033(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/how-mehad-works', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Static Public Pages Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Student — Book 1-on-1 Session — /en/find-tutors?page=1&limit=10&sortBy=createdAt&sortOrder=DESC&sort=top
# ==============================================================
class TestSpec_StudentBookSession:
    """Auto-generated tests from specs/student_book_session.md
    Page: Spec: Student — Book 1-on-1 Session  URL: /en/find-tutors?page=1&limit=10&sortBy=createdAt&sortOrder=DESC&sort=top
    Selectors: 25  Flows: 7  ECs: 4  Reqs: 20
    """
    PAGE_PATH = '/find-tutors'
    PAGE_URL  = '/en/find-tutors?page=1&limit=10&sortBy=createdAt&sortOrder=DESC&sort=top'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Student — Book 1-on-1 Session loads without server error."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_tabs_0002(self, page: Page):
        """Element must be present: Tabs"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.tabs.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Tabs' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_icons_0003(self, page: Page):
        """Element must be present: Icons"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.icons.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Icons' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_apply_0004(self, page: Page):
        """Element must be present: Apply"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.apply.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Apply' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_search_for_a_tutor_by_name_0005(self, page: Page):
        """Flow: Search for a tutor by name"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_navigate_to_tutor_profile_0006(self, page: Page):
        """Flow: Navigate to tutor profile"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_navigate_to_the_week_that_has_availabili_0007(self, page: Page):
        """Flow: Navigate to the week that has availability slots"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_open_booking_modal_via_book_trial_lesson_0008(self, page: Page):
        """Flow: Open booking modal via Book Trial Lesson"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_select_date_and_time_in_booking_modal_0009(self, page: Page):
        """Flow: Select date and time in booking modal"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_review_and_pay_0010(self, page: Page):
        """Flow: Review and pay"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = ''
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_payment_page_structure_0011(self, page: Page):
        """Flow: Payment page structure"""
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0012(self, page: Page):
        """REQ: REQ-01: the URL updates to include ?search=Automations"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0013(self, page: Page):
        """REQ: REQ-02: the results show 1 tutors available"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0014(self, page: Page):
        """REQ: REQ-03: one card with name Automations Tutor is displayed"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.tabs.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0015(self, page: Page):
        """REQ: REQ-04: the card shows: Subject Math, Price 100 SAR /hour, Trial Lesson 100"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0016(self, page: Page):
        """REQ: REQ-05: three buttons: Book Trial Lesson, Message, save"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0017(self, page: Page):
        """REQ: REQ-06: the student is navigated to /en/tutor/89?week=YYYY-MM-DD"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0018(self, page: Page):
        """REQ: REQ-07: the calendar column for Monday shows at least one available time slot"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0019(self, page: Page):
        """REQ: REQ-08: a dialog Book a Session opens"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0020(self, page: Page):
        """REQ: REQ-09: shows Step 1 Date & Time and Step 2 reviewAndDetails"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0021(self, page: Page):
        """REQ: REQ-10: the modal calendar starts at the current week with days disabled"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0022(self, page: Page):
        """REQ: REQ-11: Mon, Tue, Wed are enabled (matching the teachers added slots)"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0023(self, page: Page):
        """REQ: REQ-12: all time slot buttons for Monday are listed below the calendar"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0024(self, page: Page):
        """REQ: REQ-13: that day button becomes [active]"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_13_0025(self, page: Page):
        """REQ: REQ-14: the message Please select a time slot appears"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.tabs.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_14_0026(self, page: Page):
        """REQ: REQ-15: button 10:00 AM becomes [active]"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_15_0027(self, page: Page):
        """REQ: REQ-16: a summary appears:"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.tabs.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_16_0028(self, page: Page):
        """REQ: REQ-17: the Continue button becomes enabled"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_17_0029(self, page: Page):
        """REQ: REQ-18: Step 2 Review Your Booking appears showing:"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.tabs.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_18_0030(self, page: Page):
        """REQ: REQ-19: the student is redirected to /en/payment with params:"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.requirement
    def test_req_19_0031(self, page: Page):
        """REQ: REQ-20: the heading Payment is shown"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.tabs.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.edge_case
    def test_ec_ec_01_0033(self, page: Page):
        """EC-01: Calendar shows No available slots text when tutor has no ava"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0035(self, page: Page):
        """EC-02: Time selection required: Please select a time slot paragraph"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0037(self, page: Page):
        """EC-03: Continue button disabled until both date and time are select"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0039(self, page: Page):
        """EC-04: Student bookings dashboard shows No upcoming sessions found """
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.performance
    def test_load_time_0040(self, page: Page):
        """Spec: Student — Book 1-on-1 Session loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Student — Book 1-on-1 Session took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0041(self, page: Page):
        """Spec: Student — Book 1-on-1 Session page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Student — Book 1-on-1 Session: page title is empty'
        assert '404' not in title, f'Spec: Student — Book 1-on-1 Session: title says 404: {title}'
        assert '500' not in title, f'Spec: Student — Book 1-on-1 Session: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0042(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Book 1-on-1 Session mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Student — Book 1-on-1 Session mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0043(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Book 1-on-1 Session tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0044(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Student — Book 1-on-1 Session uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0045(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Student — Book 1-on-1 Session: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Student — Book 1-on-1 Session: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0046(self, page: Page):
        """Spec: Student — Book 1-on-1 Session body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Student — Book 1-on-1 Session: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0047(self, page: Page):
        """Spec: Student — Book 1-on-1 Session must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Student — Book 1-on-1 Session: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0048(self, page: Page):
        """Spec: Student — Book 1-on-1 Session must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Student — Book 1-on-1 Session: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0049(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Student — Book 1-on-1 Session 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0050(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/find-tutors'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Book 1-on-1 Session AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Student — Book 1-on-1 Session AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0051(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Student — Book 1-on-1 Session: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Dashboard — My Bookings Spec — https://dev.mehadedu.com/en/dashboard/bookings
# ==============================================================
class TestSpec_StudentDashboardBookings:
    """Auto-generated tests from specs/student_dashboard_bookings.md
    Page: Student Dashboard — My Bookings Spec  URL: https://dev.mehadedu.com/en/dashboard/bookings
    Selectors: 24  Flows: 11  ECs: 0  Reqs: 11
    """
    PAGE_PATH = '/dashboard/bookings'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/bookings'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Dashboard — My Bookings Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_link_0002(self, page: Page):
        """Element must be present: Link"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.link.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Link' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_redirects_unauthenticated_users_to__0003(self, page: Page):
        """Flow: Page redirects unauthenticated users to login"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/home'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_authenticated_student_sees_greeting_and__0004(self, page: Page):
        """Flow: Authenticated student sees greeting and booking page"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_sidebar_shows_all_student_navigation_ite_0005(self, page: Page):
        """Flow: Sidebar shows all student navigation items"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_unbooked_hours_counter_is_shown_0006(self, page: Page):
        """Flow: Unbooked hours counter is shown"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_upcoming_sessions_and_session_history_ta_0007(self, page: Page):
        """Flow: Upcoming Sessions and Session History tabs are present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_empty_state_shows_browse___book_cta_0008(self, page: Page):
        """Flow: Empty state shows Browse & Book CTA"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_session_history_tab_switches_view_0009(self, page: Page):
        """Flow: Session History tab switches view"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_sidebar_logo_links_back_to_homepage_0010(self, page: Page):
        """Flow: Sidebar logo links back to homepage"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0011(self, page: Page):
        """REQ: REQ-01: the user is redirected to the homepage or login page"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0012(self, page: Page):
        """REQ: REQ-02: Hello Automations Student heading and Manage your upcoming and past s"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        _exp = 'Hello Automations Student'
        t = page.title()
        assert _exp in t or '500' not in t, f'Expected title with {_exp!r}, got {t!r}'

    @pytest.mark.requirement
    def test_req_02_0013(self, page: Page):
        """REQ: REQ-03: links for My Bookings, Messages, Favorite Teachers, Payment & Wallet, Re"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0014(self, page: Page):
        """REQ: REQ-04: an Unbooked X.X hours button/badge is visible"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_04_0015(self, page: Page):
        """REQ: REQ-05: Upcoming Sessions and Session History tab buttons are visible"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_05_0016(self, page: Page):
        """REQ: REQ-06: Find Your Perfect Teacher section with Browse & Book Sessions button"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_06_0017(self, page: Page):
        """REQ: REQ-07: the view switches to show completed/past sessions (or empty state)"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0018(self, page: Page):
        """REQ: REQ-08: the browser navigates to `/en`"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0019(self, page: Page):
        """REQ: REQ-09: the account switcher shows Automations Student and Student role labe"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0020(self, page: Page):
        """REQ: REQ-10: the sidebar collapses or expands"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0021(self, page: Page):
        """REQ: REQ-11: a Page Not Found message is displayed (correct path is `/en/dashboard/"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.performance
    def test_load_time_0022(self, page: Page):
        """Student Dashboard — My Bookings Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Dashboard — My Bookings Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0023(self, page: Page):
        """Student Dashboard — My Bookings Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Dashboard — My Bookings Spec: page title is empty'
        assert '404' not in title, f'Student Dashboard — My Bookings Spec: title says 404: {title}'
        assert '500' not in title, f'Student Dashboard — My Bookings Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0024(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Dashboard — My Bookings Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Student Dashboard — My Bookings Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0025(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Dashboard — My Bookings Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0026(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Dashboard — My Bookings Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0027(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Dashboard — My Bookings Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Dashboard — My Bookings Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0028(self, page: Page):
        """Student Dashboard — My Bookings Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Dashboard — My Bookings Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0029(self, page: Page):
        """Student Dashboard — My Bookings Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Dashboard — My Bookings Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0030(self, page: Page):
        """Student Dashboard — My Bookings Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Dashboard — My Bookings Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0031(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Dashboard — My Bookings Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0032(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/bookings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Dashboard — My Bookings Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Dashboard — My Bookings Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0033(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Dashboard — My Bookings Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Favorite Teachers — https://dev.mehadedu.com/en/dashboard/favorites
# ==============================================================
class TestSpec_StudentFavorite:
    """Auto-generated tests from specs/student_favorite.md
    Page: Student Favorite Teachers  URL: https://dev.mehadedu.com/en/dashboard/favorites
    Selectors: 9  Flows: 4  ECs: 5  Reqs: 8
    """
    PAGE_PATH = '/dashboard/favorites'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/favorites'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Favorite Teachers loads without server error."""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_favorite_tutors_0002(self, page: Page):
        """Flow: Flow 1: View Favorite Tutors"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__remove_tutor_from_favorites_0003(self, page: Page):
        """Flow: Flow 2: Remove Tutor from Favorites"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__book_lesson_from_favorites_0004(self, page: Page):
        """Flow: Flow 3: Book Lesson from Favorites"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = '/profile'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_4__toggle_favorite_state_0005(self, page: Page):
        """Flow: Flow 4: Toggle Favorite State"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0006(self, page: Page):
        """REQ: REQ-01: Favorites page accessible to authenticated students at /dashboard/favori"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0007(self, page: Page):
        """REQ: REQ-02: Saved tutors listed with name, rating, rate, languages"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0008(self, page: Page):
        """REQ: REQ-03: Heart icon toggles favorite state"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Toggle check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0009(self, page: Page):
        """REQ: REQ-04: Removed tutor disappears from list"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0010(self, page: Page):
        """REQ: REQ-05: Favorite state persists after page refresh"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0011(self, page: Page):
        """REQ: REQ-06: Book Lesson initiates booking flow"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0012(self, page: Page):
        """REQ: REQ-07: Empty state shows when no tutors saved"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0013(self, page: Page):
        """REQ: REQ-08: Browse more tutors links to find-tutors page"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0015(self, page: Page):
        """EC-01: No favorites saved"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0017(self, page: Page):
        """EC-02: Toggle heart rapidly"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Rapid click EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0019(self, page: Page):
        """EC-03: Remove last favorite"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0021(self, page: Page):
        """EC-04: Unauthenticated access"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0023(self, page: Page):
        """EC-05: Tutor becomes inactive"""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0024(self, page: Page):
        """Student Favorite Teachers loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Favorite Teachers took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0025(self, page: Page):
        """Student Favorite Teachers page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Favorite Teachers: page title is empty'
        assert '404' not in title, f'Student Favorite Teachers: title says 404: {title}'
        assert '500' not in title, f'Student Favorite Teachers: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0026(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Favorite Teachers mobile viewport 500: {title}'
        assert '502' not in title, f'Student Favorite Teachers mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0027(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Favorite Teachers tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0028(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Favorite Teachers uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0029(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Favorite Teachers: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Favorite Teachers: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0030(self, page: Page):
        """Student Favorite Teachers body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Favorite Teachers: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0031(self, page: Page):
        """Student Favorite Teachers must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Favorite Teachers: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0032(self, page: Page):
        """Student Favorite Teachers must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Favorite Teachers: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0033(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Favorite Teachers 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0034(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/favorites'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Favorite Teachers AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Favorite Teachers AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0035(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/favorites', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Favorite Teachers: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Find Group Course and Enroll — https://dev.mehadedu.com/en/group-sessions
# ==============================================================
class TestSpec_StudentFindCourseandenrolled:
    """Auto-generated tests from specs/student_find_courseandenrolled.md
    Page: Student Find Group Course and Enroll  URL: https://dev.mehadedu.com/en/group-sessions
    Selectors: 13  Flows: 3  ECs: 5  Reqs: 10
    """
    PAGE_PATH = '/group-sessions'
    PAGE_URL  = 'https://dev.mehadedu.com/en/group-sessions'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Find Group Course and Enroll loads without server error."""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__enroll_in_group_session_0002(self, page: Page):
        """Flow: Flow 1: Enroll in Group Session"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = ''
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__filter_group_sessions_0003(self, page: Page):
        """Flow: Flow 2: Filter Group Sessions"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__prevent_duplicate_enrollment_0004(self, page: Page):
        """Flow: Flow 3: Prevent Duplicate Enrollment"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Group Session accessible from header navigation at `/en/group-sessions`"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Group sessions page shows available courses"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Subject, price, and time filters work correctly"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Each course card shows tutor details and session info"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Enroll Now opens booking modal"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Booking modal has Continue and Confirm & Pay steps"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Payment completes enrollment"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Enrolled course appears in My Bookings"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: Duplicate enrollment is prevented"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: Fully booked sessions show enrollment disabled"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: Duplicate enrollment attempt"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: Session fully booked"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: No sessions match filters"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0022(self, page: Page):
        """EC-04: Payment fails"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0024(self, page: Page):
        """EC-05: Close booking modal before payment"""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0025(self, page: Page):
        """Student Find Group Course and Enroll loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Find Group Course and Enroll took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0026(self, page: Page):
        """Student Find Group Course and Enroll page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Find Group Course and Enroll: page title is empty'
        assert '404' not in title, f'Student Find Group Course and Enroll: title says 404: {title}'
        assert '500' not in title, f'Student Find Group Course and Enroll: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0027(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Find Group Course and Enroll mobile viewport 500: {title}'
        assert '502' not in title, f'Student Find Group Course and Enroll mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0028(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Find Group Course and Enroll tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0029(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Find Group Course and Enroll uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0030(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Find Group Course and Enroll: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Find Group Course and Enroll: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0031(self, page: Page):
        """Student Find Group Course and Enroll body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Find Group Course and Enroll: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0032(self, page: Page):
        """Student Find Group Course and Enroll must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Find Group Course and Enroll: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0033(self, page: Page):
        """Student Find Group Course and Enroll must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Find Group Course and Enroll: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0034(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Find Group Course and Enroll 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0035(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/group-sessions'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Find Group Course and Enroll AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Find Group Course and Enroll AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0036(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Find Group Course and Enroll: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Find Tutor Workflow — https://dev.mehadedu.com/en/find-tutors
# ==============================================================
class TestSpec_StudentFindTutorWorkflow:
    """Auto-generated tests from specs/student_find_tutor_workflow.md
    Page: Student Find Tutor Workflow  URL: https://dev.mehadedu.com/en/find-tutors
    Selectors: 14  Flows: 3  ECs: 7  Reqs: 10
    """
    PAGE_PATH = '/find-tutors'
    PAGE_URL  = 'https://dev.mehadedu.com/en/find-tutors'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Find Tutor Workflow loads without server error."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__find_and_book_1_to_1_session_0002(self, page: Page):
        """Flow: Flow 1: Find and Book 1-to-1 Session"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__find_and_enroll_in_group_session_0003(self, page: Page):
        """Flow: Flow 2: Find and Enroll in Group Session"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__filter_tutors_0004(self, page: Page):
        """Flow: Flow 3: Filter Tutors"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Find Tutors button in header shows dropdown with 1-to-1 and Group Sessio"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: /en/find-tutors shows heading Find your perfect tutor"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Subject, level, price, time filters are present and functional"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Tutor count is displayed (e.g., 64 tutors available)"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Each tutor card shows name, rating, subjects, price"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Book Trial Lesson leads to tutor profile with slot calendar"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Enroll Now on group session opens booking modal"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Group session filter includes I want to learn subject dropdown"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: Load more button loads additional tutors"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: Search input filters tutors by name"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: No tutors match filters"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: All filters applied at once"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: Student tries to enroll same group session twice"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0022(self, page: Page):
        """EC-04: Tutor has no available slots"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0024(self, page: Page):
        """EC-05: Group session fully booked"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0026(self, page: Page):
        """EC-06: Search with special characters"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0028(self, page: Page):
        """EC-07: Load more at end of list"""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0029(self, page: Page):
        """Student Find Tutor Workflow loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Find Tutor Workflow took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0030(self, page: Page):
        """Student Find Tutor Workflow page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Find Tutor Workflow: page title is empty'
        assert '404' not in title, f'Student Find Tutor Workflow: title says 404: {title}'
        assert '500' not in title, f'Student Find Tutor Workflow: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0031(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Find Tutor Workflow mobile viewport 500: {title}'
        assert '502' not in title, f'Student Find Tutor Workflow mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0032(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Find Tutor Workflow tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0033(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Find Tutor Workflow uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0034(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Find Tutor Workflow: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Find Tutor Workflow: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0035(self, page: Page):
        """Student Find Tutor Workflow body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Find Tutor Workflow: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0036(self, page: Page):
        """Student Find Tutor Workflow must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Find Tutor Workflow: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0037(self, page: Page):
        """Student Find Tutor Workflow must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Find Tutor Workflow: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0038(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Find Tutor Workflow 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0039(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/find-tutors'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Find Tutor Workflow AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Find Tutor Workflow AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0040(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/find-tutors', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Find Tutor Workflow: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Student — Enroll in Group Session — /en/tutor/{id}
# ==============================================================
class TestSpec_StudentGroupSessionEnroll:
    """Auto-generated tests from specs/student_group_session_enroll.md
    Page: Spec: Student — Enroll in Group Session  URL: /en/tutor/{id}
    Selectors: 13  Flows: 4  ECs: 3  Reqs: 16
    """
    PAGE_PATH = '/tutor/{id}'
    PAGE_URL  = '/en/tutor/{id}'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Student — Enroll in Group Session loads without server error."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_apply_0002(self, page: Page):
        """Element must be present: Apply"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.apply.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Apply' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_switch_to_group_session_tab_on_tutor_pro_0003(self, page: Page):
        """Flow: Switch to Group Session tab on tutor profile"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_open_group_session_enrollment_modal_0004(self, page: Page):
        """Flow: Open group session enrollment modal"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_proceed_to_step_2_review_0005(self, page: Page):
        """Flow: Proceed to Step 2 review"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_confirm_and_redirect_to_payment_0006(self, page: Page):
        """Flow: Confirm and redirect to payment"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = ''
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0007(self, page: Page):
        """REQ: REQ-01: the 1-to-1 tab is active by default"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0008(self, page: Page):
        """REQ: REQ-02: the tab becomes [active]"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0009(self, page: Page):
        """REQ: REQ-03: the section heading Group Courses appears"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.apply.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0010(self, page: Page):
        """REQ: REQ-04: a group session card is displayed showing:"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.apply.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_04_0011(self, page: Page):
        """REQ: REQ-05: a button Enroll in this batch is shown below the card"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.apply.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_05_0012(self, page: Page):
        """REQ: REQ-06: a dialog Book Group Session opens"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0013(self, page: Page):
        """REQ: REQ-07: the header shows: Book Group Session + Automations Tutor - Math"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0014(self, page: Page):
        """REQ: REQ-08: Step 1 Select Sessions is active"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0015(self, page: Page):
        """REQ: REQ-09: Step 2 Review Details is inactive"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0016(self, page: Page):
        """REQ: REQ-10: the session card shows:"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0017(self, page: Page):
        """REQ: REQ-11: a note: After completing payment, youll schedule all your lessons."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0018(self, page: Page):
        """REQ: REQ-12: a Continue button"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0019(self, page: Page):
        """REQ: REQ-13: the modal advances to Step 2 Review Your Package"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_13_0020(self, page: Page):
        """REQ: REQ-14: Step 1 shows a checkmark (completed)"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_14_0021(self, page: Page):
        """REQ: REQ-15: the review screen shows:"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_15_0022(self, page: Page):
        """REQ: REQ-16: the student is redirected to /en/payment with params:"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0024(self, page: Page):
        """EC-01: No group sessions: The Group Session tab shows empty state i"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0026(self, page: Page):
        """EC-02: Seats filled: When 0/10 Students shows 10/10, the Enroll but"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0028(self, page: Page):
        """EC-03: Payment not completed: Session does not appear in students b"""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.performance
    def test_load_time_0029(self, page: Page):
        """Spec: Student — Enroll in Group Session loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Student — Enroll in Group Session took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0030(self, page: Page):
        """Spec: Student — Enroll in Group Session page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Student — Enroll in Group Session: page title is empty'
        assert '404' not in title, f'Spec: Student — Enroll in Group Session: title says 404: {title}'
        assert '500' not in title, f'Spec: Student — Enroll in Group Session: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0031(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Enroll in Group Session mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Student — Enroll in Group Session mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0032(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Enroll in Group Session tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0033(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Student — Enroll in Group Session uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0034(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Student — Enroll in Group Session: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Student — Enroll in Group Session: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0035(self, page: Page):
        """Spec: Student — Enroll in Group Session body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Student — Enroll in Group Session: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0036(self, page: Page):
        """Spec: Student — Enroll in Group Session must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Student — Enroll in Group Session: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0037(self, page: Page):
        """Spec: Student — Enroll in Group Session must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Student — Enroll in Group Session: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0038(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Student — Enroll in Group Session 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0039(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/tutor/{id}'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Enroll in Group Session AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Student — Enroll in Group Session AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0040(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/tutor/{id}', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Student — Enroll in Group Session: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Hero Section — Homepage Search — https://dev.mehadedu.com/en
# ==============================================================
class TestSpec_StudentHeroSection:
    """Auto-generated tests from specs/student_hero_section.md
    Page: Student Hero Section — Homepage Search  URL: https://dev.mehadedu.com/en
    Selectors: 9  Flows: 3  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/'
    PAGE_URL  = 'https://dev.mehadedu.com/en'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Hero Section — Homepage Search loads without server error."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__find_tutor_via_hero_search_0002(self, page: Page):
        """Flow: Flow 1: Find Tutor via Hero Search"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__search_without_filters_0003(self, page: Page):
        """Flow: Flow 2: Search Without Filters"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__click_subject_badge_0004(self, page: Page):
        """Flow: Flow 3: Click Subject Badge"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Hero section is visible on homepage"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Subject, Level, Availability, Price Range dropdowns are present"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Find a Teacher button navigates to /en/find-tutors"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Applied filters pass as query parameters to find-tutors"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: No login required to use hero search"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Subject badges in STUDY SUBJECTS section link to filtered results"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Click Find a Teacher with no filters"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Select conflicting filters"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Page loads without JavaScript"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: Direct navigation to /en"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0019(self, page: Page):
        """Student Hero Section — Homepage Search loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Hero Section — Homepage Search took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0020(self, page: Page):
        """Student Hero Section — Homepage Search page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Hero Section — Homepage Search: page title is empty'
        assert '404' not in title, f'Student Hero Section — Homepage Search: title says 404: {title}'
        assert '500' not in title, f'Student Hero Section — Homepage Search: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0021(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Hero Section — Homepage Search mobile viewport 500: {title}'
        assert '502' not in title, f'Student Hero Section — Homepage Search mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0022(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Hero Section — Homepage Search tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0023(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Hero Section — Homepage Search uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0024(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Hero Section — Homepage Search: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Hero Section — Homepage Search: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0025(self, page: Page):
        """Student Hero Section — Homepage Search body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Hero Section — Homepage Search: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0026(self, page: Page):
        """Student Hero Section — Homepage Search must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Hero Section — Homepage Search: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0027(self, page: Page):
        """Student Hero Section — Homepage Search must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Hero Section — Homepage Search: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0028(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Hero Section — Homepage Search 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0029(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Hero Section — Homepage Search AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Hero Section — Homepage Search AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0030(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Hero Section — Homepage Search: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Language Switcher — https://dev.mehadedu.com/en
# ==============================================================
class TestSpec_StudentLanguageSwitch:
    """Auto-generated tests from specs/student_language_switch.md
    Page: Language Switcher  URL: https://dev.mehadedu.com/en
    Selectors: 5  Flows: 3  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/'
    PAGE_URL  = 'https://dev.mehadedu.com/en'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Language Switcher loads without server error."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__switch_to_arabic_0002(self, page: Page):
        """Flow: Flow 1: Switch to Arabic"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__switch_back_to_english_0003(self, page: Page):
        """Flow: Flow 2: Switch Back to English"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__language_persists_across_navigat_0004(self, page: Page):
        """Flow: Flow 3: Language Persists Across Navigation"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: EN and AR language toggle buttons visible in header"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Toggle check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Clicking AR navigates to /ar locale"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Arabic locale sets dir=rtl on HTML element"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Clicking EN navigates to /en locale"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Active language button is visually indicated"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: All page content translates to selected language"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Click active language button"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Direct navigation to /ar"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Arabic with login modal"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: Language switch while logged in"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0019(self, page: Page):
        """Language Switcher loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Language Switcher took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0020(self, page: Page):
        """Language Switcher page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Language Switcher: page title is empty'
        assert '404' not in title, f'Language Switcher: title says 404: {title}'
        assert '500' not in title, f'Language Switcher: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0021(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Language Switcher mobile viewport 500: {title}'
        assert '502' not in title, f'Language Switcher mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0022(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Language Switcher tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0023(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Language Switcher uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0024(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Language Switcher: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Language Switcher: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0025(self, page: Page):
        """Language Switcher body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Language Switcher: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0026(self, page: Page):
        """Language Switcher must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Language Switcher: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0027(self, page: Page):
        """Language Switcher must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Language Switcher: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0028(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Language Switcher 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0029(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Language Switcher AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Language Switcher AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0030(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Language Switcher: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Login — WhatsApp OTP Authentication — https://dev.mehadedu.com/en
# ==============================================================
class TestSpec_StudentLogin:
    """Auto-generated tests from specs/student_login.md
    Page: Student Login — WhatsApp OTP Authentication  URL: https://dev.mehadedu.com/en
    Selectors: 12  Flows: 3  ECs: 8  Reqs: 10
    """
    PAGE_PATH = '/'
    PAGE_URL  = 'https://dev.mehadedu.com/en'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Login — WhatsApp OTP Authentication loads without server error."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__complete_student_login_0002(self, page: Page):
        """Flow: Flow 1: Complete Student Login"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__wrong_otp_rejected_0003(self, page: Page):
        """Flow: Flow 2: Wrong OTP Rejected"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__change_phone_number_0004(self, page: Page):
        """Flow: Flow 3: Change Phone Number"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Log In button must be visible in unauthenticated header"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Dialog must open with role=dialog and heading Welcome back"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Country code defaults to +966, can be changed to +880"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Phone input is type=tel and accepts only numeric input"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Send Code is disabled when phone field is empty"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: OTP input is disabled until Send Code is successfully clicked"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: OTP field accepts exactly 6 digits with maxlength=6"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Continue triggers authentication"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: Successful login closes modal and shows user name in header"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: Failed OTP shows error without full page reload"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: Empty phone submitted"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: Non-numeric phone characters"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: Wrong OTP 000000"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0022(self, page: Page):
        """EC-04: Phone too short (3 digits)"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0024(self, page: Page):
        """EC-05: Modal closed before completing"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0026(self, page: Page):
        """EC-06: Rapid double-click Continue"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Rapid click EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0028(self, page: Page):
        """EC-07: Network error during Send Code"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_08_0030(self, page: Page):
        """EC-08: OTP entered before Send Code click"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0031(self, page: Page):
        """Student Login — WhatsApp OTP Authentication loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Login — WhatsApp OTP Authentication took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0032(self, page: Page):
        """Student Login — WhatsApp OTP Authentication page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Login — WhatsApp OTP Authentication: page title is empty'
        assert '404' not in title, f'Student Login — WhatsApp OTP Authentication: title says 404: {title}'
        assert '500' not in title, f'Student Login — WhatsApp OTP Authentication: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0033(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Login — WhatsApp OTP Authentication mobile viewport 500: {title}'
        assert '502' not in title, f'Student Login — WhatsApp OTP Authentication mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0034(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Login — WhatsApp OTP Authentication tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0035(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Login — WhatsApp OTP Authentication uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0036(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Login — WhatsApp OTP Authentication: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Login — WhatsApp OTP Authentication: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0037(self, page: Page):
        """Student Login — WhatsApp OTP Authentication body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Login — WhatsApp OTP Authentication: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0038(self, page: Page):
        """Student Login — WhatsApp OTP Authentication must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Login — WhatsApp OTP Authentication: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0039(self, page: Page):
        """Student Login — WhatsApp OTP Authentication must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Login — WhatsApp OTP Authentication: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0040(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Login — WhatsApp OTP Authentication 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0041(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Login — WhatsApp OTP Authentication AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Login — WhatsApp OTP Authentication AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0042(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Login — WhatsApp OTP Authentication: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Messages — https://dev.mehadedu.com/en/dashboard/messages
# ==============================================================
class TestSpec_StudentMessage:
    """Auto-generated tests from specs/student_message.md
    Page: Student Messages  URL: https://dev.mehadedu.com/en/dashboard/messages
    Selectors: 12  Flows: 3  ECs: 7  Reqs: 10
    """
    PAGE_PATH = '/dashboard/messages'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/messages'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Messages loads without server error."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__send_a_message_0002(self, page: Page):
        """Flow: Flow 1: Send a Message"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__search_conversations_0003(self, page: Page):
        """Flow: Flow 2: Search Conversations"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__archive_conversation_0004(self, page: Page):
        """Flow: Flow 3: Archive Conversation"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Messages page loads with three-panel layout"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Conversation list shows all active conversations"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Clicking conversation loads chat history"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Message input accepts text input"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Send button enabled only when message text exists"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Sent message appears immediately in chat"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Search filters conversation list in real time"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Archive moves conversation to archived tab"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: Empty message cannot be sent"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: Messages page requires authentication"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: Send empty message"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: XSS in message text"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'XSS EC check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: Very long message"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Long input EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0022(self, page: Page):
        """EC-04: Network error during send"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0024(self, page: Page):
        """EC-05: Archive conversation"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0026(self, page: Page):
        """EC-06: Search with no match"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0028(self, page: Page):
        """EC-07: Unauthenticated access"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0029(self, page: Page):
        """Student Messages loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Messages took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0030(self, page: Page):
        """Student Messages page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Messages: page title is empty'
        assert '404' not in title, f'Student Messages: title says 404: {title}'
        assert '500' not in title, f'Student Messages: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0031(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Messages mobile viewport 500: {title}'
        assert '502' not in title, f'Student Messages mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0032(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Messages tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0033(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Messages uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0034(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Messages: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Messages: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0035(self, page: Page):
        """Student Messages body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Messages: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0036(self, page: Page):
        """Student Messages must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Messages: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0037(self, page: Page):
        """Student Messages must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Messages: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0038(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Messages 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0039(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/messages'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Messages AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Messages AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0040(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Messages: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student My Bookings — https://dev.mehadedu.com/en/dashboard/bookings
# ==============================================================
class TestSpec_StudentMybooking:
    """Auto-generated tests from specs/student_mybooking.md
    Page: Student My Bookings  URL: https://dev.mehadedu.com/en/dashboard/bookings
    Selectors: 11  Flows: 4  ECs: 7  Reqs: 10
    """
    PAGE_PATH = '/dashboard/bookings'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/bookings'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student My Bookings loads without server error."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_my_bookings_0002(self, page: Page):
        """Flow: Flow 1: View My Bookings"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__join_a_session_at_scheduled_time_0003(self, page: Page):
        """Flow: Flow 2: Join a Session at Scheduled Time"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = ''
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__complete_session_and_write_revie_0004(self, page: Page):
        """Flow: Flow 3: Complete Session and Write Review"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_4__view_recording_in_session_histor_0005(self, page: Page):
        """Flow: Flow 4: View Recording in Session History"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0006(self, page: Page):
        """REQ: REQ-01: My Bookings page accessible at /en/dashboard/bookings"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0007(self, page: Page):
        """REQ: REQ-02: Upcoming sessions show session title, date-time, status"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert page.title() != '', f'Empty title on {page.url}'
        assert '500' not in page.title(), f'Server error: {page.title()!r}'

    @pytest.mark.requirement
    def test_req_02_0008(self, page: Page):
        """REQ: REQ-03: Join Classroom button only appears at scheduled session time"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0009(self, page: Page):
        """REQ: REQ-04: Complete Session button appears after session ends"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0010(self, page: Page):
        """REQ: REQ-05: Clicking Complete Session updates status and moves to history"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0011(self, page: Page):
        """REQ: REQ-06: Session History shows completed sessions with recording option"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0012(self, page: Page):
        """REQ: REQ-07: View Recording opens a valid recording file"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0013(self, page: Page):
        """REQ: REQ-08: Tutor cancellation moves session to Unbooked section"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0014(self, page: Page):
        """REQ: REQ-09: Student cannot join session before scheduled time"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0015(self, page: Page):
        """REQ: REQ-10: Cancelled sessions do not appear in Upcoming"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0017(self, page: Page):
        """EC-01: Join before session time"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0019(self, page: Page):
        """EC-02: Complete Session before session ends"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0021(self, page: Page):
        """EC-03: No bookings exist"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0023(self, page: Page):
        """EC-04: Tutor cancels 1-to-1 session"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0025(self, page: Page):
        """EC-05: Student books same group session twice"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0027(self, page: Page):
        """EC-06: Recording not yet available"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0029(self, page: Page):
        """EC-07: Unauthenticated user accesses bookings"""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0030(self, page: Page):
        """Student My Bookings loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student My Bookings took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0031(self, page: Page):
        """Student My Bookings page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student My Bookings: page title is empty'
        assert '404' not in title, f'Student My Bookings: title says 404: {title}'
        assert '500' not in title, f'Student My Bookings: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0032(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student My Bookings mobile viewport 500: {title}'
        assert '502' not in title, f'Student My Bookings mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0033(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student My Bookings tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0034(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student My Bookings uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0035(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student My Bookings: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student My Bookings: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0036(self, page: Page):
        """Student My Bookings body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student My Bookings: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0037(self, page: Page):
        """Student My Bookings must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student My Bookings: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0038(self, page: Page):
        """Student My Bookings must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student My Bookings: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0039(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student My Bookings 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0040(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/bookings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student My Bookings AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student My Bookings AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0041(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/bookings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student My Bookings: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Payment — https://dev.mehadedu.com/en/payment
# ==============================================================
class TestSpec_StudentPayment:
    """Auto-generated tests from specs/student_payment.md
    Page: Student Payment  URL: https://dev.mehadedu.com/en/payment
    Selectors: 16  Flows: 3  ECs: 8  Reqs: 10
    """
    PAGE_PATH = '/payment'
    PAGE_URL  = 'https://dev.mehadedu.com/en/payment'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Payment loads without server error."""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_myfatoorah_iframe_0002(self, page: Page):
        """Element must be present: MyFatoorah iframe"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.iframe#MFEmbeddedIframe.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'MyFatoorah iframe' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__complete_1_to_1_session_payment__0003(self, page: Page):
        """Flow: Flow 1: Complete 1-to-1 Session Payment (sandbox)"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = ''
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        try:
            page.iframe#MFEmbeddedIframe.click(timeout=5000)
            page.wait_for_timeout(500)
        except Exception: pass
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = '/dashboard'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__view_wallet_after_payment_0004(self, page: Page):
        """Flow: Flow 2: View Wallet after Payment"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__view_empty_wallet_0005(self, page: Page):
        """Flow: Flow 3: View Empty Wallet"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0006(self, page: Page):
        """REQ: REQ-01: Payment page shows booking number, total amount in SAR"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0007(self, page: Page):
        """REQ: REQ-02: Card payment method is pre-selected by default"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0008(self, page: Page):
        """REQ: REQ-03: MyFatoorah iframe renders card holder name, card number, expiry, CVV fie"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0009(self, page: Page):
        """REQ: REQ-04: Card number auto-formats with spaces (4111 1111 1111 1111)"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0010(self, page: Page):
        """REQ: REQ-05: Pay now button outside the iframe triggers payment processing"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0011(self, page: Page):
        """REQ: REQ-06: 3DS ACS emulator iframe appears in sandbox environment"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe#MFEmbeddedIframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_06_0012(self, page: Page):
        """REQ: REQ-07: After 3DS approval, payment completes and redirects to My Bookings"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0013(self, page: Page):
        """REQ: REQ-08: Payment history visible in wallet page after successful payment"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0014(self, page: Page):
        """REQ: REQ-09: Invalid card number shows error inside the iframe"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0015(self, page: Page):
        """REQ: REQ-10: Expired card shows error inside the iframe"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0017(self, page: Page):
        """EC-01: Invalid card number (1234)"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0019(self, page: Page):
        """EC-02: Expired card (01/20)"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0021(self, page: Page):
        """EC-03: Wrong CVV"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0023(self, page: Page):
        """EC-04: Empty card holder name"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0025(self, page: Page):
        """EC-05: 3DS result = N (Not Authenticated)"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0027(self, page: Page):
        """EC-06: Close page during processing"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0029(self, page: Page):
        """EC-07: Navigate back after Pay now"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.go_back()
        page.wait_for_timeout(500)
        assert '500' not in page.title(), f'Server error after back-button EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_08_0031(self, page: Page):
        """EC-08: Pay with valid sandbox card 4111111111111111"""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0032(self, page: Page):
        """Student Payment loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Payment took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0033(self, page: Page):
        """Student Payment page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Payment: page title is empty'
        assert '404' not in title, f'Student Payment: title says 404: {title}'
        assert '500' not in title, f'Student Payment: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0034(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Payment mobile viewport 500: {title}'
        assert '502' not in title, f'Student Payment mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0035(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Payment tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0036(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Payment uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0037(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Payment: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Payment: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0038(self, page: Page):
        """Student Payment body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Payment: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0039(self, page: Page):
        """Student Payment must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Payment: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0040(self, page: Page):
        """Student Payment must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Payment: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0041(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Payment 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0042(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/payment'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Payment AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Payment AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0043(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/payment', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Payment: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Profile Page Spec — https://dev.mehadedu.com/en/dashboard/profile
# ==============================================================
class TestSpec_StudentProfile:
    """Auto-generated tests from specs/student_profile.md
    Page: Student Profile Page Spec  URL: https://dev.mehadedu.com/en/dashboard/profile
    Selectors: 17  Flows: 6  ECs: 0  Reqs: 6
    """
    PAGE_PATH = '/dashboard/profile'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/profile'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Profile Page Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_profile_page_loads_with_student_name_0002(self, page: Page):
        """Flow: Profile page loads with student name"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_profile_info_section_shows_all_fields_0003(self, page: Page):
        """Flow: Profile info section shows all fields"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_change_avatar_button_is_visible_0004(self, page: Page):
        """Flow: Change Avatar button is visible"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_edit_profile_button_opens_edit_form_or_m_0005(self, page: Page):
        """Flow: Edit Profile button opens edit form or modal"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_profile_accessible_from_settings___edit__0006(self, page: Page):
        """Flow: Profile accessible from Settings > Edit Profile"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__unauthenticated_access_redirec_0007(self, page: Page):
        """Flow: Negative — Unauthenticated access redirects"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/home'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0008(self, page: Page):
        """REQ: REQ-01: My Profile heading is shown and Automations Student name is visible"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0009(self, page: Page):
        """REQ: REQ-02: Email, Location, Phone, and Member Since fields are all displayed with v"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0010(self, page: Page):
        """REQ: REQ-03: a Change Avatar button overlaid on the profile image is visible"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0011(self, page: Page):
        """REQ: REQ-04: an edit form or modal appears allowing profile fields to be changed"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0012(self, page: Page):
        """REQ: REQ-05: the browser navigates to `/en/dashboard/profile`"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0013(self, page: Page):
        """REQ: REQ-06: the user is redirected to the homepage or shown a login prompt"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0014(self, page: Page):
        """Student Profile Page Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Profile Page Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0015(self, page: Page):
        """Student Profile Page Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Profile Page Spec: page title is empty'
        assert '404' not in title, f'Student Profile Page Spec: title says 404: {title}'
        assert '500' not in title, f'Student Profile Page Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0016(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Profile Page Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Student Profile Page Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0017(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Profile Page Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0018(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Profile Page Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0019(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Profile Page Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Profile Page Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0020(self, page: Page):
        """Student Profile Page Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Profile Page Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0021(self, page: Page):
        """Student Profile Page Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Profile Page Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0022(self, page: Page):
        """Student Profile Page Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Profile Page Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0023(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Profile Page Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0024(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/profile'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Profile Page Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Profile Page Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0025(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Profile Page Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Reviews and Ratings — https://dev.mehadedu.com/en/dashboard/reviews
# ==============================================================
class TestSpec_StudentReviewAndRatings:
    """Auto-generated tests from specs/student_review_and_ratings.md
    Page: Student Reviews and Ratings  URL: https://dev.mehadedu.com/en/dashboard/reviews
    Selectors: 9  Flows: 2  ECs: 5  Reqs: 7
    """
    PAGE_PATH = '/dashboard/reviews'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/reviews'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Reviews and Ratings loads without server error."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__submit_review_after_session_0002(self, page: Page):
        """Flow: Flow 1: Submit Review After Session"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__view_reviews_page_0003(self, page: Page):
        """Flow: Flow 2: View Reviews Page"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Reviews page shows all submitted tutor reviews"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Review submission requires star rating"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Written feedback is optional"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Review is linked to specific tutor and session"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Only completed sessions can be reviewed"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Review appears on tutor profile after submission"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0010(self, page: Page):
        """REQ: REQ-07: Cannot review the same session twice"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Review without star rating"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Review same session twice"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Review incomplete session"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: Very long review text"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Long input EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0020(self, page: Page):
        """EC-05: Review with XSS in text"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'XSS EC check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0021(self, page: Page):
        """Student Reviews and Ratings loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Reviews and Ratings took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0022(self, page: Page):
        """Student Reviews and Ratings page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Reviews and Ratings: page title is empty'
        assert '404' not in title, f'Student Reviews and Ratings: title says 404: {title}'
        assert '500' not in title, f'Student Reviews and Ratings: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0023(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Reviews and Ratings mobile viewport 500: {title}'
        assert '502' not in title, f'Student Reviews and Ratings mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0024(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Reviews and Ratings tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0025(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Reviews and Ratings uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0026(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Reviews and Ratings: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Reviews and Ratings: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0027(self, page: Page):
        """Student Reviews and Ratings body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Reviews and Ratings: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0028(self, page: Page):
        """Student Reviews and Ratings must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Reviews and Ratings: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0029(self, page: Page):
        """Student Reviews and Ratings must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Reviews and Ratings: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0030(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Reviews and Ratings 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0031(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/reviews'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Reviews and Ratings AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Reviews and Ratings AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0032(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Reviews and Ratings: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Student — Reviews & Ratings — /en/dashboard/reviews
# ==============================================================
class TestSpec_StudentReviewsRatings:
    """Auto-generated tests from specs/student_reviews_ratings.md
    Page: Spec: Student — Reviews & Ratings  URL: /en/dashboard/reviews
    Selectors: 7  Flows: 3  ECs: 3  Reqs: 10
    """
    PAGE_PATH = '/dashboard/reviews'
    PAGE_URL  = '/en/dashboard/reviews'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Student — Reviews & Ratings loads without server error."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_student_with_no_completed_sessions_sees__0002(self, page: Page):
        """Flow: Student with no completed sessions sees empty state"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_filter_controls_are_present_even_with_no_0003(self, page: Page):
        """Flow: Filter controls are present even with no reviews"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_student_reviews_appear_after_completed_s_0004(self, page: Page):
        """Flow: Student reviews appear after completed session (expected beh"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: the heading Reviews & Ratings is displayed"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: three filter comboboxes are shown: All ratings, Newest, 10"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: the main content area shows No reviews found."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: there is no Write a Review button or form visible"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: the combobox All ratings is visible and selectable"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: the combobox Newest is visible and selectable"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: the combobox 10 is visible and selectable"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: a review form or trigger is available for that session"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: clicks submit"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: the review appears in the list with:"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: No reviews found. when student has no completed sessions or """
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: Review form/button not visible without completed sessions"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: Tutor public profile shows No reviews yet. when tutor has re"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0021(self, page: Page):
        """Spec: Student — Reviews & Ratings loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Student — Reviews & Ratings took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0022(self, page: Page):
        """Spec: Student — Reviews & Ratings page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Student — Reviews & Ratings: page title is empty'
        assert '404' not in title, f'Spec: Student — Reviews & Ratings: title says 404: {title}'
        assert '500' not in title, f'Spec: Student — Reviews & Ratings: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0023(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Reviews & Ratings mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Student — Reviews & Ratings mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0024(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Reviews & Ratings tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0025(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Student — Reviews & Ratings uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0026(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Student — Reviews & Ratings: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Student — Reviews & Ratings: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0027(self, page: Page):
        """Spec: Student — Reviews & Ratings body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Student — Reviews & Ratings: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0028(self, page: Page):
        """Spec: Student — Reviews & Ratings must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Student — Reviews & Ratings: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0029(self, page: Page):
        """Spec: Student — Reviews & Ratings must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Student — Reviews & Ratings: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0030(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Student — Reviews & Ratings 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0031(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/reviews'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Reviews & Ratings AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Student — Reviews & Ratings AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0032(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Student — Reviews & Ratings: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Profile Settings — https://dev.mehadedu.com/en/dashboard/settings
# ==============================================================
class TestSpec_StudentSettings:
    """Auto-generated tests from specs/student_settings.md
    Page: Student Profile Settings  URL: https://dev.mehadedu.com/en/dashboard/settings
    Selectors: 9  Flows: 3  ECs: 6  Reqs: 6
    """
    PAGE_PATH = '/dashboard/settings'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/settings'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Profile Settings loads without server error."""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__update_display_name_0002(self, page: Page):
        """Flow: Flow 1: Update Display Name"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__upload_profile_picture_0003(self, page: Page):
        """Flow: Flow 2: Upload Profile Picture"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__view_profile_information_0004(self, page: Page):
        """Flow: Flow 3: View Profile Information"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Settings page accessible to authenticated students"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Display name field is editable"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Save Changes persists updates"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Phone number shown (read-only)"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Email field validates format"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Profile picture upload accepts images only"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Name field empty on save"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Invalid email format"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Non-image file for avatar"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: XSS in name field"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'XSS EC check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0020(self, page: Page):
        """EC-05: Name over max length"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Long input EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0022(self, page: Page):
        """EC-06: Unauthenticated access to settings"""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0023(self, page: Page):
        """Student Profile Settings loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Profile Settings took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0024(self, page: Page):
        """Student Profile Settings page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Profile Settings: page title is empty'
        assert '404' not in title, f'Student Profile Settings: title says 404: {title}'
        assert '500' not in title, f'Student Profile Settings: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0025(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Profile Settings mobile viewport 500: {title}'
        assert '502' not in title, f'Student Profile Settings mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0026(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Profile Settings tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0027(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Profile Settings uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0028(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Profile Settings: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Profile Settings: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0029(self, page: Page):
        """Student Profile Settings body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Profile Settings: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0030(self, page: Page):
        """Student Profile Settings must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Profile Settings: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0031(self, page: Page):
        """Student Profile Settings must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Profile Settings: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0032(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Profile Settings 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0033(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/settings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Profile Settings AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Profile Settings AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0034(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/settings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Profile Settings: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Payment & Wallet Spec — https://dev.mehadedu.com/en/dashboard/wallet
# ==============================================================
class TestSpec_StudentWallet:
    """Auto-generated tests from specs/student_wallet.md
    Page: Student Payment & Wallet Spec  URL: https://dev.mehadedu.com/en/dashboard/wallet
    Selectors: 6  Flows: 5  ECs: 0  Reqs: 5
    """
    PAGE_PATH = '/dashboard/wallet'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/wallet'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Payment & Wallet Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_wallet_page_loads_with_payments_heading_0002(self, page: Page):
        """Flow: Wallet page loads with Payments heading"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_recent_transactions_section_shows_empty__0003(self, page: Page):
        """Flow: Recent Transactions section shows empty state when no transa"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_transactions_list_shows_entries_when_pur_0004(self, page: Page):
        """Flow: Transactions list shows entries when purchases exist"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_sidebar_navigation_is_present_0005(self, page: Page):
        """Flow: Sidebar navigation is present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__unauthenticated_access_redirec_0006(self, page: Page):
        """Flow: Negative — Unauthenticated access redirects"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/home'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0007(self, page: Page):
        """REQ: REQ-01: the Payments heading is visible"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0008(self, page: Page):
        """REQ: REQ-02: Recent Transactions heading and No transactions found message are di"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0009(self, page: Page):
        """REQ: REQ-03: transaction entries are shown with amount, date, and status"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0010(self, page: Page):
        """REQ: REQ-04: the Payment & Wallet link in the sidebar is active/highlighted"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0011(self, page: Page):
        """REQ: REQ-05: the user is redirected to the homepage or login"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0012(self, page: Page):
        """Student Payment & Wallet Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Payment & Wallet Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0013(self, page: Page):
        """Student Payment & Wallet Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Payment & Wallet Spec: page title is empty'
        assert '404' not in title, f'Student Payment & Wallet Spec: title says 404: {title}'
        assert '500' not in title, f'Student Payment & Wallet Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0014(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Payment & Wallet Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Student Payment & Wallet Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0015(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Payment & Wallet Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0016(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Payment & Wallet Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0017(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Payment & Wallet Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Payment & Wallet Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0018(self, page: Page):
        """Student Payment & Wallet Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Payment & Wallet Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0019(self, page: Page):
        """Student Payment & Wallet Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Payment & Wallet Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0020(self, page: Page):
        """Student Payment & Wallet Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Payment & Wallet Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0021(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Payment & Wallet Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0022(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/wallet'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Payment & Wallet Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Payment & Wallet Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0023(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Payment & Wallet Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Student — Wallet & Payments (Full) — /en/dashboard/wallet
# ==============================================================
class TestSpec_StudentWalletPaymentsFull:
    """Auto-generated tests from specs/student_wallet_payments_full.md
    Page: Spec: Student — Wallet & Payments (Full)  URL: /en/dashboard/wallet
    Selectors: 22  Flows: 1  ECs: 5  Reqs: 20
    """
    PAGE_PATH = '/dashboard/wallet'
    PAGE_URL  = '/en/dashboard/wallet'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Student — Wallet & Payments (Full) loads without server error."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_paytabs_iframe_0002(self, page: Page):
        """Element must be present: PayTabs iframe"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.iframe.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'PayTabs iframe' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_booking_dashboard__student_____url_______0003(self, page: Page):
        """Flow: Booking Dashboard (Student)

**URL:** `/en/dashboard/booking"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: the heading Payments is displayed"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: the Recent Transactions section is shown"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: the text No transactions found appears"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: the heading Payment is shown"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: the booking number DBK-{date}-{id} is displayed"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: the total amount shows 50 SAR"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0010(self, page: Page):
        """REQ: REQ-07: the Card payment method button is pre-selected [pressed]"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0011(self, page: Page):
        """REQ: REQ-08: a PayTabs iframe renders for card entry"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0012(self, page: Page):
        """REQ: REQ-09: a Pay now button is visible"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_09_0013(self, page: Page):
        """REQ: REQ-10: the footer note Your card details are encrypted and securely processed."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_10_0014(self, page: Page):
        """REQ: REQ-11: the booking number matches a group booking format"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0015(self, page: Page):
        """REQ: REQ-12: the page structure is identical to the 1-on-1 payment page"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0016(self, page: Page):
        """REQ: REQ-13: the URL includes the query param bookingType=group"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_13_0017(self, page: Page):
        """REQ: REQ-14: the booking status changes to confirmed"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_14_0018(self, page: Page):
        """REQ: REQ-15: the booking appears in /en/dashboard/bookings under Upcoming Sessions"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_15_0019(self, page: Page):
        """REQ: REQ-16: a transaction entry appears in /en/dashboard/wallet Recent Transactions"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_16_0020(self, page: Page):
        """REQ: REQ-17: the heading Hello Automations Student is shown"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_17_0021(self, page: Page):
        """REQ: REQ-18: Manage your upcoming and past sessions paragraph is shown"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_18_0022(self, page: Page):
        """REQ: REQ-19: Unbooked 0.0 hours button is shown"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_19_0023(self, page: Page):
        """REQ: REQ-20: the tabs Upcoming Sessions and Session History are shown"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.iframe.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.edge_case
    def test_ec_ec_01_0025(self, page: Page):
        """EC-01: No transactions found  when no payments have been completed"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0027(self, page: Page):
        """EC-02: No upcoming sessions found  when no bookings are confirmed"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0029(self, page: Page):
        """EC-03: Payment iframe may fail to load if PayTabs is not configured"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0031(self, page: Page):
        """EC-04: Booking number format: `DBK-{YYYYMMDD}-{6-char-id}`, e.g., `"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0033(self, page: Page):
        """EC-05: `bookingType=group` is appended to the URL for group session"""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.performance
    def test_load_time_0034(self, page: Page):
        """Spec: Student — Wallet & Payments (Full) loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Student — Wallet & Payments (Full) took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0035(self, page: Page):
        """Spec: Student — Wallet & Payments (Full) page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Student — Wallet & Payments (Full): page title is empty'
        assert '404' not in title, f'Spec: Student — Wallet & Payments (Full): title says 404: {title}'
        assert '500' not in title, f'Spec: Student — Wallet & Payments (Full): title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0036(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Wallet & Payments (Full) mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Student — Wallet & Payments (Full) mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0037(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Wallet & Payments (Full) tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0038(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Student — Wallet & Payments (Full) uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0039(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Student — Wallet & Payments (Full): final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Student — Wallet & Payments (Full): stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0040(self, page: Page):
        """Spec: Student — Wallet & Payments (Full) body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Student — Wallet & Payments (Full): body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0041(self, page: Page):
        """Spec: Student — Wallet & Payments (Full) must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Student — Wallet & Payments (Full): no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0042(self, page: Page):
        """Spec: Student — Wallet & Payments (Full) must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Student — Wallet & Payments (Full): debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0043(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Student — Wallet & Payments (Full) 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0044(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/wallet'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Student — Wallet & Payments (Full) AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Student — Wallet & Payments (Full) AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0045(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/wallet', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Student — Wallet & Payments (Full): served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Student Management — Super Admin — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_Students:
    """Auto-generated tests from specs/students.md
    Page: Student Management — Super Admin  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 13  Flows: 4  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Student Management — Super Admin loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_all_students_0002(self, page: Page):
        """Flow: Flow 1: View All Students"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__search_students_0003(self, page: Page):
        """Flow: Flow 2: Search Students"""
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__suspend_student_0004(self, page: Page):
        """Flow: Flow 3: Suspend Student"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_4__delete_student_0005(self, page: Page):
        """Flow: Flow 4: Delete Student"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0006(self, page: Page):
        """REQ: REQ-01: Student list shows name, phone, location, member date, status"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0007(self, page: Page):
        """REQ: REQ-02: Search by name or email filters correctly"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0008(self, page: Page):
        """REQ: REQ-03: Status filter: Pending, Active, Suspended"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0009(self, page: Page):
        """REQ: REQ-04: Verify Profile adds verified badge"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0010(self, page: Page):
        """REQ: REQ-05: Suspend prevents student from booking sessions"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0011(self, page: Page):
        """REQ: REQ-06: Delete permanently removes account"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0013(self, page: Page):
        """EC-01: Search with no match"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0015(self, page: Page):
        """EC-02: Suspended student tries to book"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0017(self, page: Page):
        """EC-03: Delete student with active bookings"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0019(self, page: Page):
        """EC-04: Filter with no results"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0020(self, page: Page):
        """Student Management — Super Admin loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Student Management — Super Admin took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0021(self, page: Page):
        """Student Management — Super Admin page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Student Management — Super Admin: page title is empty'
        assert '404' not in title, f'Student Management — Super Admin: title says 404: {title}'
        assert '500' not in title, f'Student Management — Super Admin: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0022(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Management — Super Admin mobile viewport 500: {title}'
        assert '502' not in title, f'Student Management — Super Admin mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0023(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Management — Super Admin tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0024(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Student Management — Super Admin uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0025(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Student Management — Super Admin: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Student Management — Super Admin: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0026(self, page: Page):
        """Student Management — Super Admin body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Student Management — Super Admin: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0027(self, page: Page):
        """Student Management — Super Admin must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Student Management — Super Admin: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0028(self, page: Page):
        """Student Management — Super Admin must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Student Management — Super Admin: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0029(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Student Management — Super Admin 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0030(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Student Management — Super Admin AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Student Management — Super Admin AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0031(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Student Management — Super Admin: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Subject Categories Management — https://dev.mehadedu.com/en/dashboard/subject-categories
# ==============================================================
class TestSpec_SubjectCategories:
    """Auto-generated tests from specs/subject_categories.md
    Page: Subject Categories Management  URL: https://dev.mehadedu.com/en/dashboard/subject-categories
    Selectors: 13  Flows: 2  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/dashboard/subject-categories'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/subject-categories'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Subject Categories Management loads without server error."""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__create_subject_category_0002(self, page: Page):
        """Flow: Flow 1: Create Subject Category"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__disable_category_0003(self, page: Page):
        """Flow: Flow 2: Disable Category"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Category name max 40 characters"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Icon upload max 2 MB"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Description max 500 characters"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Active categories appear in Subject creation"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Disabled categories hidden from Subject selection"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Sorting reflects in public order"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0011(self, page: Page):
        """EC-01: Name over 40 chars"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0013(self, page: Page):
        """EC-02: Icon file over 2 MB"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0015(self, page: Page):
        """EC-03: Description over 500 chars"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0017(self, page: Page):
        """EC-04: Disable used category"""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0018(self, page: Page):
        """Subject Categories Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Subject Categories Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0019(self, page: Page):
        """Subject Categories Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Subject Categories Management: page title is empty'
        assert '404' not in title, f'Subject Categories Management: title says 404: {title}'
        assert '500' not in title, f'Subject Categories Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0020(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Subject Categories Management mobile viewport 500: {title}'
        assert '502' not in title, f'Subject Categories Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0021(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Subject Categories Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0022(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Subject Categories Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0023(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Subject Categories Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Subject Categories Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0024(self, page: Page):
        """Subject Categories Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Subject Categories Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0025(self, page: Page):
        """Subject Categories Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Subject Categories Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0026(self, page: Page):
        """Subject Categories Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Subject Categories Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0027(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Subject Categories Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0028(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/subject-categories'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Subject Categories Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Subject Categories Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0029(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/subject-categories', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Subject Categories Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Subjects Management — https://dev.mehadedu.com/en/dashboard/subjects
# ==============================================================
class TestSpec_Subjects:
    """Auto-generated tests from specs/subjects.md
    Page: Subjects Management  URL: https://dev.mehadedu.com/en/dashboard/subjects
    Selectors: 14  Flows: 2  ECs: 5  Reqs: 7
    """
    PAGE_PATH = '/dashboard/subjects'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/subjects'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Subjects Management loads without server error."""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__create_subject_0002(self, page: Page):
        """Flow: Flow 1: Create Subject"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__disable_subject_0003(self, page: Page):
        """Flow: Flow 3: Disable Subject"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Subject name max 32 characters"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Logo upload max 10 MB"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Description max 500 characters"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Category selection required"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Active subjects available on tutor profile, search, booking"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Featured subjects appear on landing page"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0010(self, page: Page):
        """REQ: REQ-07: Arabic name used when language switched to Arabic"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: Subject name over 32 chars"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Logo over 10 MB"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Description over 500 chars"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: Disable subject in active use"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0020(self, page: Page):
        """EC-05: Empty required fields"""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0021(self, page: Page):
        """Subjects Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Subjects Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0022(self, page: Page):
        """Subjects Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Subjects Management: page title is empty'
        assert '404' not in title, f'Subjects Management: title says 404: {title}'
        assert '500' not in title, f'Subjects Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0023(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Subjects Management mobile viewport 500: {title}'
        assert '502' not in title, f'Subjects Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0024(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Subjects Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0025(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Subjects Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0026(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Subjects Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Subjects Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0027(self, page: Page):
        """Subjects Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Subjects Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0028(self, page: Page):
        """Subjects Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Subjects Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0029(self, page: Page):
        """Subjects Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Subjects Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0030(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Subjects Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0031(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/subjects'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Subjects Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Subjects Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0032(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/subjects', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Subjects Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Teacher Booked Sessions Spec — https://dev.mehadedu.com/en/dashboard/booked-sessions
# ==============================================================
class TestSpec_TeacherBookedSessions:
    """Auto-generated tests from specs/teacher_booked_sessions.md
    Page: Teacher Booked Sessions Spec  URL: https://dev.mehadedu.com/en/dashboard/booked-sessions
    Selectors: 11  Flows: 9  ECs: 0  Reqs: 1
    """
    PAGE_PATH = '/dashboard/booked-sessions'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/booked-sessions'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Booked Sessions Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_search_0002(self, page: Page):
        """Element must be present: Search"""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.search.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Search' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_filters_0003(self, page: Page):
        """Element must be present: Filters"""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.filters.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Filters' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_with_all_sessions_heading_0004(self, page: Page):
        """Flow: Page loads with All Sessions heading"""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_upcoming_sessions_and_session_history_ta_0005(self, page: Page):
        """Flow: Upcoming Sessions and Session History tabs are shown"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_search_box_is_present_0006(self, page: Page):
        """Flow: Search box is present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        try:
            page.search.fill('test_value', timeout=5000)
        except Exception: pass
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_filters_button_is_present_0007(self, page: Page):
        """Flow: Filters button is present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_empty_state_message_shown_when_no_sessio_0008(self, page: Page):
        """Flow: Empty state message shown when no sessions"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_session_history_tab_switches_content_0009(self, page: Page):
        """Flow: Session History tab switches content"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_filters_button_opens_filter_panel_0010(self, page: Page):
        """Flow: Filters button opens filter panel"""
        page.wait_for_timeout(200)
        try:
            page.filters.click(timeout=5000)
            page.wait_for_timeout(500)
        except Exception: pass
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_keyword_search_filters_sessions_in_real__0011(self, page: Page):
        """Flow: Keyword search filters sessions in real time"""
        page.wait_for_timeout(200)
        try:
            page.search.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0012(self, page: Page):
        """REQ: REQ-01: only matching sessions are shown"""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.search.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.parametrize('xss_p', ['<script>alert(1)</script>', '"><script>alert(1)</script>', '<img src=x onerror=alert(1)>', '<svg onload=alert(1)>', 'javascript:alert(1)', '<body onload=alert(1)>'])
    @pytest.mark.security
    def test_xss_search_0013(self, page: Page, xss_p: str):
        """XSS payload must not execute in search."""
        alerts: list = []
        page.on('dialog', lambda d: (alerts.append(d.message), d.dismiss()))
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(xss_p, timeout=5000)
            page.wait_for_timeout(500)
            assert len(alerts) == 0, f'XSS executed! payload={xss_p!r} alerts={alerts}'
            assert '500' not in page.title(), f'Server error on XSS: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('sqli_p', ["' OR '1'='1", "' OR '1'='1'--", "' OR 1=1--", "admin'--", "' OR 'x'='x", "'; DROP TABLE users;--"])
    @pytest.mark.security
    def test_sqli_search_0014(self, page: Page, sqli_p: str):
        """SQLi must not expose DB errors in search."""
        db_errs = ['sql syntax', 'mysql_fetch', 'pg_query', 'sqlite_', 'ORA-', 'SQLSTATE', 'syntax error']
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(sqli_p, timeout=5000)
            page.wait_for_timeout(500)
            content = page.content().lower()
            for kw in db_errs:
                assert kw not in content, f'DB error exposed! kw={kw!r} payload={sqli_p!r}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('bad_val', ['abc', '000000'])
    @pytest.mark.negative
    def test_invalid_input_search_0015(self, page: Page, bad_val: str):
        """Invalid input from spec — must not cause server error."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(bad_val, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on invalid {bad_val!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.performance
    def test_load_time_0016(self, page: Page):
        """Teacher Booked Sessions Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Booked Sessions Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0017(self, page: Page):
        """Teacher Booked Sessions Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Booked Sessions Spec: page title is empty'
        assert '404' not in title, f'Teacher Booked Sessions Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Booked Sessions Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0018(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Booked Sessions Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Booked Sessions Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0019(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Booked Sessions Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0020(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Booked Sessions Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0021(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Booked Sessions Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Booked Sessions Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0022(self, page: Page):
        """Teacher Booked Sessions Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Booked Sessions Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0023(self, page: Page):
        """Teacher Booked Sessions Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Booked Sessions Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0024(self, page: Page):
        """Teacher Booked Sessions Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Booked Sessions Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0025(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Booked Sessions Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0026(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/booked-sessions'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Booked Sessions Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Booked Sessions Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0027(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Booked Sessions Spec: served over HTTP, not HTTPS: {page.url}')

    @pytest.mark.parametrize('boundary', ['', ' ', 'A', 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA', '', 'null', 'undefined', 'None', '0'])
    @pytest.mark.boundary
    def test_boundary_search_0028(self, page: Page, boundary: str):
        """Boundary values for Search."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(boundary, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on boundary {boundary!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.parametrize('value', ['url: /en/dashboard/booked-sessions', '98976564', '123456'])
    @pytest.mark.functional
    def test_fill_search_0029(self, page: Page, value: str):
        """Fill Search with valid data — must not crash."""
        page.goto(BASE_URL + '/dashboard/booked-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(value, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error filling Search={value!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not found on {page.url}')



# ==============================================================
# SPEC: Teacher Dashboard Overview Spec — https://dev.mehadedu.com/en/dashboard/availability
# ==============================================================
class TestSpec_TeacherDashboardOverview:
    """Auto-generated tests from specs/teacher_dashboard_overview.md
    Page: Teacher Dashboard Overview Spec  URL: https://dev.mehadedu.com/en/dashboard/availability
    Selectors: 26  Flows: 10  ECs: 0  Reqs: 10
    """
    PAGE_PATH = '/dashboard/availability'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/availability'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Dashboard Overview Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_link_0002(self, page: Page):
        """Element must be present: Link"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.link.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Link' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_after_teacher_login__redirects_to_availa_0003(self, page: Page):
        """Flow: After teacher login, redirects to availability calendar"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_teacher_sidebar_shows_all_navigation_ite_0004(self, page: Page):
        """Flow: Teacher sidebar shows all navigation items"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_notifications_badge_shows_unread_count_0005(self, page: Page):
        """Flow: Notifications badge shows unread count"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_account_switcher_shows_teacher_name_and__0006(self, page: Page):
        """Flow: Account switcher shows teacher name and Tutor role"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_toggle_sidebar_collapses_sidebar_0007(self, page: Page):
        """Flow: Toggle Sidebar collapses sidebar"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_mehad_logo_links_to_homepage_0008(self, page: Page):
        """Flow: Mehad logo links to homepage"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_language_switcher_shows_current_language_0009(self, page: Page):
        """Flow: Language switcher shows current language"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_account_switcher_opens_profile_and_logou_0010(self, page: Page):
        """Flow: Account switcher opens profile and logout options"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0011(self, page: Page):
        """REQ: REQ-01: the browser navigates to `/en/dashboard/availability`"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0012(self, page: Page):
        """REQ: REQ-02: links for Availability Calendar, Booked Sessions, Group Sessions, Messag"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0013(self, page: Page):
        """REQ: REQ-03: a numeric badge with the unread count is shown on the Notifications link"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0014(self, page: Page):
        """REQ: REQ-04: Automations Tutor and Tutor role label are displayed"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_04_0015(self, page: Page):
        """REQ: REQ-05: the sidebar collapses or expands"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0016(self, page: Page):
        """REQ: REQ-06: the browser navigates to `/en`"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0017(self, page: Page):
        """REQ: REQ-07: the current language (e.g. English) is shown"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_07_0018(self, page: Page):
        """REQ: REQ-08: a dropdown with My Profile link and Logout button appears"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.link.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_08_0019(self, page: Page):
        """REQ: REQ-09: the browser navigates to `/en` and the teacher is logged out"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0020(self, page: Page):
        """REQ: REQ-10: the user is redirected to the homepage or login page"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0021(self, page: Page):
        """Teacher Dashboard Overview Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Dashboard Overview Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0022(self, page: Page):
        """Teacher Dashboard Overview Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Dashboard Overview Spec: page title is empty'
        assert '404' not in title, f'Teacher Dashboard Overview Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Dashboard Overview Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0023(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Dashboard Overview Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Dashboard Overview Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0024(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Dashboard Overview Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0025(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Dashboard Overview Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0026(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Dashboard Overview Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Dashboard Overview Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0027(self, page: Page):
        """Teacher Dashboard Overview Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Dashboard Overview Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0028(self, page: Page):
        """Teacher Dashboard Overview Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Dashboard Overview Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0029(self, page: Page):
        """Teacher Dashboard Overview Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Dashboard Overview Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0030(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Dashboard Overview Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0031(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/availability'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Dashboard Overview Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Dashboard Overview Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0032(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Dashboard Overview Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Teacher Earnings & Payouts Spec — https://dev.mehadedu.com/en/dashboard/earnings
# ==============================================================
class TestSpec_TeacherEarnings:
    """Auto-generated tests from specs/teacher_earnings.md
    Page: Teacher Earnings & Payouts Spec  URL: https://dev.mehadedu.com/en/dashboard/earnings
    Selectors: 11  Flows: 7  ECs: 0  Reqs: 7
    """
    PAGE_PATH = '/dashboard/earnings'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/earnings'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Earnings & Payouts Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_with_earnings___payouts_headi_0002(self, page: Page):
        """Flow: Page loads with Earnings & Payouts heading"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_four_metric_cards_are_displayed_0003(self, page: Page):
        """Flow: Four metric cards are displayed"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_earnings_and_payout_tab_buttons_are_pres_0004(self, page: Page):
        """Flow: Earnings and Payout tab buttons are present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_recent_earnings_section_shows_empty_stat_0005(self, page: Page):
        """Flow: Recent Earnings section shows empty state when no earnings"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_payout_tab_shows_payout_request_form_or__0006(self, page: Page):
        """Flow: Payout tab shows payout request form or history"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_metric_values_are_numeric__sar_formatted_0007(self, page: Page):
        """Flow: Metric values are numeric (SAR formatted)"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__unauthenticated_access_redirec_0008(self, page: Page):
        """Flow: Negative — Unauthenticated access redirects"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/home'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0009(self, page: Page):
        """REQ: REQ-01: the Earnings & Payouts heading is visible"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0010(self, page: Page):
        """REQ: REQ-02: four cards show: Total Earnings, Available Balance, Pending Earnings, Co"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0011(self, page: Page):
        """REQ: REQ-03: Earnings and Payout tab buttons are visible"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0012(self, page: Page):
        """REQ: REQ-04: Recent Earnings heading and No earnings yet. message are shown"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0013(self, page: Page):
        """REQ: REQ-05: the view switches to show payout request options or payout history"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0014(self, page: Page):
        """REQ: REQ-06: all values display a numeric format (e.g. 0.00)"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0015(self, page: Page):
        """REQ: REQ-07: the user is redirected to the homepage or login"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0016(self, page: Page):
        """Teacher Earnings & Payouts Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Earnings & Payouts Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0017(self, page: Page):
        """Teacher Earnings & Payouts Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Earnings & Payouts Spec: page title is empty'
        assert '404' not in title, f'Teacher Earnings & Payouts Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Earnings & Payouts Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0018(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Earnings & Payouts Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Earnings & Payouts Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0019(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Earnings & Payouts Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0020(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Earnings & Payouts Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0021(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Earnings & Payouts Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Earnings & Payouts Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0022(self, page: Page):
        """Teacher Earnings & Payouts Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Earnings & Payouts Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0023(self, page: Page):
        """Teacher Earnings & Payouts Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Earnings & Payouts Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0024(self, page: Page):
        """Teacher Earnings & Payouts Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Earnings & Payouts Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0025(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Earnings & Payouts Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0026(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/earnings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Earnings & Payouts Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Earnings & Payouts Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0027(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Earnings & Payouts Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Teacher Group Sessions Page Spec — https://dev.mehadedu.com/en/dashboard/group-sessions
# ==============================================================
class TestSpec_TeacherGroupSessionsPage:
    """Auto-generated tests from specs/teacher_group_sessions_page.md
    Page: Teacher Group Sessions Page Spec  URL: https://dev.mehadedu.com/en/dashboard/group-sessions
    Selectors: 8  Flows: 6  ECs: 0  Reqs: 6
    """
    PAGE_PATH = '/dashboard/group-sessions'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/group-sessions'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Group Sessions Page Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_with_group_sessions_heading_0002(self, page: Page):
        """Flow: Page loads with Group Sessions heading"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_empty_state_shown_when_no_group_sessions_0003(self, page: Page):
        """Flow: Empty state shown when no group sessions"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_group_session_cards_show_when_sessions_e_0004(self, page: Page):
        """Flow: Group session cards show when sessions exist"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_group_sessions_created_from_availability_0005(self, page: Page):
        """Flow: Group sessions created from Availability Calendar"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_navigation_to_group_sessions_from_sideba_0006(self, page: Page):
        """Flow: Navigation to Group Sessions from sidebar works"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__unauthenticated_access_redirec_0007(self, page: Page):
        """Flow: Negative — Unauthenticated access redirects"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/home'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0008(self, page: Page):
        """REQ: REQ-01: the Group Sessions heading is visible"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0009(self, page: Page):
        """REQ: REQ-02: No group sessions found message is displayed"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0010(self, page: Page):
        """REQ: REQ-03: group session cards with course name, date/time, enrolled count, and pri"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0011(self, page: Page):
        """REQ: REQ-04: a 3-step modal opens for creating a group session"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0012(self, page: Page):
        """REQ: REQ-05: the browser navigates to `/en/dashboard/group-sessions`"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0013(self, page: Page):
        """REQ: REQ-06: the user is redirected to the homepage or login"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0014(self, page: Page):
        """Teacher Group Sessions Page Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Group Sessions Page Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0015(self, page: Page):
        """Teacher Group Sessions Page Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Group Sessions Page Spec: page title is empty'
        assert '404' not in title, f'Teacher Group Sessions Page Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Group Sessions Page Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0016(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Group Sessions Page Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Group Sessions Page Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0017(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Group Sessions Page Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0018(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Group Sessions Page Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0019(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Group Sessions Page Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Group Sessions Page Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0020(self, page: Page):
        """Teacher Group Sessions Page Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Group Sessions Page Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0021(self, page: Page):
        """Teacher Group Sessions Page Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Group Sessions Page Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0022(self, page: Page):
        """Teacher Group Sessions Page Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Group Sessions Page Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0023(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Group Sessions Page Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0024(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/group-sessions'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Group Sessions Page Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Group Sessions Page Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0025(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Group Sessions Page Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Teacher Instructor Profile Edit Spec — https://dev.mehadedu.com/en/dashboard/instructor-profile
# ==============================================================
class TestSpec_TeacherInstructorProfileEdit:
    """Auto-generated tests from specs/teacher_instructor_profile_edit.md
    Page: Teacher Instructor Profile Edit Spec  URL: https://dev.mehadedu.com/en/dashboard/instructor-profile
    Selectors: 18  Flows: 11  ECs: 2  Reqs: 11
    """
    PAGE_PATH = '/dashboard/instructor-profile'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/instructor-profile'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Instructor Profile Edit Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_with_instructor_profile_headi_0002(self, page: Page):
        """Flow: Page loads with Instructor Profile heading"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_three_profile_tabs_are_shown_0003(self, page: Page):
        """Flow: Three profile tabs are shown"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_public_availability_toggle_is_present_0004(self, page: Page):
        """Flow: Public Availability toggle is present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_personal_information_tab_shows_profile_d_0005(self, page: Page):
        """Flow: Personal Information tab shows profile data"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_edit_profile_button_opens_edit_form_0006(self, page: Page):
        """Flow: Edit Profile button opens edit form"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_certificates___expertise_tab_is_navigabl_0007(self, page: Page):
        """Flow: Certificates & Expertise tab is navigable"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_teaching_subjects_tab_is_navigable_0008(self, page: Page):
        """Flow: Teaching Subjects tab is navigable"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_toggle_public_availability_turns_tutor_o_0009(self, page: Page):
        """Flow: Toggle Public Availability turns tutor on/off"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0010(self, page: Page):
        """REQ: REQ-01: the Instructor Profile heading is visible"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0011(self, page: Page):
        """REQ: REQ-02: three tabs are visible: Personal Information, Certificates & Expertise, """
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0012(self, page: Page):
        """REQ: REQ-03: a Public Availability label and a toggle switch are visible"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Toggle check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0013(self, page: Page):
        """REQ: REQ-04: the Personal Information section heading and Edit Profile button are"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0014(self, page: Page):
        """REQ: REQ-05: an edit form or modal appears with editable fields for name, bio, locati"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0015(self, page: Page):
        """REQ: REQ-06: the tab panel switches to show certificate/expertise fields"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0016(self, page: Page):
        """REQ: REQ-07: the tab panel switches to show subject management options"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0017(self, page: Page):
        """REQ: REQ-08: the toggle state changes (checked/unchecked) and the profile visibility """
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Toggle check: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0018(self, page: Page):
        """REQ: REQ-09: a success toast or confirmation message appears"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0019(self, page: Page):
        """REQ: REQ-10: a validation error is shown for that field"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0020(self, page: Page):
        """REQ: REQ-11: the user is redirected to the homepage or login"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0022(self, page: Page):
        """EC-01: - first name input: `input[name=firstName]`"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0024(self, page: Page):
        """EC-02: error: `[aria-invalid=true], text=/required|cant be empty/i`"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0025(self, page: Page):
        """Teacher Instructor Profile Edit Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Instructor Profile Edit Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0026(self, page: Page):
        """Teacher Instructor Profile Edit Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Instructor Profile Edit Spec: page title is empty'
        assert '404' not in title, f'Teacher Instructor Profile Edit Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Instructor Profile Edit Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0027(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Instructor Profile Edit Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Instructor Profile Edit Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0028(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Instructor Profile Edit Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0029(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Instructor Profile Edit Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0030(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Instructor Profile Edit Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Instructor Profile Edit Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0031(self, page: Page):
        """Teacher Instructor Profile Edit Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Instructor Profile Edit Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0032(self, page: Page):
        """Teacher Instructor Profile Edit Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Instructor Profile Edit Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0033(self, page: Page):
        """Teacher Instructor Profile Edit Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Instructor Profile Edit Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0034(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Instructor Profile Edit Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0035(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/instructor-profile'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Instructor Profile Edit Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Instructor Profile Edit Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0036(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Instructor Profile Edit Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Teacher Notifications Spec — https://dev.mehadedu.com/en/dashboard/notifications
# ==============================================================
class TestSpec_TeacherNotifications:
    """Auto-generated tests from specs/teacher_notifications.md
    Page: Teacher Notifications Spec  URL: https://dev.mehadedu.com/en/dashboard/notifications
    Selectors: 16  Flows: 10  ECs: 0  Reqs: 1
    """
    PAGE_PATH = '/dashboard/notifications'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/notifications'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Notifications Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_notifications_page_loads_with_heading_0002(self, page: Page):
        """Flow: Notifications page loads with heading"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_all_and_unread_tabs_are_shown_0003(self, page: Page):
        """Flow: All and Unread tabs are shown"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_mark_all_as_read_button_is_present_0004(self, page: Page):
        """Flow: Mark all as read button is present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_notification_items_show_title__date__and_0005(self, page: Page):
        """Flow: Notification items show title, date, and description"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_unread_tab_shows_only_unread_notificatio_0006(self, page: Page):
        """Flow: Unread tab shows only unread notifications"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_mark_all_as_read_clears_unread_badge_in__0007(self, page: Page):
        """Flow: Mark all as read clears unread badge in sidebar"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_clicking_a_notification_marks_it_as_read_0008(self, page: Page):
        """Flow: Clicking a notification marks it as read"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_sidebar_badge_shows_correct_unread_count_0009(self, page: Page):
        """Flow: Sidebar badge shows correct unread count"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0010(self, page: Page):
        """REQ: REQ-01: only notifications that have not been read are shown"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0011(self, page: Page):
        """Teacher Notifications Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Notifications Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0012(self, page: Page):
        """Teacher Notifications Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Notifications Spec: page title is empty'
        assert '404' not in title, f'Teacher Notifications Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Notifications Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0013(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Notifications Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Notifications Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0014(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Notifications Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0015(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Notifications Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0016(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Notifications Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Notifications Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0017(self, page: Page):
        """Teacher Notifications Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Notifications Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0018(self, page: Page):
        """Teacher Notifications Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Notifications Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0019(self, page: Page):
        """Teacher Notifications Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Notifications Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0020(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Notifications Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0021(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/notifications'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Notifications Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Notifications Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0022(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Notifications Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Teacher Reviews Page Spec — https://dev.mehadedu.com/en/dashboard/reviews
# ==============================================================
class TestSpec_TeacherReviewsPage:
    """Auto-generated tests from specs/teacher_reviews_page.md
    Page: Teacher Reviews Page Spec  URL: https://dev.mehadedu.com/en/dashboard/reviews
    Selectors: 18  Flows: 8  ECs: 0  Reqs: 2
    """
    PAGE_PATH = '/dashboard/reviews'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/reviews'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Reviews Page Spec loads without server error."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_search_0002(self, page: Page):
        """Element must be present: Search"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.search.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Search' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_reviews_page_loads_with_reviews_heading_0003(self, page: Page):
        """Flow: Reviews page loads with Reviews heading"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_three_summary_metric_cards_are_shown_0004(self, page: Page):
        """Flow: Three summary metric cards are shown"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_rating_distribution_section_is_shown_wit_0005(self, page: Page):
        """Flow: Rating Distribution section is shown with 5 star rows"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_search_box_filters_reviews_by_keyword_0006(self, page: Page):
        """Flow: Search box filters reviews by keyword"""
        page.wait_for_timeout(200)
        try:
            page.search.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_star_filter_buttons_narrow_displayed_rev_0007(self, page: Page):
        """Flow: Star filter buttons narrow displayed reviews"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_empty_state_shown_when_no_reviews_match_0008(self, page: Page):
        """Flow: Empty state shown when no reviews match"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_review_items_show_reviewer_name__rating__0009(self, page: Page):
        """Flow: Review items show reviewer name, rating stars, date, and com"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_negative__unauthenticated_access_redirec_0010(self, page: Page):
        """Flow: Negative — Unauthenticated access redirects"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/home'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0011(self, page: Page):
        """REQ: REQ-01: only reviews matching the keyword are shown"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.search.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0012(self, page: Page):
        """REQ: REQ-02: only 5-star reviews are shown"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.search.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.parametrize('xss_p', ['<script>alert(1)</script>', '"><script>alert(1)</script>', '<img src=x onerror=alert(1)>', '<svg onload=alert(1)>', 'javascript:alert(1)', '<body onload=alert(1)>'])
    @pytest.mark.security
    def test_xss_search_0013(self, page: Page, xss_p: str):
        """XSS payload must not execute in search."""
        alerts: list = []
        page.on('dialog', lambda d: (alerts.append(d.message), d.dismiss()))
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(xss_p, timeout=5000)
            page.wait_for_timeout(500)
            assert len(alerts) == 0, f'XSS executed! payload={xss_p!r} alerts={alerts}'
            assert '500' not in page.title(), f'Server error on XSS: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('sqli_p', ["' OR '1'='1", "' OR '1'='1'--", "' OR 1=1--", "admin'--", "' OR 'x'='x", "'; DROP TABLE users;--"])
    @pytest.mark.security
    def test_sqli_search_0014(self, page: Page, sqli_p: str):
        """SQLi must not expose DB errors in search."""
        db_errs = ['sql syntax', 'mysql_fetch', 'pg_query', 'sqlite_', 'ORA-', 'SQLSTATE', 'syntax error']
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(sqli_p, timeout=5000)
            page.wait_for_timeout(500)
            content = page.content().lower()
            for kw in db_errs:
                assert kw not in content, f'DB error exposed! kw={kw!r} payload={sqli_p!r}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('bad_val', ['abc', '000000'])
    @pytest.mark.negative
    def test_invalid_input_search_0015(self, page: Page, bad_val: str):
        """Invalid input from spec — must not cause server error."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(bad_val, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on invalid {bad_val!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.performance
    def test_load_time_0016(self, page: Page):
        """Teacher Reviews Page Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Reviews Page Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0017(self, page: Page):
        """Teacher Reviews Page Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Reviews Page Spec: page title is empty'
        assert '404' not in title, f'Teacher Reviews Page Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Reviews Page Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0018(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Reviews Page Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Reviews Page Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0019(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Reviews Page Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0020(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Reviews Page Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0021(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Reviews Page Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Reviews Page Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0022(self, page: Page):
        """Teacher Reviews Page Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Reviews Page Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0023(self, page: Page):
        """Teacher Reviews Page Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Reviews Page Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0024(self, page: Page):
        """Teacher Reviews Page Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Reviews Page Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0025(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Reviews Page Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0026(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/reviews'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Reviews Page Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Reviews Page Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0027(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Reviews Page Spec: served over HTTP, not HTTPS: {page.url}')

    @pytest.mark.parametrize('boundary', ['', ' ', 'A', 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA', '', 'null', 'undefined', 'None', '0'])
    @pytest.mark.boundary
    def test_boundary_search_0028(self, page: Page, boundary: str):
        """Boundary values for Search."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(boundary, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on boundary {boundary!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.parametrize('value', ['url: /en/dashboard/reviews', '98976564', '123456'])
    @pytest.mark.functional
    def test_fill_search_0029(self, page: Page, value: str):
        """Fill Search with valid data — must not crash."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(value, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error filling Search={value!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not found on {page.url}')



# ==============================================================
# SPEC: Teacher–Student Full E2E Workflow — https://dev.mehadedu.com/en
# ==============================================================
class TestSpec_TeacherStudentE2e:
    """Auto-generated tests from specs/teacher_student_e2e.md
    Page: Teacher–Student Full E2E Workflow  URL: https://dev.mehadedu.com/en
    Selectors: 41  Flows: 11  ECs: 10  Reqs: 13
    """
    PAGE_PATH = '/'
    PAGE_URL  = 'https://dev.mehadedu.com/en'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher–Student Full E2E Workflow loads without server error."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__teacher_signup_with_real_phone_n_0002(self, page: Page):
        """Flow: Flow 1: Teacher Signup with Real Phone Number"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = '/login'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__teacher_creates_group_session_0003(self, page: Page):
        """Flow: Flow 2: Teacher Creates Group Session"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__teacher_sets_1_to_1_availability_0004(self, page: Page):
        """Flow: Flow 3: Teacher Sets 1-to-1 Availability"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_4__student_signup_with_real_phone_n_0005(self, page: Page):
        """Flow: Flow 4: Student Signup with Real Phone Number"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_5__student_finds_and_enrolls_in_gro_0006(self, page: Page):
        """Flow: Flow 5: Student Finds and Enrolls in Group Session"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_6__student_books_1_to_1_trial_lesso_0007(self, page: Page):
        """Flow: Flow 6: Student Books 1-to-1 Trial Lesson"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_7__verify_cross_account_data__teach_0008(self, page: Page):
        """Flow: Flow 7: Verify Cross-Account Data (Teacher sees Student)"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_8__verify_cross_account_data__stude_0009(self, page: Page):
        """Flow: Flow 8: Verify Cross-Account Data (Student sees Teacher)"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0010(self, page: Page):
        """REQ: REQ-01: Teacher phone used for signup must be the same phone used for subsequent"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0011(self, page: Page):
        """REQ: REQ-02: Student phone used for signup must be the same phone used for subsequent"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0012(self, page: Page):
        """REQ: REQ-03: Group session created by teacher appears on student-facing /en/find-tuto"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0013(self, page: Page):
        """REQ: REQ-04: Student enrollment is visible in teachers enrolled students list"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0014(self, page: Page):
        """REQ: REQ-05: Booking created by student appears in teachers calendar/bookings"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0015(self, page: Page):
        """REQ: REQ-06: Teacher profile info (name, bio, subjects, rate) displays correctly on s"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0016(self, page: Page):
        """REQ: REQ-07: Payment of 25 SAR for group session reflects in teachers pending balanc"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0017(self, page: Page):
        """REQ: REQ-08: 1-to-1 availability slots set by teacher are shown on teachers booking """
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0018(self, page: Page):
        """REQ: REQ-09: Both teacher and student can join the classroom using the same session l"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0019(self, page: Page):
        """REQ: REQ-10: Completed session creates a recording accessible to the student"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0020(self, page: Page):
        """REQ: REQ-11: Students My Bookings list matches teachers bookings list (same session"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0021(self, page: Page):
        """REQ: REQ-12: Cancellation by teacher returns hours/credits to student"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0022(self, page: Page):
        """REQ: REQ-13: Student cancellation within 12 hours receives full refund"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0024(self, page: Page):
        """EC-01: Teacher uses same phone to sign up twice"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0026(self, page: Page):
        """EC-02: Student tries to enroll in fully booked session"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0028(self, page: Page):
        """EC-03: Student enrolls same group session twice"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0030(self, page: Page):
        """EC-04: Payment fails (invalid card)"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0032(self, page: Page):
        """EC-05: Teacher cancels session after student enrolled"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0034(self, page: Page):
        """EC-06: Student cancels within 12 hours"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0036(self, page: Page):
        """EC-07: Student cancels after 12-hour window"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_08_0038(self, page: Page):
        """EC-08: Teacher has no availability set"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_09_0040(self, page: Page):
        """EC-09: Both teacher and student log in with same phone"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_10_0042(self, page: Page):
        """EC-10: Session data mismatch between teacher and student view"""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.performance
    def test_load_time_0043(self, page: Page):
        """Teacher–Student Full E2E Workflow loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher–Student Full E2E Workflow took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0044(self, page: Page):
        """Teacher–Student Full E2E Workflow page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher–Student Full E2E Workflow: page title is empty'
        assert '404' not in title, f'Teacher–Student Full E2E Workflow: title says 404: {title}'
        assert '500' not in title, f'Teacher–Student Full E2E Workflow: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0045(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher–Student Full E2E Workflow mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher–Student Full E2E Workflow mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0046(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher–Student Full E2E Workflow tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0047(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher–Student Full E2E Workflow uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0048(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher–Student Full E2E Workflow: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher–Student Full E2E Workflow: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0049(self, page: Page):
        """Teacher–Student Full E2E Workflow body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher–Student Full E2E Workflow: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0050(self, page: Page):
        """Teacher–Student Full E2E Workflow must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher–Student Full E2E Workflow: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0051(self, page: Page):
        """Teacher–Student Full E2E Workflow must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher–Student Full E2E Workflow: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0052(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher–Student Full E2E Workflow 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0053(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher–Student Full E2E Workflow AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher–Student Full E2E Workflow AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0054(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher–Student Full E2E Workflow: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Testimonials Management — Super Admin — https://dev.mehadedu.com/en/super-admin-login
# ==============================================================
class TestSpec_Testimonials:
    """Auto-generated tests from specs/testimonials.md
    Page: Testimonials Management — Super Admin  URL: https://dev.mehadedu.com/en/super-admin-login
    Selectors: 16  Flows: 3  ECs: 4  Reqs: 7
    """
    PAGE_PATH = '/super-admin-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/super-admin-login'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Testimonials Management — Super Admin loads without server error."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__create_active_testimonial_0002(self, page: Page):
        """Flow: Flow 1: Create Active Testimonial"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__create_inactive_testimonial_0003(self, page: Page):
        """Flow: Flow 2: Create Inactive Testimonial"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__edit_testimonial_0004(self, page: Page):
        """Flow: Flow 3: Edit Testimonial"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Testimonials page accessible from Content Management menu"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Name field max 32 characters"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Role field max 32 characters"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Avatar max 2 MB"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Active testimonials visible on landing page"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Inactive testimonials hidden from public"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Delete removes testimonial permanently"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0013(self, page: Page):
        """EC-01: Name over 32 chars"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0015(self, page: Page):
        """EC-02: Avatar over 2 MB"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0017(self, page: Page):
        """EC-03: Create with Inactive status"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0019(self, page: Page):
        """EC-04: Delete active testimonial"""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0020(self, page: Page):
        """Testimonials Management — Super Admin loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Testimonials Management — Super Admin took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0021(self, page: Page):
        """Testimonials Management — Super Admin page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Testimonials Management — Super Admin: page title is empty'
        assert '404' not in title, f'Testimonials Management — Super Admin: title says 404: {title}'
        assert '500' not in title, f'Testimonials Management — Super Admin: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0022(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Testimonials Management — Super Admin mobile viewport 500: {title}'
        assert '502' not in title, f'Testimonials Management — Super Admin mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0023(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Testimonials Management — Super Admin tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0024(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Testimonials Management — Super Admin uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0025(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Testimonials Management — Super Admin: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Testimonials Management — Super Admin: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0026(self, page: Page):
        """Testimonials Management — Super Admin body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Testimonials Management — Super Admin: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0027(self, page: Page):
        """Testimonials Management — Super Admin must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Testimonials Management — Super Admin: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0028(self, page: Page):
        """Testimonials Management — Super Admin must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Testimonials Management — Super Admin: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0029(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Testimonials Management — Super Admin 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0030(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/super-admin-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Testimonials Management — Super Admin AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Testimonials Management — Super Admin AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0031(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/super-admin-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Testimonials Management — Super Admin: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Translation Management — Super Admin — https://dev.mehadedu.com/en/dashboard/translations
# ==============================================================
class TestSpec_Translations:
    """Auto-generated tests from specs/translations.md
    Page: Translation Management — Super Admin  URL: https://dev.mehadedu.com/en/dashboard/translations
    Selectors: 16  Flows: 3  ECs: 4  Reqs: 7
    """
    PAGE_PATH = '/dashboard/translations'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/translations'

    pytestmark = pytest.mark.skipif(
        not (_os.getenv('SUPER_ADMIN_EMAIL') and _os.getenv('SUPER_ADMIN_PASS')),
        reason='Super Admin credentials not configured; admin-only spec skipped until credentials are provided',
    )

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Translation Management — Super Admin loads without server error."""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__create_translation_0002(self, page: Page):
        """Flow: Flow 1: Create Translation"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__filter_translations_0003(self, page: Page):
        """Flow: Flow 2: Filter Translations"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__edit_translation_0004(self, page: Page):
        """Flow: Flow 3: Edit Translation"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Translation module accessible from Account Settings"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Key-value pairs support English and Arabic"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Role-based: Admin, Student, Tutor, Public"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Group-based organization"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Search by key or value works"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Edit saves and applies immediately"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Delete removes translation from system"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0013(self, page: Page):
        """EC-01: Empty key"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0015(self, page: Page):
        """EC-02: Duplicate key for same role/group"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0017(self, page: Page):
        """EC-03: Delete used translation"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0019(self, page: Page):
        """EC-04: Search with no match"""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0020(self, page: Page):
        """Translation Management — Super Admin loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Translation Management — Super Admin took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0021(self, page: Page):
        """Translation Management — Super Admin page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Translation Management — Super Admin: page title is empty'
        assert '404' not in title, f'Translation Management — Super Admin: title says 404: {title}'
        assert '500' not in title, f'Translation Management — Super Admin: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0022(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Translation Management — Super Admin mobile viewport 500: {title}'
        assert '502' not in title, f'Translation Management — Super Admin mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0023(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Translation Management — Super Admin tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0024(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Translation Management — Super Admin uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0025(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Translation Management — Super Admin: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Translation Management — Super Admin: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0026(self, page: Page):
        """Translation Management — Super Admin body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Translation Management — Super Admin: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0027(self, page: Page):
        """Translation Management — Super Admin must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Translation Management — Super Admin: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0028(self, page: Page):
        """Translation Management — Super Admin must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Translation Management — Super Admin: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0029(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Translation Management — Super Admin 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0030(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/translations'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Translation Management — Super Admin AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Translation Management — Super Admin AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0031(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/translations', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Translation Management — Super Admin: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Tutor Availability Calendar — Full Flow — /en/dashboard/availability
# ==============================================================
class TestSpec_TutorAvailabilityCalendarFull:
    """Auto-generated tests from specs/tutor_availability_calendar_full.md
    Page: Spec: Tutor Availability Calendar — Full Flow  URL: /en/dashboard/availability
    Selectors: 17  Flows: 6  ECs: 4  Reqs: 13
    """
    PAGE_PATH = '/dashboard/availability'
    PAGE_URL  = '/en/dashboard/availability'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Tutor Availability Calendar — Full Flow loads without server error."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_date_0002(self, page: Page):
        """Element must be present: Date"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.date.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Date' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_apply_0003(self, page: Page):
        """Element must be present: Apply"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.apply.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Apply' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_cancel_0004(self, page: Page):
        """Element must be present: Cancel"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.cancel.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Cancel' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_open_the_add_availability_modal_0005(self, page: Page):
        """Flow: Open the Add Availability modal"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_set_date_range_and_select_days_0006(self, page: Page):
        """Flow: Set date range and select days"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        try:
            page.date.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_select_start_and_end_times_0007(self, page: Page):
        """Flow: Select start and end times"""
        try:
            page.date.click(timeout=5000)
            page.wait_for_timeout(500)
        except Exception: pass
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_apply_creates_slots_0008(self, page: Page):
        """Flow: Apply creates slots"""
        page.wait_for_timeout(300)
        try:
            page.apply.click(timeout=5000)
            page.wait_for_timeout(500)
        except Exception: pass
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_view_slot_details__day_slots_dialog_0009(self, page: Page):
        """Flow: View slot details (Day Slots dialog)"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_delete_a_slot_0010(self, page: Page):
        """Flow: Delete a slot"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0011(self, page: Page):
        """REQ: REQ-01: a dialog titled Add Availability opens"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        _exp = 'Add Availability'
        t = page.title()
        assert _exp in t or '500' not in t, f'Expected title with {_exp!r}, got {t!r}'

    @pytest.mark.requirement
    def test_req_01_0012(self, page: Page):
        """REQ: REQ-02: the dialog contains:"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0013(self, page: Page):
        """REQ: REQ-03: clicks the M day button (Monday)"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0014(self, page: Page):
        """REQ: REQ-04: the M button becomes [active]"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0015(self, page: Page):
        """REQ: REQ-05: the Apply button becomes enabled"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0016(self, page: Page):
        """REQ: REQ-06: options appear in 30-minute increments from 12:00 AM to 11:30 PM"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.date.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_06_0017(self, page: Page):
        """REQ: REQ-07: opens the End Time combobox"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0018(self, page: Page):
        """REQ: REQ-08: only times after 10:00 AM are enabled (e.g. 10:30 AM through 11:30 """
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0019(self, page: Page):
        """REQ: REQ-09: the end time shows 12:00 PM"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0020(self, page: Page):
        """REQ: REQ-10: the dialog closes"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0021(self, page: Page):
        """REQ: REQ-11: a success toast appears"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.date.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_11_0022(self, page: Page):
        """REQ: REQ-12: a dialog opens showing:"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0023(self, page: Page):
        """REQ: REQ-13: the slot is removed from the dialog and from the calendar cell"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0025(self, page: Page):
        """EC-01: Calendar cell with no slots shows No slots"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0027(self, page: Page):
        """EC-02: Apply button stays disabled until both date range, at least """
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0029(self, page: Page):
        """EC-03: End Time combobox disables options earlier than the selected"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0031(self, page: Page):
        """EC-04: Navigating to previous months is blocked (prev month button """
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0032(self, page: Page):
        """Spec: Tutor Availability Calendar — Full Flow loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Tutor Availability Calendar — Full Flow took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0033(self, page: Page):
        """Spec: Tutor Availability Calendar — Full Flow page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Tutor Availability Calendar — Full Flow: page title is empty'
        assert '404' not in title, f'Spec: Tutor Availability Calendar — Full Flow: title says 404: {title}'
        assert '500' not in title, f'Spec: Tutor Availability Calendar — Full Flow: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0034(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor Availability Calendar — Full Flow mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Tutor Availability Calendar — Full Flow mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0035(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor Availability Calendar — Full Flow tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0036(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Tutor Availability Calendar — Full Flow uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0037(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Tutor Availability Calendar — Full Flow: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Tutor Availability Calendar — Full Flow: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0038(self, page: Page):
        """Spec: Tutor Availability Calendar — Full Flow body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Tutor Availability Calendar — Full Flow: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0039(self, page: Page):
        """Spec: Tutor Availability Calendar — Full Flow must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Tutor Availability Calendar — Full Flow: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0040(self, page: Page):
        """Spec: Tutor Availability Calendar — Full Flow must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Tutor Availability Calendar — Full Flow: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0041(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Tutor Availability Calendar — Full Flow 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0042(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/availability'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor Availability Calendar — Full Flow AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Tutor Availability Calendar — Full Flow AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0043(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Tutor Availability Calendar — Full Flow: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Availability Calendar — https://dev.mehadedu.com/en/dashboard/availability
# ==============================================================
class TestSpec_TutorAvailbilityCalender:
    """Auto-generated tests from specs/tutor_availbility_calender.md
    Page: Tutor Availability Calendar  URL: https://dev.mehadedu.com/en/dashboard/availability
    Selectors: 16  Flows: 3  ECs: 8  Reqs: 10
    """
    PAGE_PATH = '/dashboard/availability'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/availability'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Availability Calendar loads without server error."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__create_group_session__3_page_mod_0002(self, page: Page):
        """Flow: Flow 1: Create Group Session (3-page modal)"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__create_1_to_1_availability_0003(self, page: Page):
        """Flow: Flow 2: Create 1-to-1 Availability"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__view_existing_sessions_0004(self, page: Page):
        """Flow: Flow 3: View Existing Sessions"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Availability calendar page loads for authenticated tutors"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Add Availability Time button is visible and clickable"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Group Session button is visible and clickable"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Group session modal has 3 pages/steps"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Start time must be set at least 30 minutes before current time"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: End date must be after or equal to start date"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Course name is required for group sessions"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Maximum students must be greater than minimum students"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: Price per student must be a positive number"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: Session creation shows confirmation and closes modal"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: End date before start date"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: No days selected in modal"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: Minimum students >= maximum students"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0022(self, page: Page):
        """EC-04: Price of 0"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0024(self, page: Page):
        """EC-05: Start time in the past"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0026(self, page: Page):
        """EC-06: Empty course name"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0028(self, page: Page):
        """EC-07: Maximum students = 0"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_08_0030(self, page: Page):
        """EC-08: Modal closed mid-creation"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0031(self, page: Page):
        """Tutor Availability Calendar loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Availability Calendar took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0032(self, page: Page):
        """Tutor Availability Calendar page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Availability Calendar: page title is empty'
        assert '404' not in title, f'Tutor Availability Calendar: title says 404: {title}'
        assert '500' not in title, f'Tutor Availability Calendar: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0033(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Availability Calendar mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Availability Calendar mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0034(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Availability Calendar tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0035(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Availability Calendar uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0036(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Availability Calendar: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Availability Calendar: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0037(self, page: Page):
        """Tutor Availability Calendar body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Availability Calendar: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0038(self, page: Page):
        """Tutor Availability Calendar must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Availability Calendar: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0039(self, page: Page):
        """Tutor Availability Calendar must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Availability Calendar: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0040(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Availability Calendar 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0041(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/availability'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Availability Calendar AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Availability Calendar AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0042(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Availability Calendar: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Conversations / Messages — https://dev.mehadedu.com/en/dashboard/messages
# ==============================================================
class TestSpec_TutorConversation:
    """Auto-generated tests from specs/tutor_conversation.md
    Page: Tutor Conversations / Messages  URL: https://dev.mehadedu.com/en/dashboard/messages
    Selectors: 10  Flows: 3  ECs: 5  Reqs: 8
    """
    PAGE_PATH = '/dashboard/messages'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/messages'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Conversations / Messages loads without server error."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_and_send_a_message_0002(self, page: Page):
        """Flow: Flow 1: View and Send a Message"""
        page.wait_for_timeout(300)
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__check_unread_messages_0003(self, page: Page):
        """Flow: Flow 2: Check Unread Messages"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__switch_conversations_0004(self, page: Page):
        """Flow: Flow 3: Switch Conversations"""
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Messages accessible from tutor dashboard sidebar"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: All conversations displayed in list"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Clicking conversation loads chat history"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Message input accepts text"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Send button works and message appears"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Unread tab shows unread conversations"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Opening unread conversation marks as read"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Special characters in messages handled correctly"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0014(self, page: Page):
        """EC-01: Empty message send attempt"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0016(self, page: Page):
        """EC-02: XSS in message content"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'XSS EC check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0018(self, page: Page):
        """EC-03: Switch conversations quickly"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0020(self, page: Page):
        """EC-04: No conversations exist"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0022(self, page: Page):
        """EC-05: Network error during send"""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0023(self, page: Page):
        """Tutor Conversations / Messages loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Conversations / Messages took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0024(self, page: Page):
        """Tutor Conversations / Messages page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Conversations / Messages: page title is empty'
        assert '404' not in title, f'Tutor Conversations / Messages: title says 404: {title}'
        assert '500' not in title, f'Tutor Conversations / Messages: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0025(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Conversations / Messages mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Conversations / Messages mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0026(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Conversations / Messages tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0027(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Conversations / Messages uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0028(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Conversations / Messages: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Conversations / Messages: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0029(self, page: Page):
        """Tutor Conversations / Messages body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Conversations / Messages: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0030(self, page: Page):
        """Tutor Conversations / Messages must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Conversations / Messages: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0031(self, page: Page):
        """Tutor Conversations / Messages must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Conversations / Messages: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0032(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Conversations / Messages 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0033(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/messages'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Conversations / Messages AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Conversations / Messages AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0034(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/messages', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Conversations / Messages: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Earnings and Payout — https://dev.mehadedu.com/en/dashboard/earnings
# ==============================================================
class TestSpec_TutorEarningsPayout:
    """Auto-generated tests from specs/tutor_earnings_payout.md
    Page: Tutor Earnings and Payout  URL: https://dev.mehadedu.com/en/dashboard/earnings
    Selectors: 9  Flows: 3  ECs: 5  Reqs: 8
    """
    PAGE_PATH = '/dashboard/earnings'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/earnings'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Earnings and Payout loads without server error."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_earnings_overview_0002(self, page: Page):
        """Flow: Flow 1: View Earnings Overview"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__verify_pending_earnings_after_gr_0003(self, page: Page):
        """Flow: Flow 2: Verify Pending Earnings After Group Session"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__verify_available_balance_after_1_0004(self, page: Page):
        """Flow: Flow 3: Verify Available Balance After 1-to-1 Session"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Earnings page loads at /en/dashboard/earnings"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Available Balance shows withdrawable amount"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Pending Earnings shows revenue awaiting course completion"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Pending earnings move to Available Balance after course completes"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Complete Payouts shows all admin-approved payments"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Earnings are accurate and up-to-date"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: 1-to-1 session earnings go to Available Balance"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Group session earnings appear in Pending Earnings until completion"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0014(self, page: Page):
        """EC-01: No sessions completed yet"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0016(self, page: Page):
        """EC-02: Pending earnings before course end"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0018(self, page: Page):
        """EC-03: After course completion"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0020(self, page: Page):
        """EC-04: Admin approves payout"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0022(self, page: Page):
        """EC-05: Page refresh"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0023(self, page: Page):
        """Tutor Earnings and Payout loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Earnings and Payout took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0024(self, page: Page):
        """Tutor Earnings and Payout page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Earnings and Payout: page title is empty'
        assert '404' not in title, f'Tutor Earnings and Payout: title says 404: {title}'
        assert '500' not in title, f'Tutor Earnings and Payout: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0025(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Earnings and Payout mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Earnings and Payout mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0026(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Earnings and Payout tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0027(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Earnings and Payout uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0028(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Earnings and Payout: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Earnings and Payout: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0029(self, page: Page):
        """Tutor Earnings and Payout body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Earnings and Payout: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0030(self, page: Page):
        """Tutor Earnings and Payout must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Earnings and Payout: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0031(self, page: Page):
        """Tutor Earnings and Payout must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Earnings and Payout: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0032(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Earnings and Payout 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0033(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/earnings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Earnings and Payout AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Earnings and Payout AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0034(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Earnings and Payout: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Tutor — Earnings & Payouts (Full) — /en/dashboard/earnings
# ==============================================================
class TestSpec_TutorEarningsPayoutsFull:
    """Auto-generated tests from specs/tutor_earnings_payouts_full.md
    Page: Spec: Tutor — Earnings & Payouts (Full)  URL: /en/dashboard/earnings
    Selectors: 19  Flows: 5  ECs: 3  Reqs: 14
    """
    PAGE_PATH = '/dashboard/earnings'
    PAGE_URL  = '/en/dashboard/earnings'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Tutor — Earnings & Payouts (Full) loads without server error."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_payout_0002(self, page: Page):
        """Element must be present: Payout"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.payout.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Payout' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_earnings_page_initial_state__no_complete_0003(self, page: Page):
        """Flow: Earnings page initial state (no completed sessions)"""
        page.wait_for_timeout(200)
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_earnings_tab_selected__default_0004(self, page: Page):
        """Flow: Earnings tab selected (default)"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_switch_to_payout_tab_0005(self, page: Page):
        """Flow: Switch to Payout tab"""
        page.wait_for_timeout(200)
        try:
            page.payout.click(timeout=5000)
            page.wait_for_timeout(500)
        except Exception: pass
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_earnings_appear_after_a_completed_sessio_0006(self, page: Page):
        """Flow: Earnings appear after a completed session (expected behavior"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_sidebar_notification_badge_0007(self, page: Page):
        """Flow: Sidebar notification badge"""
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0008(self, page: Page):
        """REQ: REQ-01: the heading Earnings & Payouts is displayed"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.payout.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0009(self, page: Page):
        """REQ: REQ-02: four summary cards are shown:"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.payout.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0010(self, page: Page):
        """REQ: REQ-03: two buttons are visible: Earnings and Payout"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.payout.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0011(self, page: Page):
        """REQ: REQ-04: the Recent Earnings section shows No earnings yet."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0012(self, page: Page):
        """REQ: REQ-05: the Earnings tab is active by default"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0013(self, page: Page):
        """REQ: REQ-06: Recent Earnings heading is visible"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.payout.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_06_0014(self, page: Page):
        """REQ: REQ-07: No earnings yet. text is shown when no transactions exist"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.payout.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_07_0015(self, page: Page):
        """REQ: REQ-08: the tab switches to Payout view"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0016(self, page: Page):
        """REQ: REQ-09: payout history or empty state is shown"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.payout.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_09_0017(self, page: Page):
        """REQ: REQ-10: the tutors Total Earnings increases by the session fee"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0018(self, page: Page):
        """REQ: REQ-11: a transaction appears in Recent Earnings showing:"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.payout.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_11_0019(self, page: Page):
        """REQ: REQ-12: Pending Earnings may show the amount before release period"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0020(self, page: Page):
        """REQ: REQ-13: the sidebar Messages link shows a badge 1"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_13_0021(self, page: Page):
        """REQ: REQ-14: Notifications link shows a badge 1 for system notifications"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0023(self, page: Page):
        """EC-01: No earnings yet.  when no sessions have been paid and comple"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0025(self, page: Page):
        """EC-02: No upcoming sessions found on Booked Sessions when no confir"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0027(self, page: Page):
        """EC-03: All earnings summary values show 0.00 when no transactions"""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0028(self, page: Page):
        """Spec: Tutor — Earnings & Payouts (Full) loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Tutor — Earnings & Payouts (Full) took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0029(self, page: Page):
        """Spec: Tutor — Earnings & Payouts (Full) page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Tutor — Earnings & Payouts (Full): page title is empty'
        assert '404' not in title, f'Spec: Tutor — Earnings & Payouts (Full): title says 404: {title}'
        assert '500' not in title, f'Spec: Tutor — Earnings & Payouts (Full): title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0030(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor — Earnings & Payouts (Full) mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Tutor — Earnings & Payouts (Full) mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0031(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor — Earnings & Payouts (Full) tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0032(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Tutor — Earnings & Payouts (Full) uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0033(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Tutor — Earnings & Payouts (Full): final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Tutor — Earnings & Payouts (Full): stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0034(self, page: Page):
        """Spec: Tutor — Earnings & Payouts (Full) body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Tutor — Earnings & Payouts (Full): body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0035(self, page: Page):
        """Spec: Tutor — Earnings & Payouts (Full) must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Tutor — Earnings & Payouts (Full): no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0036(self, page: Page):
        """Spec: Tutor — Earnings & Payouts (Full) must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Tutor — Earnings & Payouts (Full): debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0037(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Tutor — Earnings & Payouts (Full) 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0038(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/earnings'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor — Earnings & Payouts (Full) AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Tutor — Earnings & Payouts (Full) AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0039(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/earnings', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Tutor — Earnings & Payouts (Full): served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Group Session Management — https://dev.mehadedu.com/en/dashboard/group-sessions
# ==============================================================
class TestSpec_TutorGroupSession:
    """Auto-generated tests from specs/tutor_group_session.md
    Page: Tutor Group Session Management  URL: https://dev.mehadedu.com/en/dashboard/group-sessions
    Selectors: 11  Flows: 4  ECs: 5  Reqs: 9
    """
    PAGE_PATH = '/dashboard/group-sessions'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/group-sessions'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Group Session Management loads without server error."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_group_sessions_list_0002(self, page: Page):
        """Flow: Flow 1: View Group Sessions List"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__manage_a_group_session_0003(self, page: Page):
        """Flow: Flow 2: Manage a Group Session"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__edit_a_group_session__before_enr_0004(self, page: Page):
        """Flow: Flow 3: Edit a Group Session (Before Enrollment)"""
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_4__join_classroom_at_session_time_0005(self, page: Page):
        """Flow: Flow 4: Join Classroom at Session Time"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0006(self, page: Page):
        """REQ: REQ-01: Group sessions page shows all created sessions"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0007(self, page: Page):
        """REQ: REQ-02: Each session card has Manage and Edit buttons"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0008(self, page: Page):
        """REQ: REQ-03: Edit button is disabled or hidden after student enrollment"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0009(self, page: Page):
        """REQ: REQ-04: Manage view shows Class and Student tabs"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0010(self, page: Page):
        """REQ: REQ-05: Class tab shows session schedule"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0011(self, page: Page):
        """REQ: REQ-06: Student tab shows enrolled students and attendance"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0012(self, page: Page):
        """REQ: REQ-07: Join Classroom button appears only at scheduled session time"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0013(self, page: Page):
        """REQ: REQ-08: Session automatically closes at end time"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0014(self, page: Page):
        """REQ: REQ-09: Revenue displayed after student enrollment"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0016(self, page: Page):
        """EC-01: Edit session with enrolled students"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0018(self, page: Page):
        """EC-02: Manage session with 0 students"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0020(self, page: Page):
        """EC-03: Join Classroom before session time"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0022(self, page: Page):
        """EC-04: Session at end time"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0024(self, page: Page):
        """EC-05: Click Join twice"""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0025(self, page: Page):
        """Tutor Group Session Management loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Group Session Management took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0026(self, page: Page):
        """Tutor Group Session Management page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Group Session Management: page title is empty'
        assert '404' not in title, f'Tutor Group Session Management: title says 404: {title}'
        assert '500' not in title, f'Tutor Group Session Management: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0027(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Group Session Management mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Group Session Management mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0028(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Group Session Management tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0029(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Group Session Management uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0030(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Group Session Management: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Group Session Management: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0031(self, page: Page):
        """Tutor Group Session Management body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Group Session Management: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0032(self, page: Page):
        """Tutor Group Session Management must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Group Session Management: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0033(self, page: Page):
        """Tutor Group Session Management must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Group Session Management: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0034(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Group Session Management 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0035(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/group-sessions'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Group Session Management AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Group Session Management AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0036(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/group-sessions', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Group Session Management: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Spec: Tutor Group Session — Create Flow — /en/dashboard/availability
# ==============================================================
class TestSpec_TutorGroupSessionCreate:
    """Auto-generated tests from specs/tutor_group_session_create.md
    Page: Spec: Tutor Group Session — Create Flow  URL: /en/dashboard/availability
    Selectors: 18  Flows: 5  ECs: 4  Reqs: 19
    """
    PAGE_PATH = '/dashboard/availability'
    PAGE_URL  = '/en/dashboard/availability'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Spec: Tutor Group Session — Create Flow loads without server error."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_calendar_0002(self, page: Page):
        """Element must be present: Calendar"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.calendar.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Calendar' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_numeric_0003(self, page: Page):
        """Element must be present: Numeric"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.numeric.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Numeric' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_subject_0004(self, page: Page):
        """Element must be present: Subject"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.subject.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Subject' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_submit_0005(self, page: Page):
        """Element must be present: Submit"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.submit.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Submit' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_create_0006(self, page: Page):
        """Element must be present: Create"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.create.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Create' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_open_group_session_creation_wizard_0007(self, page: Page):
        """Flow: Open group session creation wizard"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_fill_step_1_session_info_0008(self, page: Page):
        """Flow: Fill Step 1 session info"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_fill_step_2_schedule_0009(self, page: Page):
        """Flow: Fill Step 2 schedule"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_fill_step_3_pricing_and_submit_0010(self, page: Page):
        """Flow: Fill Step 3 pricing and submit"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_view_group_sessions_list_0011(self, page: Page):
        """Flow: View group sessions list"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0012(self, page: Page):
        """REQ: REQ-01: a dialog opens with title Create Group Session"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        _exp = 'Create Group Session'
        t = page.title()
        assert _exp in t or '500' not in t, f'Expected title with {_exp!r}, got {t!r}'

    @pytest.mark.requirement
    def test_req_01_0013(self, page: Page):
        """REQ: REQ-02: the wizard shows Step 1 indicator active"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0014(self, page: Page):
        """REQ: REQ-03: Step 2 and Step 3 indicators are visible but inactive"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.calendar.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0015(self, page: Page):
        """REQ: REQ-04: the Subject field shows Math (read-only, auto-set)"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0016(self, page: Page):
        """REQ: REQ-05: the tutor enters description text"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0017(self, page: Page):
        """REQ: REQ-06: clicks Next"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0018(self, page: Page):
        """REQ: REQ-07: the wizard advances to Step 2 (Schedule)"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0019(self, page: Page):
        """REQ: REQ-08: clicks the M day button to select Monday"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0020(self, page: Page):
        """REQ: REQ-09: selects Start Time 10:00 AM"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0021(self, page: Page):
        """REQ: REQ-10: selects End Time 12:00 PM"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0022(self, page: Page):
        """REQ: REQ-11: sets Max Students to 10"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0023(self, page: Page):
        """REQ: REQ-12: the wizard advances to Step 3 (Pricing)"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0024(self, page: Page):
        """REQ: REQ-13: clicks the Create button"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_13_0025(self, page: Page):
        """REQ: REQ-14: the dialog closes"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_14_0026(self, page: Page):
        """REQ: REQ-15: a success toast appears"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.calendar.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_15_0027(self, page: Page):
        """REQ: REQ-16: the group session QA Math Group Session appears in the list at /en/das"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.calendar.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_16_0028(self, page: Page):
        """REQ: REQ-17: the page shows a list of created group sessions"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_17_0029(self, page: Page):
        """REQ: REQ-18: each row shows: session name, subject, schedule, enrollment status (e.g."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_18_0030(self, page: Page):
        """REQ: REQ-19: there is no Create button on this page (creation only via Availability"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0032(self, page: Page):
        """EC-01: Subject field is readonly  filling it causes no change; do n"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0034(self, page: Page):
        """EC-02: Day button must be clicked explicitly even if date is set to"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0036(self, page: Page):
        """EC-03: QA Math Group Session shows on tutor profile under Group Ses"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0038(self, page: Page):
        """EC-04: Empty state on `/en/dashboard/group-sessions`: No group sess"""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.performance
    def test_load_time_0039(self, page: Page):
        """Spec: Tutor Group Session — Create Flow loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Spec: Tutor Group Session — Create Flow took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0040(self, page: Page):
        """Spec: Tutor Group Session — Create Flow page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Spec: Tutor Group Session — Create Flow: page title is empty'
        assert '404' not in title, f'Spec: Tutor Group Session — Create Flow: title says 404: {title}'
        assert '500' not in title, f'Spec: Tutor Group Session — Create Flow: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0041(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor Group Session — Create Flow mobile viewport 500: {title}'
        assert '502' not in title, f'Spec: Tutor Group Session — Create Flow mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0042(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor Group Session — Create Flow tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0043(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Spec: Tutor Group Session — Create Flow uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0044(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Spec: Tutor Group Session — Create Flow: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Spec: Tutor Group Session — Create Flow: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0045(self, page: Page):
        """Spec: Tutor Group Session — Create Flow body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Spec: Tutor Group Session — Create Flow: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0046(self, page: Page):
        """Spec: Tutor Group Session — Create Flow must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Spec: Tutor Group Session — Create Flow: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0047(self, page: Page):
        """Spec: Tutor Group Session — Create Flow must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Spec: Tutor Group Session — Create Flow: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0048(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Spec: Tutor Group Session — Create Flow 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0049(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/availability'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Spec: Tutor Group Session — Create Flow AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Spec: Tutor Group Session — Create Flow AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0050(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/availability', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Spec: Tutor Group Session — Create Flow: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Instructor Profile — https://dev.mehadedu.com/en/dashboard/instructor-profile
# ==============================================================
class TestSpec_TutorInstructorProfile:
    """Auto-generated tests from specs/tutor_instructor_profile.md
    Page: Tutor Instructor Profile  URL: https://dev.mehadedu.com/en/dashboard/instructor-profile
    Selectors: 16  Flows: 3  ECs: 5  Reqs: 8
    """
    PAGE_PATH = '/dashboard/instructor-profile'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/instructor-profile'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Instructor Profile loads without server error."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__update_personal_information_0002(self, page: Page):
        """Flow: Flow 1: Update Personal Information"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__add_certificate_0003(self, page: Page):
        """Flow: Flow 2: Add Certificate"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__update_teaching_subjects_0004(self, page: Page):
        """Flow: Flow 3: Update Teaching Subjects"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Instructor profile has Personal Info, Certificates, and Teaching Subject"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Bio maximum 500 characters"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Experience field maximum 1000 characters"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: About Subject maximum 500 characters"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Hourly rate between 0 and 10000"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Certificate upload maximum 10 MB"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Changes set to pending status until admin approves"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Graduation year must be 4-digit number"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0014(self, page: Page):
        """EC-01: Bio over 500 chars"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0016(self, page: Page):
        """EC-02: Hourly rate over 10000"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0018(self, page: Page):
        """EC-03: Certificate file over 10 MB"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0020(self, page: Page):
        """EC-04: Graduation year non-numeric"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0022(self, page: Page):
        """EC-05: Empty required subject fields"""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0023(self, page: Page):
        """Tutor Instructor Profile loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Instructor Profile took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0024(self, page: Page):
        """Tutor Instructor Profile page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Instructor Profile: page title is empty'
        assert '404' not in title, f'Tutor Instructor Profile: title says 404: {title}'
        assert '500' not in title, f'Tutor Instructor Profile: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0025(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Instructor Profile mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Instructor Profile mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0026(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Instructor Profile tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0027(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Instructor Profile uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0028(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Instructor Profile: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Instructor Profile: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0029(self, page: Page):
        """Tutor Instructor Profile body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Instructor Profile: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0030(self, page: Page):
        """Tutor Instructor Profile must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Instructor Profile: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0031(self, page: Page):
        """Tutor Instructor Profile must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Instructor Profile: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0032(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Instructor Profile 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0033(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/instructor-profile'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Instructor Profile AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Instructor Profile AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0034(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/instructor-profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Instructor Profile: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Teacher Login Page Spec — https://dev.mehadedu.com/en/tutor-login
# ==============================================================
class TestSpec_TutorLoginPage:
    """Auto-generated tests from specs/tutor_login_page.md
    Page: Teacher Login Page Spec  URL: https://dev.mehadedu.com/en/tutor-login
    Selectors: 30  Flows: 12  ECs: 0  Reqs: 12
    """
    PAGE_PATH = '/tutor-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/tutor-login'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Teacher Login Page Spec loads without server error."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_disabled_0002(self, page: Page):
        """Element must be present: Disabled"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.disabled.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Disabled' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_enabled_0003(self, page: Page):
        """Element must be present: Enabled"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.enabled.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Enabled' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_search_0004(self, page: Page):
        """Element must be present: Search"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.search.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Search' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_phone_0005(self, page: Page):
        """Element must be present: Phone"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.phone.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Phone' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_otp_0006(self, page: Page):
        """Element must be present: Otp"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.otp.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Otp' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_page_loads_with_teacher_login_heading_0007(self, page: Page):
        """Flow: Page loads with Teacher Login heading"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_form_is_inline__not_a_modal_dialog_0008(self, page: Page):
        """Flow: Form is inline (not a modal dialog)"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_country_code_defaults_to__966_0009(self, page: Page):
        """Flow: Country code defaults to +966"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_country_code_can_be_changed_via_searchab_0010(self, page: Page):
        """Flow: Country code can be changed via searchable dropdown"""
        page.wait_for_timeout(200)
        try:
            page.search.click(timeout=5000)
            page.wait_for_timeout(500)
        except Exception: pass
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_send_code_button_disabled_when_phone_is__0011(self, page: Page):
        """Flow: Send Code button disabled when phone is empty"""
        page.wait_for_timeout(200)
        try:
            page.phone.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_send_code_enables_after_valid_phone_entr_0012(self, page: Page):
        """Flow: Send Code enables after valid phone entry"""
        page.wait_for_timeout(200)
        try:
            page.phone.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_otp_field_is_disabled_until_send_code_is_0013(self, page: Page):
        """Flow: OTP field is disabled until Send Code is clicked"""
        try:
            page.phone.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(300)
        try:
            page.disabled.fill('test_value', timeout=5000)
        except Exception: pass
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_after_send_code__otp_field_enables_and_r_0014(self, page: Page):
        """Flow: After Send Code, OTP field enables and Resend timer appears"""
        try:
            page.phone.fill('test_value', timeout=5000)
        except Exception: pass
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0015(self, page: Page):
        """REQ: REQ-01: the heading Teacher Login and subtitle Sign in to access your teachin"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        _exp = 'Teacher Login'
        t = page.title()
        assert _exp in t or '500' not in t, f'Expected title with {_exp!r}, got {t!r}'

    @pytest.mark.requirement
    def test_req_01_0016(self, page: Page):
        """REQ: REQ-02: the OTP form is displayed inline on the page (not inside a [role=dialog]"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.disabled.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0017(self, page: Page):
        """REQ: REQ-03: it shows +966 (Saudi Arabia) by default"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0018(self, page: Page):
        """REQ: REQ-04: the Bangladesh +880 option appears and can be selected"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.disabled.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_04_0019(self, page: Page):
        """REQ: REQ-05: the Send Code button is disabled"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0020(self, page: Page):
        """REQ: REQ-06: the Send Code button becomes enabled and a Valid phone number hint app"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.disabled.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_06_0021(self, page: Page):
        """REQ: REQ-07: the OTP input field with placeholder 000000 is disabled"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0022(self, page: Page):
        """REQ: REQ-08: the OTP field becomes enabled, Code already sent hint appears, and a d"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.disabled.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_08_0023(self, page: Page):
        """REQ: REQ-09: the browser navigates to `/en/dashboard/availability` and the sidebar sh"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0024(self, page: Page):
        """REQ: REQ-10: the OTP field is hidden/disabled and the phone entry state is restored"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0025(self, page: Page):
        """REQ: REQ-11: an error message is displayed and login does not succeed"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Error message check: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0026(self, page: Page):
        """REQ: REQ-12: the Send Code button remains disabled"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.parametrize('xss_p', ['<script>alert(1)</script>', '"><script>alert(1)</script>', '<img src=x onerror=alert(1)>', '<svg onload=alert(1)>', 'javascript:alert(1)', '<body onload=alert(1)>'])
    @pytest.mark.security
    def test_xss_search_0027(self, page: Page, xss_p: str):
        """XSS payload must not execute in search."""
        alerts: list = []
        page.on('dialog', lambda d: (alerts.append(d.message), d.dismiss()))
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(xss_p, timeout=5000)
            page.wait_for_timeout(500)
            assert len(alerts) == 0, f'XSS executed! payload={xss_p!r} alerts={alerts}'
            assert '500' not in page.title(), f'Server error on XSS: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('sqli_p', ["' OR '1'='1", "' OR '1'='1'--", "' OR 1=1--", "admin'--", "' OR 'x'='x", "'; DROP TABLE users;--"])
    @pytest.mark.security
    def test_sqli_search_0028(self, page: Page, sqli_p: str):
        """SQLi must not expose DB errors in search."""
        db_errs = ['sql syntax', 'mysql_fetch', 'pg_query', 'sqlite_', 'ORA-', 'SQLSTATE', 'syntax error']
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(sqli_p, timeout=5000)
            page.wait_for_timeout(500)
            content = page.content().lower()
            for kw in db_errs:
                assert kw not in content, f'DB error exposed! kw={kw!r} payload={sqli_p!r}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field not found on {page.url}')

    @pytest.mark.parametrize('bad_val', ['abc', '000000'])
    @pytest.mark.negative
    def test_invalid_input_search_0029(self, page: Page, bad_val: str):
        """Invalid input from spec — must not cause server error."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(bad_val, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on invalid {bad_val!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.performance
    def test_load_time_0030(self, page: Page):
        """Teacher Login Page Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Teacher Login Page Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0031(self, page: Page):
        """Teacher Login Page Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Teacher Login Page Spec: page title is empty'
        assert '404' not in title, f'Teacher Login Page Spec: title says 404: {title}'
        assert '500' not in title, f'Teacher Login Page Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0032(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Login Page Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Teacher Login Page Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0033(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Login Page Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0034(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Teacher Login Page Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0035(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Teacher Login Page Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Teacher Login Page Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0036(self, page: Page):
        """Teacher Login Page Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Teacher Login Page Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0037(self, page: Page):
        """Teacher Login Page Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Teacher Login Page Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0038(self, page: Page):
        """Teacher Login Page Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Teacher Login Page Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0039(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Teacher Login Page Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0040(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/tutor-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Teacher Login Page Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Teacher Login Page Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0041(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Teacher Login Page Spec: served over HTTP, not HTTPS: {page.url}')

    @pytest.mark.parametrize('boundary', ['', ' ', 'A', 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA', '', 'null', 'undefined', 'None', '0'])
    @pytest.mark.boundary
    def test_boundary_search_0042(self, page: Page, boundary: str):
        """Boundary values for Search."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(boundary, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error on boundary {boundary!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not visible on {page.url}')

    @pytest.mark.parametrize('value', ['url: /en/tutor-login', '98976564', '123456'])
    @pytest.mark.functional
    def test_fill_search_0043(self, page: Page, value: str):
        """Fill Search with valid data — must not crash."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        try:
            page.search.filter(visible=True).first.fill(value, timeout=5000)
            page.wait_for_timeout(300)
            assert '500' not in page.title(), f'Server error filling Search={value!r}: {page.url}'
        except AssertionError: raise
        except Exception: pytest.skip(f'Field Search not found on {page.url}')



# ==============================================================
# SPEC: Tutor Logout — https://dev.mehadedu.com/en/dashboard
# ==============================================================
class TestSpec_TutorLogout:
    """Auto-generated tests from specs/tutor_logout.md
    Page: Tutor Logout  URL: https://dev.mehadedu.com/en/dashboard
    Selectors: 6  Flows: 2  ECs: 4  Reqs: 5
    """
    PAGE_PATH = '/dashboard'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Logout loads without server error."""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__tutor_logout_0002(self, page: Page):
        """Flow: Flow 1: Tutor Logout"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = ''
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__verify_post_logout_state_0003(self, page: Page):
        """Flow: Flow 2: Verify Post-Logout State"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(1000)
        _exp_url = '/dashboard'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Sidebar shows tutor avatar and name while authenticated"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Clicking avatar shows My Profile and Logout popup"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Logout clears session cookie and localStorage"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: After logout, user is redirected to landing page"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Dashboard URLs redirect to login after logout"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0010(self, page: Page):
        """EC-01: Logout and navigate back"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.go_back()
        page.wait_for_timeout(500)
        assert '500' not in page.title(), f'Server error after back-button EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0012(self, page: Page):
        """EC-02: Logout in multiple tabs"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0014(self, page: Page):
        """EC-03: Session already expired"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        page.evaluate('sessionStorage.clear()')
        page.evaluate('localStorage.removeItem("token")')
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error on session EC: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0016(self, page: Page):
        """EC-04: Network error during logout"""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0017(self, page: Page):
        """Tutor Logout loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Logout took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0018(self, page: Page):
        """Tutor Logout page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Logout: page title is empty'
        assert '404' not in title, f'Tutor Logout: title says 404: {title}'
        assert '500' not in title, f'Tutor Logout: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0019(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Logout mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Logout mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0020(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Logout tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0021(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Logout uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0022(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Logout: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Logout: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0023(self, page: Page):
        """Tutor Logout body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Logout: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0024(self, page: Page):
        """Tutor Logout must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Logout: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0025(self, page: Page):
        """Tutor Logout must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Logout: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0026(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Logout 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0027(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Logout AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Logout AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0028(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Logout: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor My Profile — https://dev.mehadedu.com/en/dashboard/profile
# ==============================================================
class TestSpec_TutorMyProfile:
    """Auto-generated tests from specs/tutor_my_profile.md
    Page: Tutor My Profile  URL: https://dev.mehadedu.com/en/dashboard/profile
    Selectors: 10  Flows: 2  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/dashboard/profile'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/profile'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor My Profile loads without server error."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__edit_profile_information_0002(self, page: Page):
        """Flow: Flow 1: Edit Profile Information"""
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__update_phone_number_0003(self, page: Page):
        """Flow: Flow 2: Update Phone Number"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Profile accessible from sidebar avatar popup"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: First name, last name, display name fields editable"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Email field validates format"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Phone field validates number format"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Save updates profile immediately"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0009(self, page: Page):
        """REQ: REQ-06: Changes do not require admin approval for basic info"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0011(self, page: Page):
        """EC-01: Invalid email format"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0013(self, page: Page):
        """EC-02: Empty first name"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0015(self, page: Page):
        """EC-03: Phone number invalid"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0017(self, page: Page):
        """EC-04: First name over 30 chars"""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0018(self, page: Page):
        """Tutor My Profile loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor My Profile took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0019(self, page: Page):
        """Tutor My Profile page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor My Profile: page title is empty'
        assert '404' not in title, f'Tutor My Profile: title says 404: {title}'
        assert '500' not in title, f'Tutor My Profile: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0020(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor My Profile mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor My Profile mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0021(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor My Profile tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0022(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor My Profile uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0023(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor My Profile: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor My Profile: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0024(self, page: Page):
        """Tutor My Profile body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor My Profile: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0025(self, page: Page):
        """Tutor My Profile must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor My Profile: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0026(self, page: Page):
        """Tutor My Profile must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor My Profile: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0027(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor My Profile 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0028(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/profile'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor My Profile AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor My Profile AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0029(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor My Profile: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Notifications — https://dev.mehadedu.com/en/dashboard/notifications
# ==============================================================
class TestSpec_TutorNotifications:
    """Auto-generated tests from specs/tutor_notifications.md
    Page: Tutor Notifications  URL: https://dev.mehadedu.com/en/dashboard/notifications
    Selectors: 8  Flows: 2  ECs: 4  Reqs: 5
    """
    PAGE_PATH = '/dashboard/notifications'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/notifications'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Notifications loads without server error."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_notifications_0002(self, page: Page):
        """Flow: Flow 1: View Notifications"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__click_a_notification_0003(self, page: Page):
        """Flow: Flow 2: Click a Notification"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0004(self, page: Page):
        """REQ: REQ-01: Notifications page accessible from tutor dashboard"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0005(self, page: Page):
        """REQ: REQ-02: Admin profile approvals/rejections trigger notifications"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0006(self, page: Page):
        """REQ: REQ-03: Each notification shows action description"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0007(self, page: Page):
        """REQ: REQ-04: Notification can be clicked for details"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0008(self, page: Page):
        """REQ: REQ-05: Empty state shown when no notifications"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0010(self, page: Page):
        """EC-01: No notifications"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0012(self, page: Page):
        """EC-02: Many notifications"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0014(self, page: Page):
        """EC-03: Notification for profile rejection"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0016(self, page: Page):
        """EC-04: Mark all as read"""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0017(self, page: Page):
        """Tutor Notifications loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Notifications took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0018(self, page: Page):
        """Tutor Notifications page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Notifications: page title is empty'
        assert '404' not in title, f'Tutor Notifications: title says 404: {title}'
        assert '500' not in title, f'Tutor Notifications: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0019(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Notifications mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Notifications mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0020(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Notifications tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0021(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Notifications uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0022(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Notifications: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Notifications: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0023(self, page: Page):
        """Tutor Notifications body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Notifications: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0024(self, page: Page):
        """Tutor Notifications must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Notifications: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0025(self, page: Page):
        """Tutor Notifications must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Notifications: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0026(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Notifications 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0027(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/notifications'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Notifications AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Notifications AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0028(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/notifications', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Notifications: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Public Profile Spec — https://dev.mehadedu.com/en/tutor/profile
# ==============================================================
class TestSpec_TutorPublicProfile:
    """Auto-generated tests from specs/tutor_public_profile.md
    Page: Tutor Public Profile Spec  URL: https://dev.mehadedu.com/en/tutor/profile
    Selectors: 31  Flows: 13  ECs: 0  Reqs: 13
    """
    PAGE_PATH = '/tutor/profile'
    PAGE_URL  = 'https://dev.mehadedu.com/en/tutor/profile'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Public Profile Spec loads without server error."""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    def test_element_save_0002(self, page: Page):
        """Element must be present: Save"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.save.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Save' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    def test_element_message_0003(self, page: Page):
        """Element must be present: Message"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        try:
            page.message.first.wait_for(state='visible', timeout=12000)
        except Exception:
            pytest.skip(f"Element 'Message' not found on {page.url} — may be state-dependent or selector changed")

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_profile_page_loads_with_tutor_name_visib_0004(self, page: Page):
        """Flow: Profile page loads with tutor name visible"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_profile_stats_section_shows_three_metric_0005(self, page: Page):
        """Flow: Profile stats section shows three metrics"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_booking_widget_has_1_to_1_and_group_sess_0006(self, page: Page):
        """Flow: Booking widget has 1-to-1 and Group Session tabs"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_subject_radio_buttons_show_subjects_and__0007(self, page: Page):
        """Flow: Subject radio buttons show subjects and prices"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_weekly_availability_calendar_is_shown_0008(self, page: Page):
        """Flow: Weekly availability calendar is shown"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_timezone_dropdown_is_present_and_functio_0009(self, page: Page):
        """Flow: Timezone dropdown is present and functional"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_book_trial_lesson_button_is_visible_0010(self, page: Page):
        """Flow: Book Trial Lesson button is visible"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_save__favorite__and_message_buttons_are__0011(self, page: Page):
        """Flow: Save (favorite) and Message buttons are present"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Visible check on {page.url}'
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0012(self, page: Page):
        """REQ: REQ-01: the tutor name Mr.Saymon is displayed in the profile header"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_01_0013(self, page: Page):
        """REQ: REQ-02: Lessons Taught, Average Rating, and Response Rate are displayed with val"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_02_0014(self, page: Page):
        """REQ: REQ-03: two tabs 1-to-1 and Group Session are visible"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_03_0015(self, page: Page):
        """REQ: REQ-04: radio buttons for each subject with SAR price per hour are displayed"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_04_0016(self, page: Page):
        """REQ: REQ-05: a weekly calendar showing Mon-Sat columns with date numbers is visible, """
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_05_0017(self, page: Page):
        """REQ: REQ-06: a combobox showing the current timezone (e.g. Asia/Dhaka) is visible"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_06_0018(self, page: Page):
        """REQ: REQ-07: a Book Trial Lesson button is visible below the calendar"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_07_0019(self, page: Page):
        """REQ: REQ-08: a save button and a Message button are visible"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_08_0020(self, page: Page):
        """REQ: REQ-09: all subsections (About me, Languages, Subjects, Specialties, Education, """
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_09_0021(self, page: Page):
        """REQ: REQ-10: the average rating and review count are shown"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_10_0022(self, page: Page):
        """REQ: REQ-11: the login modal appears with Welcome back heading"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_11_0023(self, page: Page):
        """REQ: REQ-12: the login modal appears"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        try:
            page.save.wait_for(state='visible', timeout=6000)
        except Exception:
            pytest.fail(f'Required element not visible on {page.url}')

    @pytest.mark.requirement
    def test_req_12_0024(self, page: Page):
        """REQ: REQ-13: a 404 or not found message is displayed, or user is redirected"""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Redirect check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0025(self, page: Page):
        """Tutor Public Profile Spec loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Public Profile Spec took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0026(self, page: Page):
        """Tutor Public Profile Spec page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Public Profile Spec: page title is empty'
        assert '404' not in title, f'Tutor Public Profile Spec: title says 404: {title}'
        assert '500' not in title, f'Tutor Public Profile Spec: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0027(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Public Profile Spec mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Public Profile Spec mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0028(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Public Profile Spec tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0029(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Public Profile Spec uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0030(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Public Profile Spec: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Public Profile Spec: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0031(self, page: Page):
        """Tutor Public Profile Spec body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Public Profile Spec: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0032(self, page: Page):
        """Tutor Public Profile Spec must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Public Profile Spec: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0033(self, page: Page):
        """Tutor Public Profile Spec must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Public Profile Spec: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0034(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Public Profile Spec 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0035(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/tutor/profile'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Public Profile Spec AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Public Profile Spec AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0036(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/tutor/profile', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Public Profile Spec: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Reviews — https://dev.mehadedu.com/en/dashboard/reviews
# ==============================================================
class TestSpec_TutorReviews:
    """Auto-generated tests from specs/tutor_reviews.md
    Page: Tutor Reviews  URL: https://dev.mehadedu.com/en/dashboard/reviews
    Selectors: 10  Flows: 3  ECs: 4  Reqs: 6
    """
    PAGE_PATH = '/dashboard/reviews'
    PAGE_URL  = 'https://dev.mehadedu.com/en/dashboard/reviews'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Reviews loads without server error."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__view_reviews_overview_0002(self, page: Page):
        """Flow: Flow 1: View Reviews Overview"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__filter_reviews_by_student_name_0003(self, page: Page):
        """Flow: Flow 2: Filter Reviews by Student Name"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(200)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__filter_reviews_by_star_rating_0004(self, page: Page):
        """Flow: Flow 3: Filter Reviews by Star Rating"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Reviews page shows overall average rating"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Total review count is accurate"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: 5-star count and percentage displayed"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: Filter by student name narrows results"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: Star rating filter shows correct distribution"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Average rating calculated correctly from all reviews"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0012(self, page: Page):
        """EC-01: No reviews yet"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0014(self, page: Page):
        """EC-02: Filter with no matches"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0016(self, page: Page):
        """EC-03: Long student name in filter"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0018(self, page: Page):
        """EC-04: XSS in filter input"""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'XSS EC check: {page.url}'

    @pytest.mark.performance
    def test_load_time_0019(self, page: Page):
        """Tutor Reviews loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Reviews took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0020(self, page: Page):
        """Tutor Reviews page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Reviews: page title is empty'
        assert '404' not in title, f'Tutor Reviews: title says 404: {title}'
        assert '500' not in title, f'Tutor Reviews: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0021(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Reviews mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Reviews mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0022(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Reviews tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0023(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Reviews uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0024(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Reviews: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Reviews: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0025(self, page: Page):
        """Tutor Reviews body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Reviews: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0026(self, page: Page):
        """Tutor Reviews must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Reviews: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0027(self, page: Page):
        """Tutor Reviews must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Reviews: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0028(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Reviews 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0029(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/dashboard/reviews'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Reviews AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Reviews AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0030(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/dashboard/reviews', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Reviews: served over HTTP, not HTTPS: {page.url}')



# ==============================================================
# SPEC: Tutor Signup — Tutor Application Form — https://dev.mehadedu.com/en/tutor-login
# ==============================================================
class TestSpec_TutorSignup:
    """Auto-generated tests from specs/tutor_signup.md
    Page: Tutor Signup — Tutor Application Form  URL: https://dev.mehadedu.com/en/tutor-login
    Selectors: 15  Flows: 3  ECs: 10  Reqs: 13
    """
    PAGE_PATH = '/tutor-login'
    PAGE_URL  = 'https://dev.mehadedu.com/en/tutor-login'

    @pytest.mark.smoke
    @pytest.mark.navigation
    def test_page_loads_0001(self, page: Page):
        """Smoke: Tutor Signup — Tutor Application Form loads without server error."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        assert '500' not in page.title(), f'Server error: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'
        assert page.title() != '', f'Empty title on: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_1__tutor_login_from_become_a_tutor__0002(self, page: Page):
        """Flow: Flow 1: Tutor Login from Become-a-Tutor Page"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        page.wait_for_timeout(300)
        page.wait_for_timeout(1000)
        _exp_url = '/login'
        assert _exp_url in page.url, f'Expected URL with {_exp_url!r}, got {page.url}'
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_2__complete_tutor_registration_form_0003(self, page: Page):
        """Flow: Flow 2: Complete Tutor Registration Form"""
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(200)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        page.wait_for_timeout(300)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.functional
    @pytest.mark.flow
    def test_flow_flow_3__direct_tutor_login_page_0004(self, page: Page):
        """Flow: Flow 3: Direct Tutor Login Page"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error after flow: {page.url}'

    @pytest.mark.requirement
    def test_req_00_0005(self, page: Page):
        """REQ: REQ-01: Become a Tutor link must be visible in homepage header"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Visible check: {page.url}'

    @pytest.mark.requirement
    def test_req_01_0006(self, page: Page):
        """REQ: REQ-02: Apply Now navigates to /en/tutor-login"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_02_0007(self, page: Page):
        """REQ: REQ-03: Tutor login page shows heading Teacher Login"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_03_0008(self, page: Page):
        """REQ: REQ-04: OTP flow identical to student login"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_04_0009(self, page: Page):
        """REQ: REQ-05: First name maximum 30 characters"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_05_0010(self, page: Page):
        """REQ: REQ-06: Last name maximum 30 characters"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_06_0011(self, page: Page):
        """REQ: REQ-07: Email must be unique and valid format (max 254 chars)"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_07_0012(self, page: Page):
        """REQ: REQ-08: Bio maximum 500 characters"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_08_0013(self, page: Page):
        """REQ: REQ-09: Introduction video maximum 100 MB"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_09_0014(self, page: Page):
        """REQ: REQ-10: Certificate upload maximum 10 MB (image or PDF)"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_10_0015(self, page: Page):
        """REQ: REQ-11: Hourly rate between 0 and 10000"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_11_0016(self, page: Page):
        """REQ: REQ-12: Experience between 1 and 10 years"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.requirement
    def test_req_12_0017(self, page: Page):
        """REQ: REQ-13: All steps validated before proceeding to next"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(2500)
        assert '500' not in page.title(), f'Server error on REQ: {page.url}'
        assert '404' not in page.title(), f'Page not found: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_01_0019(self, page: Page):
        """EC-01: First name over 30 characters"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_02_0021(self, page: Page):
        """EC-02: Duplicate email address"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_03_0023(self, page: Page):
        """EC-03: Invalid email format"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_04_0025(self, page: Page):
        """EC-04: Bio over 500 characters"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_05_0027(self, page: Page):
        """EC-05: Hourly rate over 10000"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_06_0029(self, page: Page):
        """EC-06: Video file over 100 MB"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_07_0031(self, page: Page):
        """EC-07: Certificate over 10 MB"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_08_0033(self, page: Page):
        """EC-08: Empty required fields on Next"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_09_0035(self, page: Page):
        """EC-09: Graduation year not 4 digits"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.edge_case
    def test_ec_ec_10_0037(self, page: Page):
        """EC-10: Submit with incomplete form"""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        assert '500' not in page.title(), f'Server error on edge case: {page.url}'
        assert '404' not in page.title(), f'Page not found on edge case: {page.url}'

    @pytest.mark.performance
    def test_load_time_0038(self, page: Page):
        """Tutor Signup — Tutor Application Form loads within 20.0 seconds."""
        t0 = time.time()
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        elapsed = time.time() - t0
        assert elapsed < 20.0, f'Tutor Signup — Tutor Application Form took {elapsed:.1f}s (limit 20.0s)'

    @pytest.mark.smoke
    def test_title_not_empty_0039(self, page: Page):
        """Tutor Signup — Tutor Application Form page must have a non-empty, non-default title."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        title = page.title()
        assert title, f'Tutor Signup — Tutor Application Form: page title is empty'
        assert '404' not in title, f'Tutor Signup — Tutor Application Form: title says 404: {title}'
        assert '500' not in title, f'Tutor Signup — Tutor Application Form: title says 500: {title}'

    @pytest.mark.responsive
    def test_viewport_mobile_0040(self, page: Page):
        """Page must not return 5xx at 375px mobile viewport."""
        page.set_viewport_size({'width': 375, 'height': 667})
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Signup — Tutor Application Form mobile viewport 500: {title}'
        assert '502' not in title, f'Tutor Signup — Tutor Application Form mobile viewport 502: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.responsive
    def test_viewport_tablet_0041(self, page: Page):
        """Page must not return 5xx at 768px tablet viewport."""
        page.set_viewport_size({'width': 768, 'height': 1024})
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Signup — Tutor Application Form tablet viewport 500: {title}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.smoke
    def test_no_uncaught_js_error_0042(self, page: Page):
        """Page must not produce uncaught JavaScript exceptions."""
        errors: list = []
        page.on('pageerror', lambda e: errors.append(str(e)))
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        critical = [e for e in errors if not any(k in e.lower() for k in
                    ('script error', 'cross-origin', 'cancelled', 'network'))]
        assert not critical, f'Tutor Signup — Tutor Application Form uncaught JS errors: {critical[:3]}'

    @pytest.mark.navigation
    def test_url_no_redirect_loop_0043(self, page: Page):
        """Page navigation must settle on a real URL (no redirect loop / blank)."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1000)
        final_url = page.url
        assert final_url, f'Tutor Signup — Tutor Application Form: final URL is empty after navigation'
        assert 'about:blank' not in final_url, f'Tutor Signup — Tutor Application Form: stuck on about:blank'

    @pytest.mark.smoke
    def test_body_content_not_empty_0044(self, page: Page):
        """Tutor Signup — Tutor Application Form body must render visible text (not a blank/white page)."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        body = (page.text_content('body') or '').strip()
        assert len(body) > 50, f'Tutor Signup — Tutor Application Form: body text too short ({len(body)} chars) — blank page?'

    @pytest.mark.smoke
    def test_heading_exists_0045(self, page: Page):
        """Tutor Signup — Tutor Application Form must have at least one heading element (h1/h2)."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1500)
        h1 = page.locator('h1, h2, h3').count()
        assert h1 > 0, f'Tutor Signup — Tutor Application Form: no heading element found — page may not have loaded'

    @pytest.mark.security
    def test_no_debug_info_exposed_0046(self, page: Page):
        """Tutor Signup — Tutor Application Form must not expose debug/stack traces in page source."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(1200)
        html = page.content().lower()
        leaks = [k for k in ['traceback (most recent', 'at line ', 'syntaxerror:', 'fatal error']
                 if k in html]
        assert not leaks, f'Tutor Signup — Tutor Application Form: debug info in source: {leaks}'

    @pytest.mark.responsive
    def test_viewport_large_0047(self, page: Page):
        """Page must not error at 1920px wide desktop viewport."""
        page.set_viewport_size({'width': 1920, 'height': 1080})
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert '500' not in page.title(), f'Tutor Signup — Tutor Application Form 1920px 500: {page.title()}'
        page.set_viewport_size({'width': 1280, 'height': 720})

    @pytest.mark.i18n
    def test_arabic_url_not_500_0048(self, page: Page):
        """Arabic (/ar) version of this page must not return a server error."""
        ar_url = BASE_URL.replace('/en', '/ar', 1) + '/tutor-login'
        page.goto(ar_url, wait_until='commit', timeout=30000)
        page.wait_for_timeout(800)
        title = page.title()
        assert '500' not in title, f'Tutor Signup — Tutor Application Form AR version 500: {title}'
        assert '404' not in title or 'not found' in title.lower(), (
            f'Tutor Signup — Tutor Application Form AR version 404: {title}')

    @pytest.mark.security
    def test_https_no_mixed_content_0049(self, page: Page):
        """Page URL must be served over HTTPS, not HTTP."""
        page.goto(BASE_URL + '/tutor-login', wait_until='commit', timeout=30000)
        page.wait_for_timeout(600)
        assert page.url.startswith('https://') or page.url.startswith('http://localhost'), (
            f'Tutor Signup — Tutor Application Form: served over HTTP, not HTTPS: {page.url}')
